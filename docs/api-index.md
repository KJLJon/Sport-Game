# API index

<!-- Generated by `pnpm api`. Do not edit by hand. -->

1767 exported symbols across 205 modules under `src/`. Read this before
opening a source file — it is the cheapest way to find a name, a signature, or an owner.

Return types are as *declared*: `=> ?` means the source left it to inference.

## src/achievements/

- `src/achievements/ids.ts`  type AchievementId  = string
- `src/achievements/ids.ts`  interface AchievementSummary  { id, requirement }  — What a locked arcade tile shows: the condition, in the words the player will see.
- `src/achievements/ids.ts`  const ARCADE_UNLOCKS  — The ten arcade unlocks from `09` §3.2. Soccer's five are declared now even though its games
- `src/achievements/ids.ts`  type ArcadeUnlockKey  = keyof typeof ARCADE_UNLOCKS
- `src/achievements/ids.ts`  const ARCADE_UNLOCKS_BY_ID  : ReadonlyMap<AchievementId, AchievementSummary>  — Every arcade unlock, keyed by id — what the hub looks a locked tile up in.
- `src/achievements/ids.ts`  function requirementFor  (id) => string  — What earns an achievement, in the words a locked tile shows. Unknown ids get an honest fallback

## src/app/

- `src/app/canvas-host.ts`  interface CanvasSize  { width, height, pixelWidth, pixelHeight, dpr }
- `src/app/canvas-host.ts`  type CanvasResizeListener  = (size: CanvasSize) => void
- `src/app/canvas-host.ts`  interface CanvasHostOptions  { maxDpr, className }
- `src/app/canvas-host.ts`  function computeCanvasSize  (cssWidth, cssHeight, devicePixelRatio, maxDpr?) => CanvasSize  — Computes the backing-store size for a CSS box, clamped and integral.
- `src/app/canvas-host.ts`  class CanvasHost  (document, options?) => CanvasHost  — A canvas that resizes itself. Listeners fire only when the backing-store size actually changes,

- `src/app/orientation.ts`  type Orientation  = 'portrait' | 'landscape'
- `src/app/orientation.ts`  type LockOutcome  = 'locked' | 'unsupported' | 'rejected'
- `src/app/orientation.ts`  interface OrientationTarget  { orientation, width, height }  — The slice of `screen` we depend on. Narrowed so tests can supply a fake.
- `src/app/orientation.ts`  function readOrientation  (target, viewport) => Orientation  — Reads the current orientation. Prefers `screen.orientation.type`; falls back to comparing
- `src/app/orientation.ts`  function requestOrientationLock  (orientation, target) => Promise<LockOutcome>  — Asks the platform to lock orientation. Resolves with what actually happened rather than
- `src/app/orientation.ts`  function releaseOrientationLock  (target) => void
- `src/app/orientation.ts`  function shouldPromptRotate  (wanted, actual, lock) => boolean  — Decides whether to show the rotate prompt. A screen declaring `any` never prompts, and a match

- `src/app/router.ts`  interface RouteLocation  { path, segments, query, raw }  — A parsed location: the path segments and the query, both already decoded.
- `src/app/router.ts`  type RouteParams  = Readonly<Record<string, string>>  — Values captured from `:name` segments in a route pattern.
- `src/app/router.ts`  interface RouteMatch  { route, params, location }
- `src/app/router.ts`  interface RouteDefinition  { pattern, value }
- `src/app/router.ts`  function parseHash  (hash) => RouteLocation  — Parses a `location.hash` into a {@link RouteLocation}. Tolerates every shape a browser or a
- `src/app/router.ts`  function buildHash  (path, query?) => string  — Builds a hash string from a path and optional query. The inverse of {@link parseHash}.
- `src/app/router.ts`  function matchPattern  (pattern, location) => RouteParams | null  — Matches a location against a `:param` pattern, or returns `null`.
- `src/app/router.ts`  function resolveRoute  (routes, location) => RouteMatch<T> | null  — Resolves a location against an ordered route table. Literal segments beat parameters, so
- `src/app/router.ts`  type RouteListener  = (match: RouteMatch<T> | null, location: RouteLocation) => void
- `src/app/router.ts`  interface RouterOptions  { routes, fallbackPath, target }
- `src/app/router.ts`  interface RouterTarget  { location, addEventListener, removeEventListener }  — The slice of `window` the router needs — narrowed so tests can supply a fake.
- `src/app/router.ts`  class Router  (options) => Router  — Owns the current route and notifies listeners on change. Deliberately does not touch the DOM —
- `src/app/router.ts`  const HOME_LOCATION

- `src/app/routes.ts`  const ROUTES  : readonly RouteDefinition<ScreenDefinition>[]
- `src/app/routes.ts`  const TABS  : readonly TabDefinition[]  — `10` §7 — bottom tabs are Play · Squad · Store · Progress. Settings lives in the header.

- `src/app/screen.ts`  type ChromeMode  = 'full' | 'bare'  — Which chrome a screen wants around it. Live matches want none of it.
- `src/app/screen.ts`  type ScreenOrientation  = 'portrait' | 'landscape' | 'any'  — Orientation a screen is designed for; the shell prompts when the device disagrees.
- `src/app/screen.ts`  interface ScreenContext  { host, params, query, navigate }
- `src/app/screen.ts`  interface Screen  { mount, unmount }
- `src/app/screen.ts`  interface ScreenDefinition  { id, title, chrome, orientation, load }

- `src/app/shell.ts`  interface TabDefinition  { id, label, path, icon }
- `src/app/shell.ts`  interface ShellOptions  { root, router, tabs, window }
- `src/app/shell.ts`  class AppShell  (options) => AppShell

## src/athletes/

- `src/athletes/attributes.ts`  interface BudgetState  { total, budget, remaining, withinBudget, outOfRange, requiresSandbox }  — What the profile editor's live budget meter shows (US-5.1).
- `src/athletes/attributes.ts`  function budgetState  (attributes) => BudgetState
- `src/athletes/attributes.ts`  interface CreationVerdict  { allowed, sandbox, budget, reason }  — The decision the editor's Save button makes. Over budget with sandbox mode off is the only
- `src/athletes/attributes.ts`  function judgeCreation  (attributes, sandboxMode) => CreationVerdict
- `src/athletes/attributes.ts`  function clampToCreationRange  (attributes) => Attributes  — Clamps a spread into the editor's per-attribute range without touching the total rule.
- `src/athletes/attributes.ts`  function fitToBudget  (attributes) => Attributes  — Scales a spread down proportionally until it fits the budget. This is the editor's "fit to
- `src/athletes/attributes.ts`  function spreadAttributes  (rng, total, bounds?) => Attributes  — Draws eleven attributes summing to `total`, shaped rather than flat.
- `src/athletes/attributes.ts`  function rollAttributes  (rng, rarity) => Attributes  — A seeded roll for a rarity band (`05` §4). The band sets the range; the shape is the draw.
- `src/athletes/attributes.ts`  function rarityForTotal  (total) => Rarity  — The rarity an attribute total implies, used when a roster file omits it (`05` §8) and to label
- `src/athletes/attributes.ts`  function rollTraits  (rng, rarity) => TraitId[]  — Trait count and identities for a rarity (`05` §4). Never duplicates a trait.
- `src/athletes/attributes.ts`  function rollPhysical  (rng) => { heightCm: number; weightKg: number; age: number; }  — A body for a rolled athlete, weight tracking height so the numbers read as one person.
- `src/athletes/attributes.ts`  function rollWithinBudget  (rng) => Attributes  — The editor's "roll" button: a spread that lands on budget and inside the editor's per-attribute

- `src/athletes/condition.ts`  type UnavailableReason  = 'injured' | 'suspended'  — Why an athlete cannot be selected, or `null` when they can.
- `src/athletes/condition.ts`  interface Availability  { available, reason, label, remaining }
- `src/athletes/condition.ts`  function availability  (athlete, now) => Availability  — Whether this athlete can be picked, and why not. Suspension outranks injury in the message when
- `src/athletes/condition.ts`  type StaminaBand  = 'fresh' | 'working' | 'tiring' | 'spent'  — Plain-language stamina bands, so the bar is never colour alone (`10` §11).
- `src/athletes/condition.ts`  function staminaBand  (stamina) => StaminaBand
- `src/athletes/condition.ts`  function fatigueMultiplier  (stamina) => number  — How much of themselves a tired athlete still has, `0`–`1`.
- `src/athletes/condition.ts`  function staminaDrain  (options) => number  — Stamina drained by playing. The athlete's own `stamina` attribute is what makes this personal:
- `src/athletes/condition.ts`  function staminaRecovery  (options) => number  — Stamina recovered between matches. Recovery is faster than drain, so a squad is not a treadmill.
- `src/athletes/condition.ts`  function afterMatch  (athlete, options) => AthleteCondition  — The condition after playing `minutes`, plus the suspension that a served match works off.
- `src/athletes/condition.ts`  function afterRest  (athlete, matchesRested, now) => AthleteCondition  — The condition after resting through `matchesRested` matches.
- `src/athletes/condition.ts`  interface Injury  { until, days }
- `src/athletes/condition.ts`  function rollInjury  (rng, options) => Injury | null  — Whether this contact injured the athlete, and for how long.
- `src/athletes/condition.ts`  function withInjury  (condition, injury) => AthleteCondition  — Applies an injury to a condition. Never shortens one already running.
- `src/athletes/condition.ts`  function withSuspension  (condition, matches) => AthleteCondition  — Applies a suspension. Suspensions accumulate — two red cards is not one suspension.

- `src/athletes/coupling.ts`  function lostness  (familiarity) => number  — How lost an athlete is, `0` at home and `1` for a complete novice. The curve is deliberately
- `src/athletes/coupling.ts`  interface Coupling  { lostness, decisionNoise, controlError, reactionPenalty }  — Everything the sim reads off familiarity. All three are zero for an athlete at home.
- `src/athletes/coupling.ts`  function couplingFor  (familiarity) => Coupling
- `src/athletes/coupling.ts`  const NO_COUPLING  : Coupling  — Nothing coupled — an athlete in their own sport, and the default before T-3.17 wires rosters.
- `src/athletes/coupling.ts`  function degradeControl  (control, coupling) => number  — Scales a `0`–`1` control quality down by the athlete's control error. Used on catches, first
- `src/athletes/coupling.ts`  function delayReaction  (chancePerStep, coupling) => number  — Scales a per-step chance of acting on a decision. A lost athlete sees the pass late; they do not
- `src/athletes/coupling.ts`  function timingSpread  (coupling) => number  — Widens a timing window's spread — how far off the ideal release a CPU athlete lands. Returns the

- `src/athletes/create.ts`  const ATHLETE_SCHEMA_VERSION  — The record version new athletes are written at. Bumped with its migration (`05` §9).
- `src/athletes/create.ts`  interface CreateAthleteOptions  { displayName, primarySport, attributes, heightCm, weightKg, age, handedness, jerseyNumber, nationalityLabel, portraitBlobId, …+8 }
- `src/athletes/create.ts`  function createAthlete  (options) => Athlete
- `src/athletes/create.ts`  function rollAthlete  (rng, options) => Athlete  — A fully rolled athlete for a rarity band — packs, and the starter roster. Everything random
- `src/athletes/create.ts`  function newId  () => string  — A random identifier, from the platform's generator — not the sim's, which must stay seeded.
- `src/athletes/create.ts`  function seededId  (rng) => string  — A reproducible identifier, so a seeded roster is identical on every device.

- `src/athletes/derivation.ts`  interface SportRatingTables  { weights, physicalModifiers, positionWeights }  — Everything about a sport that derivation needs. Supplied by the sport module.
- `src/athletes/derivation.ts`  type DerivedRatings  = Readonly<Record<string, number>>  — Derived rating name → 1–99.
- `src/athletes/derivation.ts`  function familiarityMultiplier  (familiarity) => number  — The familiarity gate (`05` §3). A total novice plays at 55% of their athletic ceiling; at full
- `src/athletes/derivation.ts`  function skillBonus  (subSkill) => number  — Learned sub-skill, 0–20, worth 0.75 rating points each (`05` §3).
- `src/athletes/derivation.ts`  function physicalModifier  (rating, heightCm, weightKg, modifiers) => number  — The physical adjustment for one rating, in rating points.
- `src/athletes/derivation.ts`  function rawRating  (rating, attributes, heightCm, weightKg, tables) => number  — The athletic ceiling: what the athlete would rate at with full familiarity and nothing learned.
- `src/athletes/derivation.ts`  interface DeriveOptions  { familiarity, subSkills }
- `src/athletes/derivation.ts`  function deriveRatings  (athlete, sport, tables, options?) => DerivedRatings  — Every rating the sport defines, for this athlete, in the table's own order.
- `src/athletes/derivation.ts`  function projectRatings  (athlete, sport, tables, familiarity?) => DerivedRatings  — The same athlete at the sport's familiarity cap with nothing learned — their ceiling (T-3.9).
- `src/athletes/derivation.ts`  interface Contribution  { attribute, weight, value, points }  — One attribute's share of a rating, for the card's "why this rating" (US-5.4).
- `src/athletes/derivation.ts`  interface RatingExplanation  { rating, contributions, physical, raw, familiarity, familiarityMultiplier, familiarityPenalty, skillBonus, final }
- `src/athletes/derivation.ts`  function explainRating  (athlete, sport, rating, tables, options?) => RatingExplanation  — Why a rating is what it is. Everything the athlete card needs to say it in plain language,
- `src/athletes/derivation.ts`  function overall  (ratings, positionWeights) => number  — `overall(sport, position) = Σ_r ( positionWeight[position][r] × rating_r )` — `05` §3.4.
- `src/athletes/derivation.ts`  interface PositionFit  { position, overall, fit, warn }
- `src/athletes/derivation.ts`  function positionFits  (ratings, positionWeights) => PositionFit[]  — Every position, best first. The fit is relative to the athlete's *own* best position, so it
- `src/athletes/derivation.ts`  interface SportOverall  { overall, position }
- `src/athletes/derivation.ts`  function sportOverall  (athlete, sport, tables, options?) => SportOverall  — One number for "how good is this athlete at this sport", taken at their *best* position rather
- `src/athletes/derivation.ts`  function bestPosition  (ratings, positionWeights) => PositionFit | null  — The athlete's best position for a sport, or `null` when the sport declares none.

- `src/athletes/explain.ts`  function humanise  (id) => string  — `ballHandling` → `Ball handling`. Acronym-free by design; the overrides cover the rest.
- `src/athletes/explain.ts`  function attributeLabel  (id) => string
- `src/athletes/explain.ts`  function ratingLabel  (id) => string
- `src/athletes/explain.ts`  function attributeLabels  () => readonly { id: AttributeId; label: string }[]  — All eleven, labelled, in `05` §2.1's order — the radar's axes and the editor's sliders.
- `src/athletes/explain.ts`  interface ContributionLine  { attribute, label, value, share, text }
- `src/athletes/explain.ts`  function contributionLines  (explanation, limit?) => readonly ContributionLine[]  — The top contributors to a rating, largest first. Shares are of the *attribute* sum rather than
- `src/athletes/explain.ts`  function points  (value) => string  — `1 point`, `2 points`. Small, but a card that says "1 points" reads as unfinished.
- `src/athletes/explain.ts`  function physicalLine  (explanation) => string | null  — "Height adds 7 points" / "Height costs 3 points" — the card's physical line (`05` §3.1).
- `src/athletes/explain.ts`  function ceilingSentence  (multiplier) => string  — `10` §6, near enough word for word: "playing at 58% of their athletic ceiling". An athlete at
- `src/athletes/explain.ts`  function familiarityLine  (sportLabel, familiarity, rank) => string  — What the familiarity badge says, in words rather than in colour (`10` §11).
- `src/athletes/explain.ts`  function provenanceLine  (source, createdAt, locale?) => string  — "Created 12 March 2026" — the provenance line `10` §6 asks for.

- `src/athletes/familiarity.ts`  function familiarityCap  (athlete, sport) => number  — The ceiling for a sport: the primary sport alone can reach 100 (`05` §3.3).
- `src/athletes/familiarity.ts`  function ageFactor  (age) => number  — How quickly this athlete picks things up, by age (`05` §3.3). A 22-year-old learns at the full
- `src/athletes/familiarity.ts`  function sportComplexity  (sport) => number  — How hard a sport is to pick up (`05` §3.3). Unknown sports learn at basketball's rate.
- `src/athletes/familiarity.ts`  function learningMinutes  (gameMinutes, clockCompression) => number  — Real minutes of play from a box-score figure. `05` §3.3's growth is per real minute; the athlete
- `src/athletes/familiarity.ts`  function familiarityGain  (options) => number  — The gain from one stint. Diminishing in current familiarity, so the first matches move the
- `src/athletes/familiarity.ts`  interface FamiliarityChange  { before, after, gained, cap, atCap }  — What a stint did, in the terms the post-match screen reports it (US-5.3).
- `src/athletes/familiarity.ts`  function applyMinutes  (athlete, sport, minutes) => { readonly skill: SportSkill; readonly change: FamiliarityChang…  — Applies minutes to an athlete's familiarity in one sport. Pure: it returns the change and the
- `src/athletes/familiarity.ts`  function projectFamiliarity  (options) => number  — The same athlete after `matches` identical stints. Used by the compare view to answer "how long
- `src/athletes/familiarity.ts`  function matchesToReach  (options) => number | null  — How many matches of `minutesPerMatch` it takes to reach `target`, or `null` if never.
- `src/athletes/familiarity.ts`  type FamiliarityBand  = 'novice' | 'learning' | 'competent' | 'comfortable' | 'natural'  — Plain-language bands for the familiarity badge (US-5.2). Colour alone must never carry this
- `src/athletes/familiarity.ts`  function familiarityBand  (familiarity) => FamiliarityBand
- `src/athletes/familiarity.ts`  function startingFamiliarity  (athlete, sport) => number  — The starting familiarity a sport gets for an athlete who has never played it (`05` §3.3).

- `src/athletes/portrait.ts`  interface Size  { width, height }  — A pixel size. Always integers — a canvas cannot be sized in fractions of a pixel.
- `src/athletes/portrait.ts`  const PORTRAIT_MAX_EDGE  — The longest edge a stored portrait may have. Big enough to look sharp in the full athlete card, small enough …
- `src/athletes/portrait.ts`  const PORTRAIT_MIME  — Re-encode target. WebP first — smaller for the same look — with JPEG as the universal fallback.
- `src/athletes/portrait.ts`  const PORTRAIT_FALLBACK_MIME
- `src/athletes/portrait.ts`  const PORTRAIT_QUALITY
- `src/athletes/portrait.ts`  function fittedSize  (width, height, maxEdge) => Size  — The size a `width × height` image lands at once its longest edge is capped to `maxEdge`,
- `src/athletes/portrait.ts`  interface DownscaleOptions  { maxEdge, mimeType, quality }
- `src/athletes/portrait.ts`  function downscalePortrait  (source, options?) => Promise<Blob>  — Reads `source` — whatever `<input type="file" accept="image/*">` or a camera capture handed

- `src/athletes/presets.ts`  interface AttributePreset  { id, label, description, attributes }
- `src/athletes/presets.ts`  const ATTRIBUTE_PRESETS  : readonly AttributePreset[]  — Six archetypes spanning the eleven attributes' obvious shapes. Every spread was hand-tuned to
- `src/athletes/presets.ts`  function presetById  (id) => AttributePreset | undefined

- `src/athletes/progression.ts`  interface ProgressionReport  { sport, familiarity, skill, minutes }  — What one athlete's session did, in the terms the post-match screen reports it (US-5.3).
- `src/athletes/progression.ts`  interface ProgressionResult  { athlete, skill, report }
- `src/athletes/progression.ts`  function applyPlay  (athlete, sport, session) => ProgressionResult  — Applies a session to one athlete. Pure: it returns a new athlete rather than mutating, so the
- `src/athletes/progression.ts`  function applyMatch  (options) => Map<EntityId, ProgressionResult>  — The whole squad after a match: who played, for how long, and what the match's events were worth.
- `src/athletes/progression.ts`  function learnedSubSkills  (athlete, sport) => readonly { readonly rating: string; readonly points: number }[]  — The sub-skills an athlete has actually learned in a sport, largest first (US-5.3, T-3.8).

- `src/athletes/repository.ts`  const ATHLETE_INDEX  — The store's indexes, named so a caller never spells one wrong. `05` §1, T-0.11.
- `src/athletes/repository.ts`  interface DeletedAthlete  { athlete, deletedAt }  — Undo for a deletion (US-5.5) — the record, so restoring it is a `put` of exactly what was there.
- `src/athletes/repository.ts`  class AthleteRepository  (db) => AthleteRepository
- `src/athletes/repository.ts`  function matchesQuery  (athlete, query) => boolean  — Case- and accent-insensitive substring match on the display name. Lives here rather than in the
- `src/athletes/repository.ts`  function normaliseForSearch  (text) => string

- `src/athletes/roster-import.ts`  const CURRENT_FORMAT_VERSION  — The highest `formatVersion` this build understands (`05` §8).
- `src/athletes/roster-import.ts`  const ROSTER_IMPORT_SPORTS  : readonly SportId[]  — The sport ids the wire format documents (`05` §8's comment: `basketball|soccer|hockey|football`).
- `src/athletes/roster-import.ts`  interface ImportIssue  { record, field, message, severity }
- `src/athletes/roster-import.ts`  interface ImportResult  { athletes, issues, accepted, rejected, rosterName }
- `src/athletes/roster-import.ts`  function parseRosterFile  (text, now?) => ImportResult  — Validates and converts the text of a roster file (`05` §8). Never throws — a file that cannot be
- `src/athletes/roster-import.ts`  type ImportClassification  = 'new' | 'conflict'
- `src/athletes/roster-import.ts`  type ConflictReason  = 'custodyId' | 'name-and-sport'  — Why an incoming record was flagged, so the conflict prompt can say something specific.
- `src/athletes/roster-import.ts`  interface ClassifiedImport  { athlete, classification, conflictsWith, reason }
- `src/athletes/roster-import.ts`  function classifyImports  (incoming, existing) => ClassifiedImport[]  — Classifies each successfully-parsed athlete against the existing roster, so the importer can
- `src/athletes/roster-import.ts`  type ConflictResolution  = 'skip' | 'replace' | 'keep-both'  — What the conflict prompt's three choices do to one conflicting record (US-5.7).
- `src/athletes/roster-import.ts`  function resolveConflict  (conflict, resolution) => Athlete | null  — Applies one conflict's resolution, returning the record to write or `null` for "don't write
- `src/athletes/roster-import.ts`  function mergeRoster  (classified, resolutions) => Athlete[]  — The whole merge in one call: every `new` record is written as-is, every `conflict` goes through

- `src/athletes/roster-query.ts`  type SortKey  = 'name' | 'rating' | 'rarity' | 'recent' | 'familiarity'
- `src/athletes/roster-query.ts`  type SortDirection  = 'asc' | 'desc'
- `src/athletes/roster-query.ts`  interface RosterFilter  { query, sports, rarities, availableOnly, sandbox }
- `src/athletes/roster-query.ts`  function filterRoster  (athletes, filter, now) => Athlete[]  — Every athlete matching `filter`, in the input's order. Never mutates `athletes` — the caller's
- `src/athletes/roster-query.ts`  function sortRoster  (athletes, key, direction?, sport?) => Athlete[]  — A new, sorted array — `athletes` is never mutated. Total and stable: every pair of athletes
- `src/athletes/roster-query.ts`  interface RosterCounts  { total, bySport, byRarity, sandbox, unavailable }
- `src/athletes/roster-query.ts`  function rosterCounts  (athletes, now?) => RosterCounts  — Summary counts for the roster browser's filter chips and header line. `now` defaults to

- `src/athletes/starter-roster.ts`  const STARTER_ROSTER_SIZE  — Total athletes generated by `generateStarterRoster`. Enough for:
- `src/athletes/starter-roster.ts`  function generateStarterRoster  (seed?) => Athlete[]  — Generates the starter roster deterministically. The same seed always produces a byte-identical

- `src/athletes/tuning.ts`  const CREATION  — `05` §2.1 — the rules a user-created athlete is comparable under.
- `src/athletes/tuning.ts`  const CREATION_DEFAULTS  — Defaults for fields the editor leaves alone, near the middle of each range (`05` §2).
- `src/athletes/tuning.ts`  interface RarityBand  { totalMin, totalMax, traitsMin, traitsMax, valueBase }
- `src/athletes/tuning.ts`  const RARITY_BANDS  — `05` §4, in ascending order. The bands overlap deliberately: rarity sets the roll range and the
- `src/athletes/tuning.ts`  const ROLL  — Shape of a rolled athlete's attribute spread. A flat draw makes every athlete the same
- `src/athletes/tuning.ts`  const DERIVATION  — The derivation curve (`05` §3, §3.4). `famMult(f) = 0.55 + 0.45 × (f/100)^0.75`, so a total
- `src/athletes/tuning.ts`  const FAMILIARITY  — Familiarity growth (`05` §3.3):
- `src/athletes/tuning.ts`  const XP  — Sport skill XP (`05` §3.3): `xpFor(level) = 100 × level^1.6`, XP from minutes plus events, and
- `src/athletes/tuning.ts`  const CONDITION  — Stamina, injury, and suspension (US-6.3). `05` gives the `condition` fields but no rates, so
- `src/athletes/tuning.ts`  const COUPLING  — Behavioural coupling (`05` §3.3's last paragraph). Every value is a *maximum*, reached only by a

- `src/athletes/types.ts`  const ATTRIBUTE_IDS  — The eleven sport-neutral attributes, in `05` §2.1's order. The order is part of the contract:
- `src/athletes/types.ts`  type AttributeId  = (typeof ATTRIBUTE_IDS)[number]
- `src/athletes/types.ts`  type Attributes  = Record<AttributeId, number>  — All eleven, always. A partial attribute set is not an athlete.
- `src/athletes/types.ts`  const RARITIES
- `src/athletes/types.ts`  type Rarity  = (typeof RARITIES)[number]
- `src/athletes/types.ts`  const TRAIT_IDS  — Traits are named modifiers with readable effects (`05` §4). Their effects land with the systems
- `src/athletes/types.ts`  type TraitId  = (typeof TRAIT_IDS)[number]
- `src/athletes/types.ts`  type Handedness  = 'left' | 'right' | 'both'
- `src/athletes/types.ts`  const ATHLETE_SOURCES  — Where a record came from. `05` §2 — drives editability, and the P2P custody chain later.
- `src/athletes/types.ts`  type AthleteSource  = (typeof ATHLETE_SOURCES)[number]
- `src/athletes/types.ts`  interface SportSkill  { familiarity, level, xp, subSkills, minutesPlayed }  — Per-sport learned state (`05` §3.3). Sub-skills are keyed by derived-rating name.
- `src/athletes/types.ts`  interface AthleteCondition  { stamina, injuredUntil, suspendedGames }  — Availability (`05` §2). Timestamps are epoch ms; `stamina` is 0–100.
- `src/athletes/types.ts`  interface Athlete  { id, schemaVersion, displayName, portraitBlobId, nationalityLabel, jerseyNumber, heightCm, weightKg, handedness, age, …+11 }  — `05` §2, field for field.
- `src/athletes/types.ts`  const ATHLETE_BOUNDS  — Structural bounds. These are what a record is *valid* inside — not the creation budget, which is
- `src/athletes/types.ts`  const STARTING_FAMILIARITY  — Familiarity a sport starts at, by whether it is the athlete's primary (`05` §3.3).
- `src/athletes/types.ts`  function clamp  (value, min, max) => number
- `src/athletes/types.ts`  function isAttributeId  (value) => value is AttributeId
- `src/athletes/types.ts`  function isRarity  (value) => value is Rarity
- `src/athletes/types.ts`  function isTraitId  (value) => value is TraitId
- `src/athletes/types.ts`  function isHandedness  (value) => value is Handedness
- `src/athletes/types.ts`  function isAthleteSource  (value) => value is AthleteSource
- `src/athletes/types.ts`  function attributeTotal  (attributes) => number  — The sum every budget rule is stated against (`05` §2.1).
- `src/athletes/types.ts`  function newSportSkill  (familiarity) => SportSkill  — A fresh skill record for a sport the athlete has never played.
- `src/athletes/types.ts`  function sportSkillFor  (athlete, sport) => SportSkill  — The skill record for `sport`, creating it at the right starting familiarity if this is the first
- `src/athletes/types.ts`  function isAvailable  (athlete, now) => boolean  — Availability at a moment in time (`03` T-3.13 builds the UI on this).
- `src/athletes/types.ts`  function isEditable  (athlete, sandboxMode) => boolean  — Whether the profile editor may change this athlete. Pack, market, and peer athletes are locked

- `src/athletes/xp.ts`  function xpForLevel  (level) => number  — XP to advance *from* `level` to the next one — so level 1 → 2 costs exactly 100, and 19 → 20
- `src/athletes/xp.ts`  function cumulativeXpForLevel  (level) => number  — Total XP to go from level 1 to `level`. What a progress bar's "career" mode would show.
- `src/athletes/xp.ts`  interface LevelProgress  { level, xp, intoLevel, levelCost, fraction, atCap }  — Where an athlete sits within their current level, for the progress bar (US-5.3).
- `src/athletes/xp.ts`  function levelProgress  (skill) => LevelProgress
- `src/athletes/xp.ts`  function xpFromMinutes  (minutes) => number  — XP from minutes on the field, the half of `05` §3.3 that does not depend on doing anything.
- `src/athletes/xp.ts`  interface XpAward  { entity, rating, xp }  — One award, before it is banked.
- `src/athletes/xp.ts`  function awardsForEvent  (sportEvent, table) => XpAward[]  — What one event is worth, to whom. An event can pay two athletes: the rule's `rating`/`xp` go to
- `src/athletes/xp.ts`  interface SkillSession  { minutes, actions, xp }  — One athlete's share of a session: what they did, and what it was worth.
- `src/athletes/xp.ts`  function emptySession  (minutes?) => SkillSession
- `src/athletes/xp.ts`  function collectSession  (events, table, minutesByEntity) => Map<EntityId, SkillSession>  — Folds a session's events into per-entity totals.
- `src/athletes/xp.ts`  interface SkillChange  { xpGained, levelBefore, levelAfter, levelsGained, subSkillsGained, pointsWasted }  — What a session did to one athlete's skill, in the terms the post-match screen reports it.
- `src/athletes/xp.ts`  function applySession  (skill, session) => { readonly skill: SportSkill; readonly change: SkillChange }  — Applies a session to one sport's skill record. Pure — it returns the new record and the change,
- `src/athletes/xp.ts`  function applySessionTo  (athlete, sport, session) => { readonly skill: SportSkill; readonly change: SkillChange }  — Reads the athlete's record for a sport and applies a session to it.

## src/engine/ai/

- `src/engine/ai/decider.ts`  interface DeciderOptions  { latencyMs, commitment }
- `src/engine/ai/decider.ts`  interface DecisionState  { committed, pendingKey, pendingSince }  — What an actor is doing and what it is starting to think about instead.
- `src/engine/ai/decider.ts`  interface Decider  { decide, inspect, forget, reset }
- `src/engine/ai/decider.ts`  function createDecider  (options?) => Decider<T>  — Creates a decider. One per team is the intended shape — the actor id keys the memory, and a
- `src/engine/ai/decider.ts`  interface AiTuning  { latencyMs, noise, commitment, threshold }  — The tuning a difficulty level hands the framework. T-7.7 builds these from `06` §7's table; the
- `src/engine/ai/decider.ts`  function deciderFor  (tuning, rng) => Decider<T>  — Convenience for the common case: one decider per side from a level's tuning and a forked rng.

- `src/engine/ai/execution.ts`  function reactionChance  (latencyMs, dtMs) => number  — The chance, per simulation step, that a CPU athlete acts on something it has decided — so that
- `src/engine/ai/execution.ts`  function reacted  (rng, latencyMs, dtMs) => boolean  — Rolls `reactionChance()`. Sports call this once per step per athlete waiting to act.
- `src/engine/ai/execution.ts`  function aimError  (rng, error, spread) => number  — Angular error on an executed action, in radians. `error` is the level's `executionError` (`0–1`);
- `src/engine/ai/execution.ts`  function powerError  (rng, error, spread) => number  — Scalar error on a magnitude — a pass hit too hard, a shot released short. Multiplicative.
- `src/engine/ai/execution.ts`  function contestChance  (base, aggression) => number  — Scales a per-step chance to compete for the ball by the level's aggression. `06` §7's row is

- `src/engine/ai/marking.ts`  interface Marker  { id, x, y, urgency }
- `src/engine/ai/marking.ts`  interface Markable  { id, x, y, danger }
- `src/engine/ai/marking.ts`  type Marks  = ReadonlyMap<EntityId, EntityId>  — Who is marking whom: marker id → attacker id.
- `src/engine/ai/marking.ts`  interface MarkOptions  { previous, hysteresis, range }
- `src/engine/ai/marking.ts`  function assignMarks  (markers, attackers, options?) => Map<EntityId, EntityId>  — Assigns each marker at most one attacker, and each attacker at most one marker.
- `src/engine/ai/marking.ts`  function goalSideSpot  (mark, goal, standoff, out?) => { x: number; y: number }  — Where to stand to mark somebody: on the line between them and the goal being defended, a

- `src/engine/ai/roles.ts`  const PlayPhase
- `src/engine/ai/roles.ts`  type PlayPhase  = (typeof PlayPhase)[keyof typeof PlayPhase]
- `src/engine/ai/roles.ts`  const PLAY_PHASES  : readonly PlayPhase[]
- `src/engine/ai/roles.ts`  const RoleJob  — What a role is responsible for, in the phase it holds this duty. Sports read this and decide what
- `src/engine/ai/roles.ts`  type RoleJob  = (typeof RoleJob)[keyof typeof RoleJob]
- `src/engine/ai/roles.ts`  interface Fraction  { x, y }  — A point as a fraction of the field: `x` from the end this side defends, `y` across it.
- `src/engine/ai/roles.ts`  interface Spot  { x, y }  — A point in world units (metres).
- `src/engine/ai/roles.ts`  interface RoleDuty  { anchor, ballShade, leash, job, urgency }
- `src/engine/ai/roles.ts`  type RoleDuties  = Readonly<Record<PlayPhase, RoleDuty>>  — One role's duties, one per phase. Every phase is present: a role always has a job.
- `src/engine/ai/roles.ts`  type DutyTable  = Readonly<Record<string, RoleDuties>>  — A sport's whole table: role id → its four duties.
- `src/engine/ai/roles.ts`  interface FieldSize  { width, height }
- `src/engine/ai/roles.ts`  function phaseFor  (options) => PlayPhase  — The phase of play, from possession alone.
- `src/engine/ai/roles.ts`  function dutySpot  (duty, ball, field, side, out?) => Spot  — Where a role should be, in metres.
- `src/engine/ai/roles.ts`  function spaced  (target, teammates, minGap, field) => Spot  — Nudges a target away from teammates who are already on it.
- `src/engine/ai/roles.ts`  function dutyFor  (table, role, phase) => RoleDuty | undefined  — The duty for a role in a phase, or `undefined` for a role the table does not know.
- `src/engine/ai/roles.ts`  function duties  (base, overrides?) => RoleDuties  — Builds a role's four duties from one base duty and per-phase overrides. Most roles differ from

- `src/engine/ai/team.ts`  interface TeamActor  { id, role, x, y }  — One of ours, as the team layer needs to see them.
- `src/engine/ai/team.ts`  interface Opponent  { id, x, y }  — One of theirs. The team layer needs no more of an opponent than where they are.
- `src/engine/ai/team.ts`  interface TeamSituation  { mates, opponents, ball, possession, carrier, trigger }  — The world as one team sees it, this tick.
- `src/engine/ai/team.ts`  const TeamIntent  — Why an athlete is where the plan says, beyond the duty.
- `src/engine/ai/team.ts`  type TeamIntent  = (typeof TeamIntent)[keyof typeof TeamIntent]
- `src/engine/ai/team.ts`  interface Assignment  { actor, role, job, intent, target, mark, urgency }  — What one athlete is doing this tick.
- `src/engine/ai/team.ts`  interface TeamPlan  { phase, sinceChange, pressing, pressers, assignments }
- `src/engine/ai/team.ts`  interface TeamShape  { spacing, compactness, pressCount, pressLine, pressRange, markStandoff, markHysteresis, markRange, helpCount, helpRange, …+1 }  — How this team plays, in the numbers the layer actually reads. A sport builds one of these from
- `src/engine/ai/team.ts`  const TEAM_SHAPE  : TeamShape  — Defaults on a soccer-sized pitch. Every one of them is a sport's to override — they exist so a
- `src/engine/ai/team.ts`  interface TeamOptions  { side, table, field, transitionSteps, attackFrom, goal, shape }
- `src/engine/ai/team.ts`  interface TeamCoordinator  { plan, last, reset }
- `src/engine/ai/team.ts`  function advance  (x, width, side) => number  — How far up this side's attacking direction a point is, `0` (our goal line) to `1` (theirs).
- `src/engine/ai/team.ts`  function compact  (targets, factor) => void  — Pulls a set of targets towards their own centre. Applied to the whole unit rather than per role,
- `src/engine/ai/team.ts`  function nearestOnSegment  (point, from, to) => { readonly x: number; readonly y: number; readonly distance: nu…  — The closest point to `point` on the segment `from`–`to`, and how far away it is.
- `src/engine/ai/team.ts`  function createTeam  (options) => TeamCoordinator  — Creates a team's coordinator. One per side, kept for the life of the match: the possession clock
- `src/engine/ai/team.ts`  function assignmentFor  (plan, actor) => Assignment | undefined  — One athlete's assignment out of a plan, or `undefined` if the table had no duty for their role.

- `src/engine/ai/utility.ts`  interface Consideration  { name, weight, score }  — One weighted reason for or against an option, scored `0–1`.
- `src/engine/ai/utility.ts`  interface Candidate  { key, option, considerations }  — An option under consideration, with a key stable across ticks so commitment can track it.
- `src/engine/ai/utility.ts`  interface ScoredCandidate  { utility, perceived }
- `src/engine/ai/utility.ts`  interface ScoreOptions  { noise, rng }
- `src/engine/ai/utility.ts`  interface SelectOptions  { threshold }
- `src/engine/ai/utility.ts`  function utility  (considerations) => number  — The weighted utility of one option, `0–1`.
- `src/engine/ai/utility.ts`  function scoreCandidates  (candidates, options?) => ScoredCandidate<T>[]  — Scores every candidate and sorts them best-first.
- `src/engine/ai/utility.ts`  function selectOption  (candidates, options?) => ScoredCandidate<T> | null  — The option to execute, or `null` for "nothing is worth doing" — which the caller should treat as
- `src/engine/ai/utility.ts`  function consider  (name, score, weight?) => Consideration  — Builds a consideration. Weight defaults to 1, which is what most of them want.
- `src/engine/ai/utility.ts`  function normalise  (value, min, max) => number  — Maps a raw quantity onto `0–1` for use as a consideration score. `06` §5's scores are almost all
- `src/engine/ai/utility.ts`  function inverse  (value, min, max) => number  — `normalise()` the other way up — nearer, cheaper, safer all score higher as the number falls.

## src/engine/input/

- `src/engine/input/joystick.ts`  interface ControlLayout  { width, height, leftHanded, radius, deadzone }
- `src/engine/input/joystick.ts`  const DEFAULT_LAYOUT  : Omit<ControlLayout, 'width' | 'height'>
- `src/engine/input/joystick.ts`  type ControlZone  = 'stick' | 'buttons'  — Which half of the screen a pointer belongs to, after handedness is applied.
- `src/engine/input/joystick.ts`  function zoneFor  (layout, screenX) => ControlZone
- `src/engine/input/joystick.ts`  interface StickState  { pointerId, originX, originY, currentX, currentY, x, y }
- `src/engine/input/joystick.ts`  function createStick  () => StickState
- `src/engine/input/joystick.ts`  function stickDown  (stick, layout, pointerId, x, y) => boolean  — A thumb landed. Claims the stick if the touch is in the stick half and nothing else holds it.
- `src/engine/input/joystick.ts`  function stickMove  (stick, layout, pointerId, x, y) => void  — The thumb moved. Beyond full deflection the origin is dragged along, so a thumb that wanders
- `src/engine/input/joystick.ts`  function stickUp  (stick, pointerId) => void  — The thumb lifted. Releases the stick and centres it.
- `src/engine/input/joystick.ts`  interface StickVisual  { visible, originX, originY, thumbX, thumbY }  — Where the on-screen stick should be drawn: origin, thumb position, and deflection.
- `src/engine/input/joystick.ts`  function stickVisual  (stick, layout) => StickVisual
- `src/engine/input/joystick.ts`  interface ButtonSpec  { id, x, y, radius }  — A round on-screen button. Positions are computed, so handedness mirrors them for free.
- `src/engine/input/joystick.ts`  function defaultButtons  (layout) => ButtonSpec[]  — Default right-thumb cluster: two primary buttons and the modifier (`06` §2). Laid out relative
- `src/engine/input/joystick.ts`  function buttonAt  (buttons, x, y) => ButtonSpec | null  — The button under a point, or `null`. Nearest wins where targets overlap.
- `src/engine/input/joystick.ts`  function meetsTouchTargets  (buttons) => boolean  — Verifies every button meets the 44 px minimum touch target from `10` §11 / INV-11.

- `src/engine/input/sources.ts`  type InputDevice  = 'touch' | 'keyboard' | 'gamepad'
- `src/engine/input/sources.ts`  class TouchInput  (layout, buttons) => TouchInput  — Touch: a floating stick in one half and a button cluster in the other.
- `src/engine/input/sources.ts`  const DEFAULT_KEYMAP  : Readonly<Record<string, string>>  — Keyboard mapping (US-2.6). WASD and the arrows both move; the action keys sit under the right
- `src/engine/input/sources.ts`  class KeyboardInput  (keymap?) => KeyboardInput
- `src/engine/input/sources.ts`  interface GamepadSnapshot  { axes, buttons }  — The subset of the Gamepad API this engine reads. Passed in, never fetched here.
- `src/engine/input/sources.ts`  class GamepadInput  (deadzone?) => GamepadInput
- `src/engine/input/sources.ts`  class InputRouter  (touch, keyboard?, gamepad?) => InputRouter  — Combines the three sources into the one frame the simulation reads, and tracks which device is

- `src/engine/input/types.ts`  const Button  — The action buttons. What each one means is state-dependent (`06` §2).
- `src/engine/input/types.ts`  type ButtonMask  = number
- `src/engine/input/types.ts`  interface InputFrame  { moveX, moveY, held, pressed, released }  — One step's worth of intent. Deliberately small and flat: it is recorded for every step of every
- `src/engine/input/types.ts`  const EMPTY_FRAME  : InputFrame
- `src/engine/input/types.ts`  function isHeld  (frame, button) => boolean
- `src/engine/input/types.ts`  function wasPressed  (frame, button) => boolean
- `src/engine/input/types.ts`  function wasReleased  (frame, button) => boolean
- `src/engine/input/types.ts`  function magnitude  (frame) => number  — Stick magnitude, `0–1`. Analog movement (US-2.1) means walking is a real option.
- `src/engine/input/types.ts`  function makeFrame  (moveX, moveY, held, previous?) => InputFrame  — Builds the frame that follows `previous`, deriving the press and release edges from it.
- `src/engine/input/types.ts`  function framesEqual  (a, b) => boolean  — Two frames are equal when the simulation cannot tell them apart.

## src/engine/

- `src/engine/loop.ts`  const STEP_HZ  — 60 Hz. The one number the simulation's behaviour is defined against.
- `src/engine/loop.ts`  const STEP_MS  — The fixed simulation step, in milliseconds.
- `src/engine/loop.ts`  interface LoopOptions  { stepMs, maxStepsPerFrame, maxFrameMs }
- `src/engine/loop.ts`  interface Tick  { steps, alpha, droppedMs }  — What one `advance()` decided. Pure data — the caller does the stepping.
- `src/engine/loop.ts`  class Clock  (options?) => Clock  — The timing policy. Holds the accumulator and nothing else — no entities, no rendering, no
- `src/engine/loop.ts`  interface LoopHandlers  { step, render, onDrop }  — The per-frame callbacks a driver needs.
- `src/engine/loop.ts`  interface FrameScheduler  { request, cancel }  — Injectable frame source, so tests drive frames by hand and never wait on a real one.
- `src/engine/loop.ts`  function rafScheduler  () => FrameScheduler  — The browser's frame source.
- `src/engine/loop.ts`  interface Loop  { clock, start, stop, running }
- `src/engine/loop.ts`  function createLoop  (handlers, options?) => Loop  — Wires a `Clock` to a frame source. Thin by design: everything worth testing is in `Clock`,

## src/engine/match/

- `src/engine/match/events.ts`  type Side  = 0 | 1 | -1  — Which side an event belongs to. `-1` for neutral events like a period ending.
- `src/engine/match/events.ts`  const EventKind  — Event kinds shared by every sport. A sport adds its own with `sportKind`, keeping the envelope
- `src/engine/match/events.ts`  type EventKindName  = (typeof EventKind)[keyof typeof EventKind]
- `src/engine/match/events.ts`  interface SportEvent  { kind, sportKind, step, side, actor, target, value, x, y, detail }  — One thing that happened. Flat and serialisable: it goes into replays, into stats aggregation,
- `src/engine/match/events.ts`  type EventListener  = (event: SportEvent) => void
- `src/engine/match/events.ts`  class EventBus  (historyLimit?) => EventBus  — Fan-out for match events, with a bounded history for replays and post-match summaries.
- `src/engine/match/events.ts`  function event  (kind, step, side?, extras?) => SportEvent  — Builds an event. A helper, so every emitter produces the same shape.

- `src/engine/match/recorder.ts`  interface InputRun  { entity, frame, steps }  — One entity's input, held for `steps` consecutive simulation steps.
- `src/engine/match/recorder.ts`  interface RecordingHeader  { seed, sport, setup, stepMs }
- `src/engine/match/recorder.ts`  interface Recording  { runs, steps }
- `src/engine/match/recorder.ts`  class InputRecorder  (header) => InputRecorder  — Records inputs as a match runs. One instance per match; `record()` is called once per step with
- `src/engine/match/recorder.ts`  class InputPlayer  (recording) => InputPlayer  — Plays a recording back, one step at a time. Returns the frames for a step, reusing one map so
- `src/engine/match/recorder.ts`  const HASH_GRID  — Quantisation grid for state hashing, in world units. Positions are rounded to the nearest
- `src/engine/match/recorder.ts`  class StateHasher  () => StateHasher  — FNV-1a over quantised numbers. Cheap enough to run every step in a determinism test, and stable
- `src/engine/match/recorder.ts`  function recordingSize  (recording) => number  — Estimates the byte cost of a recording, for the storage budget in `05` §9.

- `src/engine/match/state-machine.ts`  const MatchPhase
- `src/engine/match/state-machine.ts`  type MatchPhaseName  = (typeof MatchPhase)[keyof typeof MatchPhase]
- `src/engine/match/state-machine.ts`  interface MatchRules  { periods, periodSteps, overtimeSteps, maxOvertimePeriods, clockRunsInStoppage }
- `src/engine/match/state-machine.ts`  interface MatchResult  { homeScore, awayScore, winner, periodsPlayed, steps }
- `src/engine/match/state-machine.ts`  class MatchStateMachine  (rules, bus?) => MatchStateMachine
- `src/engine/match/state-machine.ts`  interface MatchSnapshot  { phase, period, periodStep, totalSteps, homeScore, awayScore, overtimePeriods, periodExtension }

## src/engine/physics/

- `src/engine/physics/ball.ts`  interface BallPhysics  { gravity, restitution, friction, rollingFriction, drag, magnus, spinDecay, radius }  — Sport-supplied physical constants. Every value is in metres, seconds, or kilograms.
- `src/engine/physics/ball.ts`  const DEFAULT_BALL_PHYSICS  : BallPhysics  — Basketball-ish defaults. A sport module overrides what it needs; these exist so the engine's
- `src/engine/physics/ball.ts`  interface BallState  { entity, carrier, spin, catchCooldown, lastToucher }  — Mutable ball state that does not fit in the `World` arrays.
- `src/engine/physics/ball.ts`  function createBall  (world, x, y, physics?, kind?) => BallState  — Spawns the ball into the world and returns its state.
- `src/engine/physics/ball.ts`  interface BallStepResult  { bounced, settled, impactSpeed }  — What one step of ball physics did, for the sport layer to turn into events.
- `src/engine/physics/ball.ts`  function stepBall  (world, ball, dt, physics?) => BallStepResult  — Advances the ball one fixed step.
- `src/engine/physics/ball.ts`  function attach  (world, ball, carrier) => void  — Gives the ball to an athlete. Cancels flight; the carrier's motion takes over.
- `src/engine/physics/ball.ts`  function release  (world, ball, vx, vy, vz, spin?, cooldownSteps?) => void  — Releases the ball with a velocity and spin — every shot, pass, and clearance goes through here.
- `src/engine/physics/ball.ts`  function launchVelocity  (fromX, fromY, fromZ, toX, toY, toZ, flightTime, gravity, out) => { x: number; y: number; z: number }  — The launch velocity that lands a ball at a target, given a flight time. Used by passing and by
- `src/engine/physics/ball.ts`  function canCatch  (world, ball, athlete, reach, maxHeight) => boolean  — Whether an athlete could take the ball this instant: in range, in reach, and off cooldown.
- `src/engine/physics/ball.ts`  function attemptCatch  (world, ball, athlete, control, rng) => boolean  — A contested catch: succeeds on a draw against `control`, a 0–1 chance the sport computes from
- `src/engine/physics/ball.ts`  function isAtRest  (world, ball, physics?) => boolean  — Whether the ball is on the ground and not moving — a dead ball.

- `src/engine/physics/collision.ts`  const CONTACT_STIFFNESS  — How much of an overlap is corrected per step, `0–1`. Correcting all of it in one step is what
- `src/engine/physics/collision.ts`  const OVERLAP_EPSILON  — Overlaps smaller than this are left alone — a millimetre of shared floor is invisible, and
- `src/engine/physics/collision.ts`  interface ContestWeights  { strength, agility, position }  — Applied to `strength` when a contest is about holding ground, `agility` when it is about beating someone to a…
- `src/engine/physics/collision.ts`  interface Contestant  { id, strength, agility, position }
- `src/engine/physics/collision.ts`  interface ContestResult  { winner, loser, probability, margin }
- `src/engine/physics/collision.ts`  const DEFAULT_CONTEST_WEIGHTS  : ContestWeights  — The default mix: contact is mostly strength, with agility and positioning mattering.
- `src/engine/physics/collision.ts`  function resolveCollisions  (world, scratch, maxRadius?) => number  — Resolves overlaps between every pair of colliding entities, in one pass over the spatial hash.
- `src/engine/physics/collision.ts`  function separatePair  (world, a, b) => boolean  — Pushes one overlapping pair apart. Returns whether they were actually overlapping.
- `src/engine/physics/collision.ts`  function isOverlapping  (world, a, b) => boolean  — Whether two entities' circles overlap right now.
- `src/engine/physics/collision.ts`  function contest  (a, b, rng, weights?) => ContestResult  — The seeded half: who wins a physical contest.
- `src/engine/physics/collision.ts`  function contestOdds  (a, b, weights?) => number  — The win probability `a` would have, without drawing. Used by AI to decide whether to contest.

- `src/engine/physics/movement.ts`  interface Vec2  { x, y }  — A mutable 2D vector. Reused by callers so the hot path allocates nothing.
- `src/engine/physics/movement.ts`  interface MovementProfile  { maxSpeed, acceleration, deceleration, turnRate }  — What an athlete's body can do, in world units (metres, seconds, radians).
- `src/engine/physics/movement.ts`  interface MovementRatings  { speed, acceleration, agility }  — Derived ratings, 1–99, as produced by `05` §3.
- `src/engine/physics/movement.ts`  const MOVEMENT_TUNING  — Rating → physical capability. The constants below are the tuning surface for how the game
- `src/engine/physics/movement.ts`  function movementProfile  (ratings) => MovementProfile
- `src/engine/physics/movement.ts`  function integrate  (world, id, profile, desired, dt) => void  — Advances one entity by one fixed step.
- `src/engine/physics/movement.ts`  function integrateAll  (world, dt, profileOf, desiredOf) => void  — Integrates every entity that is neither frozen nor benched, using one profile per entity.
- `src/engine/physics/movement.ts`  function signedAngleDelta  (from, to) => number  — The shortest signed rotation from `from` to `to`, in `(-π, π]`. Taking the short way round is
- `src/engine/physics/movement.ts`  function normaliseAngle  (angle) => number  — Wraps an angle into `(-π, π]`.
- `src/engine/physics/movement.ts`  function clamp  (value, min, max) => number
- `src/engine/physics/movement.ts`  function limit  (out, max) => Vec2  — Scales `out` to at most `max` in length, in place.

- `src/engine/physics/steering.ts`  function seek  (fromX, fromY, toX, toY, maxSpeed, out) => Vec2  — Full speed straight at a point. The bluntest behaviour, and the right one surprisingly often.
- `src/engine/physics/steering.ts`  function flee  (fromX, fromY, awayFromX, awayFromY, maxSpeed, out) => Vec2  — Directly away from a point — the same maths with the sign flipped.
- `src/engine/physics/steering.ts`  function arrive  (fromX, fromY, toX, toY, maxSpeed, slowRadius, out, stopRadius?) => Vec2  — Seek, but slowing inside `slowRadius` so the athlete settles on the spot instead of oscillating
- `src/engine/physics/steering.ts`  function pursue  (fromX, fromY, targetX, targetY, targetVx, targetVy, maxSpeed, out, maxPrediction?) => Vec2  — Seeks where the target *will be*, not where it is. Prediction time scales with distance and
- `src/engine/physics/steering.ts`  function evade  (fromX, fromY, threatX, threatY, threatVx, threatVy, maxSpeed, out, maxPrediction?) => Vec2  — Pursue's mirror: run to where the threat *won't* be.
- `src/engine/physics/steering.ts`  function separate  (world, id, radius, maxSpeed, scratch, out) => Vec2  — Steers away from nearby entities, weighted by how close they are — the thing that stops five
- `src/engine/physics/steering.ts`  function avoid  (fromX, fromY, vx, vy, obstacleX, obstacleY, obstacleRadius, maxSpeed, out, lookahead?) => boolean  — Steers around a single circular obstacle, but only when the athlete is actually heading into it.
- `src/engine/physics/steering.ts`  function blend  (out, maxSpeed, ...weighted) => Vec2  — Weighted sum of behaviours, clamped to `maxSpeed`. Priority-based arbitration (take the first

## src/engine/render/

- `src/engine/render/camera.ts`  interface CameraOptions  { width, height, worldWidth, worldHeight, maxScale, minScale, followRate, zoomRate, lookahead, reducedMotion }
- `src/engine/render/camera.ts`  interface FollowTarget  { x, y, vx, vy }
- `src/engine/render/camera.ts`  class Camera  (options) => Camera

- `src/engine/render/perf.ts`  interface PerfSnapshot  { fps, frameMs, frameP95Ms, worstFrameMs, simMs, simP95Ms, jankRatio, samples }
- `src/engine/render/perf.ts`  const FRAME_BUDGET_MS  — One frame at 60 fps. Anything longer has dropped a frame.
- `src/engine/render/perf.ts`  class PerfMonitor  () => PerfMonitor
- `src/engine/render/perf.ts`  const PERF_BUDGETS  — The `12` §6 budgets this engine can measure without a browser.
- `src/engine/render/perf.ts`  interface BudgetCheck  { metric, measured, budget, passed }
- `src/engine/render/perf.ts`  function checkBudgets  (snapshot) => BudgetCheck[]  — Checks a snapshot against the budgets. Returns every result, passing or failing, because a

- `src/engine/render/renderer.ts`  interface Canvas2D  { save, restore, scale, translate, rotate, clearRect, fillRect, strokeRect, beginPath, closePath, …+13 }  — The slice of the 2D context this engine uses. Anything outside it is not available to sports.
- `src/engine/render/renderer.ts`  const LAYERS  — Draw order. Explicit and fixed: a sport asks for a layer by name and cannot reorder the stack,
- `src/engine/render/renderer.ts`  type LayerName  = (typeof LAYERS)[number]
- `src/engine/render/renderer.ts`  type DrawCommand  = (ctx: Canvas2D, view: ViewTransform) => void  — Draw commands are functions, so a sport can draw anything without extending an enum.
- `src/engine/render/renderer.ts`  interface ViewTransform  { x, y, scale, width, height }  — World → screen. Owned by the camera (T-1.8); the renderer only applies it.
- `src/engine/render/renderer.ts`  const Detail  — Detail tier for one entity this frame. Sports decide what each tier draws.
- `src/engine/render/renderer.ts`  type DetailLevel  = (typeof Detail)[keyof typeof Detail]
- `src/engine/render/renderer.ts`  interface RendererOptions  { fullDetailRatio, reducedDetailRatio, reducedMotion }
- `src/engine/render/renderer.ts`  interface FrameStats  { commands, styleChanges, full, reduced, minimal, staticRedrawn }
- `src/engine/render/renderer.ts`  interface OffscreenLayer  { canvas, ctx, width, height }  — A canvas the renderer can draw the static field into once and blit thereafter.
- `src/engine/render/renderer.ts`  type OffscreenFactory  = (width: number, height: number) => OffscreenLayer
- `src/engine/render/renderer.ts`  class Renderer  (createOffscreen?, options?) => Renderer
- `src/engine/render/renderer.ts`  function domOffscreenFactory  () => OffscreenFactory  — A DOM-backed off-screen layer. Kept out of `Renderer` so the class stays testable in node.
- `src/engine/render/renderer.ts`  interface DebugInfo  { fps, frameMs, simMs, entities, stats }
- `src/engine/render/renderer.ts`  function drawDebugOverlay  (ctx, info, x?, y?) => void  — The developer overlay from `04` §6 — frame time, sim time, entity count, and what the batching

## src/engine/

- `src/engine/rng.ts`  interface RngState  { a, b, c, d, spare }  — A serialisable snapshot of a generator's position in its stream.
- `src/engine/rng.ts`  interface Rng  { seed, nextU32, next, int, float, bool, pick, shuffle, gaussian, fork, …+3 }  — The interface threaded through the sim. Sport modules, AI, and physics receive this — never a
- `src/engine/rng.ts`  function hashString  (text) => number  — FNV-1a, 32-bit. Turns a seed string into a starting integer.
- `src/engine/rng.ts`  function createRng  (seed) => Rng  — Creates a generator. The same seed always produces the same stream, on any engine.
- `src/engine/rng.ts`  function restoreRng  (seed, state) => Rng  — Creates a generator positioned at a saved state. Used when resuming a match from a checkpoint,
- `src/engine/rng.ts`  function randomSeed  () => string  — A seed string for a new match. This is the one place a *non*-deterministic value legitimately

- `src/engine/world.ts`  type EntityId  = number  — An index into the entity arrays. Stable while the entity is alive; reused after `despawn`.
- `src/engine/world.ts`  const NO_ENTITY  : EntityId  — Not an entity. Returned where an id is expected but none applies.
- `src/engine/world.ts`  interface WorldOptions  { capacity, width, height, cellSize }
- `src/engine/world.ts`  interface SpawnInit  { x, y, z, vx, vy, vz, facing, radius, mass, team, …+2 }
- `src/engine/world.ts`  const Flags  — Bit flags on an entity. Sports may use the high bits; the low four are the engine's.
- `src/engine/world.ts`  class World  (options) => World

## src/modes/arcade/

- `src/modes/arcade/accessibility.ts`  const LEFT_HANDED_KEY  — Preference keys, shared with Settings. Read here, never written.
- `src/modes/arcade/accessibility.ts`  const REDUCED_MOTION_KEY
- `src/modes/arcade/accessibility.ts`  const FEEDBACK_SECONDS  — How long an outcome banner stays up, in seconds.
- `src/modes/arcade/accessibility.ts`  function reducedMotion  (view) => boolean  — Whether motion should be reduced: the app's own setting, or the operating system's, whichever
- `src/modes/arcade/accessibility.ts`  function leftHanded  () => boolean
- `src/modes/arcade/accessibility.ts`  function arcadeLayout  (view) => ArcadeLayout  — The layout an arcade run draws into, before the canvas has been measured.
- `src/modes/arcade/accessibility.ts`  function applyMotionPreference  (doc, view) => void  — Puts the motion preference on the document root, so `tokens.css`'s `[data-motion='reduced']`
- `src/modes/arcade/accessibility.ts`  interface FeedbackState  { outcome, age }
- `src/modes/arcade/accessibility.ts`  function drawOutcomeFeedback  (ctx, layout, state) => void  — The outcome banner. Text always, in a panel that is outlined as well as filled and marked with a

- `src/modes/arcade/calibration.ts`  interface ArcadeWindowShape  { window, speed, drift, reaction, floor, ceiling }  — The endpoints every calibrated number is interpolated between: the value at rating 0 and the value
- `src/modes/arcade/calibration.ts`  const DEFAULT_WINDOW_SHAPE  : ArcadeWindowShape
- `src/modes/arcade/calibration.ts`  function arcadeRating  (ratings, names) => number  — The athlete's ability at one game: the mean of the derived ratings the game reads. A mean rather
- `src/modes/arcade/calibration.ts`  function windowLabel  (rating) => ArcadeWindowLabel  — Plain-language width for the athlete picker (US-16.3: "states plainly whether it is wide").
- `src/modes/arcade/calibration.ts`  function windowHint  (label, familiarity) => string  — The one sentence the athlete picker shows. Names both halves of the reason — the window and the
- `src/modes/arcade/calibration.ts`  interface CalibrateOptions  { rating, familiarity, difficulty, shape }
- `src/modes/arcade/calibration.ts`  function calibrateWindow  (options) => ArcadeCalibration  — The calibration for one rating at one difficulty. Difficulty scales only the two forgiveness
- `src/modes/arcade/calibration.ts`  interface AthleteCalibrationOptions  { athlete, sport, tables, ratings, difficulty, shape }
- `src/modes/arcade/calibration.ts`  function calibrateForAthlete  (options) => ArcadeCalibration  — What every `ArcadeGameDef.calibrate()` calls. Runs the athlete through the *same* derivation the

- `src/modes/arcade/daily.ts`  const DAILY_DIFFICULTY  : Difficulty  — The daily always runs at one level, so two players' scores are comparable (`09` §7).
- `src/modes/arcade/daily.ts`  function dateKey  (now?) => string  — `YYYY-MM-DD` in UTC — the identity of a day, and the only input the challenge has.
- `src/modes/arcade/daily.ts`  function millisUntilNextDay  (now?) => number  — Milliseconds until the next UTC midnight — the "new challenge in 6h 12m" line.
- `src/modes/arcade/daily.ts`  function dailySeed  (day, game) => string  — The seed for a day's run of one game. Everything below forks from it.
- `src/modes/arcade/daily.ts`  interface DailyChallenge  { day, game, seed, athlete, difficulty, modifiers }
- `src/modes/arcade/daily.ts`  function dailyGame  (day, games) => ArcadeGameDef | undefined  — Which game today is. A rotation out of the day's own generator rather than `day % games.length`,
- `src/modes/arcade/daily.ts`  function dailyModifiers  (day) => readonly ArcadeModifier[]  — Today's twists.
- `src/modes/arcade/daily.ts`  function dailyAthlete  (day, game) => Athlete  — The athlete everyone plays today. Rolled `rare` so the day's window is interesting rather than
- `src/modes/arcade/daily.ts`  function dailyChallenge  (day, games) => DailyChallenge | null  — Today's challenge, from the day and the catalogue. Pure: same inputs, same challenge, forever.
- `src/modes/arcade/daily.ts`  function dailyConfig  (challenge) => ArcadeConfig  — The config that plays a challenge.
- `src/modes/arcade/daily.ts`  interface ChallengeScenario  { game, seed, modifiers }  — A shareable run (US-16.4 — "sharable as a challenge code so a friend can attempt the identical
- `src/modes/arcade/daily.ts`  function encodeChallenge  (scenario) => string
- `src/modes/arcade/daily.ts`  function decodeChallenge  (code) => ChallengeScenario | null  — Reads a code back. Returns `null` for anything that is not a valid code — a typo, a code from a
- `src/modes/arcade/daily.ts`  function challengeCode  (challenge) => string  — The code for a challenge.
- `src/modes/arcade/daily.ts`  function scenarioConfig  (scenario, games, athlete) => ArcadeConfig | null  — The config that plays a decoded code, against the catalogue this build has. Returns `null` when

- `src/modes/arcade/draw.ts`  const ARCADE_COLOURS  — The palette. Read from `10` §3's token values rather than invented here, and paired with a shape
- `src/modes/arcade/draw.ts`  function mirrorX  (x, layout) => number  — Mirrors an x coordinate for a left-handed layout (T-4.12). Presentation only.
- `src/modes/arcade/draw.ts`  function bar  (ctx, x, y, width, height, fill) => void  — A rounded-ish bar, drawn with the primitives `Canvas2D` actually exposes.
- `src/modes/arcade/draw.ts`  function label  (ctx, text, x, y, options?) => void  — Centres a line of text.
- `src/modes/arcade/draw.ts`  const METER_THUMB_SIDE  — Where the meter sits across the stage, as a fraction of the width, for a right-handed player.
- `src/modes/arcade/draw.ts`  function drawMeter  (ctx, layout, options) => void  — The vertical release meter four of the games draw: a track, the athlete's band, and the marker.

- `src/modes/arcade/meter.ts`  const BASE_SWEEP_SECONDS  — Seconds for one full sweep, up and back, at speed 1.
- `src/modes/arcade/meter.ts`  const BAND_LIMITS  — The band never shrinks past this or grows past this, in meter units.
- `src/modes/arcade/meter.ts`  interface MeterOptions  { calibration, centre, speedScale, windowScale, rng }
- `src/modes/arcade/meter.ts`  class ReleaseMeter  (options) => ReleaseMeter  — A marker sweeping up and down past a target band. Deterministic: it advances only by the `dt` it
- `src/modes/arcade/meter.ts`  function outcomeChance  (calibration, quality) => number  — Maps a release quality onto the athlete's outcome band. A perfect release from a novice is still
- `src/modes/arcade/meter.ts`  function resolveShot  (calibration, quality, rng) => boolean  — Whether a release goes in, drawn from the run's generator.

- `src/modes/arcade/modes.ts`  interface ArcadeModifier  { id, name, description, window, speed, drift, clock, lives, scoreMultiplier }  — One twist the daily challenge can apply. Every field is optional, and a game that understands
- `src/modes/arcade/modes.ts`  const ARCADE_MODIFIERS  : readonly ArcadeModifier[]  — The vocabulary. Small on purpose: a daily challenge is meant to be legible in one line, and five
- `src/modes/arcade/modes.ts`  const ARCADE_MODIFIERS_BY_ID  : ReadonlyMap<ArcadeModifierId, ArcadeModifier>
- `src/modes/arcade/modes.ts`  function resolveModifiers  (ids) => readonly ArcadeModifier[]  — Resolves ids to modifiers, silently dropping any this build does not know.
- `src/modes/arcade/modes.ts`  function applyModifiers  (calibration, modifiers) => ArcadeCalibration  — The calibrated window with today's twists on top. Order-independent: every effect is a product.
- `src/modes/arcade/modes.ts`  function applyRuleModifiers  (rules, modifiers) => ArcadeRunRules  — The run rules with today's twists on top. A modifier never adds a limit a mode did not have.
- `src/modes/arcade/modes.ts`  function scoreMultiplierFor  (modifiers) => number
- `src/modes/arcade/modes.ts`  function startRun  (game, config) => ArcadeRun  — Starts a run. Every arcade entry point — the hub, the daily card, a party round, and Phase 5's key
- `src/modes/arcade/modes.ts`  const ARCADE_MODE_BLURBS  — What the mode selector says about each choice (`09` §3.3).

- `src/modes/arcade/party.ts`  const PARTY_FORMATS
- `src/modes/arcade/party.ts`  type PartyFormat  = (typeof PARTY_FORMATS)[number]
- `src/modes/arcade/party.ts`  const DEFAULT_ROUNDS
- `src/modes/arcade/party.ts`  interface PartyOptions  { game, players, format, rounds, seed, athlete, difficulty }
- `src/modes/arcade/party.ts`  interface PartyScore  { round, playerId, score, stars }
- `src/modes/arcade/party.ts`  interface PartyState  { options, round, index, active, scores, eliminated, finished }
- `src/modes/arcade/party.ts`  interface PartyTurn  { round, player, seed, remainingThisRound }
- `src/modes/arcade/party.ts`  interface Standing  { player, total, best, rounds, place, eliminated }
- `src/modes/arcade/party.ts`  function roundSeed  (seed, round) => string  — Every player in a round plays this.
- `src/modes/arcade/party.ts`  function startParty  (options) => PartyState
- `src/modes/arcade/party.ts`  function currentTurn  (state) => PartyTurn | null  — Whose turn it is, and on what seed. `null` once the party is over.
- `src/modes/arcade/party.ts`  function turnConfig  (state) => ArcadeConfig | null  — The config for the current turn. Every player gets the identical one but for whose turn it is.
- `src/modes/arcade/party.ts`  function eliminatedAfter  (state, round) => readonly string[]  — Who goes out after a round. Everyone tied at the lowest score goes — except when that would be
- `src/modes/arcade/party.ts`  function recordTurn  (state, result) => PartyState  — Records the current player's run and advances the party.
- `src/modes/arcade/party.ts`  function standings  (state) => readonly Standing[]  — The table at the end. Ranked on total across the rounds played, then on best single round, then
- `src/modes/arcade/party.ts`  function partyWinner  (state) => LocalPlayer | null  — The winner, or `null` while the party is still going or genuinely tied at the top.
- `src/modes/arcade/party.ts`  function passPrompt  (turn, game) => string  — The one line the pass-the-device screen shows.

- `src/modes/arcade/progression.ts`  const ARCADE_LEARNING_RATE  — Arcade's share of a match's learning, per minute played.
- `src/modes/arcade/progression.ts`  const ARCADE_ENTITY  — The entity arcade events are attributed to. Matches the sport modules' `ARCADE_ACTOR`.
- `src/modes/arcade/progression.ts`  interface ArcadeProgressionOptions  { result, athlete, awards, rate }
- `src/modes/arcade/progression.ts`  function arcadeProgression  (options) => ProgressionResult | null  — What a finished run did to the athlete who played it, or `null` when it did nothing — a practice
- `src/modes/arcade/progression.ts`  function progressionSummary  (progress) => string  — The one line the run-over screen shows about learning (US-16.5).

- `src/modes/arcade/records.ts`  const ANY_ATHLETE  — The key an overall (any-athlete) best is stored under. Not a legal athlete id, deliberately.
- `src/modes/arcade/records.ts`  interface ArcadeBest  { kind, id, game, athleteId, score, stars, runs, bestStreak, updatedAt }
- `src/modes/arcade/records.ts`  interface ArcadeDay  { kind, id, day, paidGames, coins, runs, dailyScore }  — What a day of arcade play added up to — the input to the reward caps (T-4.13).
- `src/modes/arcade/records.ts`  type ArcadeRecord  = ArcadeBest | ArcadeDay
- `src/modes/arcade/records.ts`  function bestKey  (game, athleteId) => string
- `src/modes/arcade/records.ts`  function dayKey  (day) => string
- `src/modes/arcade/records.ts`  function emptyDay  (day) => ArcadeDay  — A day with nothing in it yet. Returned rather than `undefined`, so callers never branch on it.
- `src/modes/arcade/records.ts`  interface BestUpdate  { best, improved }
- `src/modes/arcade/records.ts`  function improveBest  (previous, result, athleteId, thresholds, now) => BestUpdate  — The personal best after a run. A run always increments `runs`, and only a higher score replaces
- `src/modes/arcade/records.ts`  class ArcadeRepository  (db) => ArcadeRepository  — Arcade's slice of the database. Practice runs are not recorded at all — `09` §3.3 makes practice

- `src/modes/arcade/registry.ts`  function arcadeCatalogue  (modules) => readonly ArcadeGameDef[]  — Every arcade game a set of sports defines, in sport order and then in the sport's own order.
- `src/modes/arcade/registry.ts`  function catalogueFrom  (registry) => readonly ArcadeGameDef[]  — The catalogue for everything in a registry, sorted by sport id so the hub's order is stable.
- `src/modes/arcade/registry.ts`  function findGame  (games, id) => ArcadeGameDef | undefined  — One game by id, or `undefined`. The hub route resolves its `:id` parameter through this.
- `src/modes/arcade/registry.ts`  function gamesForSport  (games, sport) => readonly ArcadeGameDef[]  — The games belonging to one sport.
- `src/modes/arcade/registry.ts`  function duplicateIds  (games) => readonly ArcadeGameId[]  — Duplicate ids, if any. A duplicate would make personal bests and unlocks collide silently, so the

- `src/modes/arcade/rewards.ts`  const COINS_BY_STARS  : readonly [number, number, number, number]  — Coins for a first run of a game today, by stars earned.
- `src/modes/arcade/rewards.ts`  const FIRST_THREE_STAR_BONUS  — The one-off for the first three-star run of each game each day (`09` §3.3).
- `src/modes/arcade/rewards.ts`  const REPEAT_DECAY  — How sharply a repeat run of the *same game* decays, per run already played today. Sharp on
- `src/modes/arcade/rewards.ts`  const DAILY_COIN_CAP  — Everything arcade can pay in one day, across every game.
- `src/modes/arcade/rewards.ts`  interface RewardBreakdown  { coins, base, firstThreeStar, capped, repeats, day }
- `src/modes/arcade/rewards.ts`  function baseCoins  (stars, repeats, firstThreeStar) => number  — Coins before the daily cap: the star value, decayed by repeats, plus the once-a-day headline.
- `src/modes/arcade/rewards.ts`  function awardRun  (result, day) => RewardBreakdown  — What a finished run pays, and the day it leaves behind. Practice pays nothing and is not even
- `src/modes/arcade/rewards.ts`  function rewardSummary  (reward) => string  — The sentence the run-over screen shows about coins.
- `src/modes/arcade/rewards.ts`  function maxDailyCoins  () => number  — The most coins a day of nothing but arcade can produce. Used by the anti-farm test and worth
- `src/modes/arcade/rewards.ts`  re-export emptyDay

- `src/modes/arcade/scoring.ts`  function starsFor  (score, thresholds) => StarCount  — Stars for a score against ascending thresholds.
- `src/modes/arcade/scoring.ts`  function toNextStar  (score, thresholds) => number | null  — Points still needed for the next star, or `null` once all three are earned.
- `src/modes/arcade/scoring.ts`  function nextStarTarget  (score, thresholds) => number | null  — The score the next star needs, or `null` at three stars. Used by the "target" caption.
- `src/modes/arcade/scoring.ts`  function starProgress  (score, thresholds) => number  — Progress towards the next star, `0–1`. Full at three stars, so a meter reads as complete.
- `src/modes/arcade/scoring.ts`  function accuracy  (made, attempts) => number  — Accuracy as a percentage, `0–100`. Zero attempts reads as zero rather than as a divide-by-zero or
- `src/modes/arcade/scoring.ts`  function qualityLabel  (quality) => string  — The wording for an attempt's quality, `0–1`. Text rather than colour, so the feedback survives
- `src/modes/arcade/scoring.ts`  function starLine  (game) => string  — A game's star line for a tile: "800 · 1,400 · 2,000".

- `src/modes/arcade/session.ts`  const READY_SECONDS  — Seconds of "get ready" before a run starts. Any button press skips it (`09` §3.1).
- `src/modes/arcade/session.ts`  function rulesFor  (game, config) => ArcadeRunRules  — Which of the game's own rules apply, given the mode and any override.
- `src/modes/arcade/session.ts`  function isRewarded  (mode) => boolean  — Whether a run in this mode pays out. Practice never does (`09` §3.3).
- `src/modes/arcade/session.ts`  interface ArcadeRunOverrides  { calibration, scoreMultiplier }  — What the mode layer (T-4.4) may change about a run *after* the athlete has been calibrated.
- `src/modes/arcade/session.ts`  class ArcadeRun  (game, config, overrides?) => ArcadeRun  — One run of one mini-game. Construct, `step()` until `finished`, read `view()` whenever you want

- `src/modes/arcade/types.ts`  type ArcadeGameId  = string
- `src/modes/arcade/types.ts`  const ARCADE_MODES  — `09` §3.3 — the three ways a game is played.
- `src/modes/arcade/types.ts`  type ArcadeMode  = (typeof ARCADE_MODES)[number]
- `src/modes/arcade/types.ts`  function isArcadeMode  (value) => value is ArcadeMode
- `src/modes/arcade/types.ts`  type ArcadeModifierId  = string  — A daily-challenge twist (T-4.4). Games apply the ones they understand and ignore the rest.
- `src/modes/arcade/types.ts`  interface ArcadeCalibration  { rating, windowSeconds, speed, drift, reactionSeconds, floor, ceiling, label, hint }  — How the athlete's ability shows up in the mechanic. Produced by `calibrate()` (T-4.2) and handed
- `src/modes/arcade/types.ts`  const ARCADE_WINDOW_LABELS
- `src/modes/arcade/types.ts`  type ArcadeWindowLabel  = (typeof ARCADE_WINDOW_LABELS)[number]
- `src/modes/arcade/types.ts`  interface ArcadeRunRules  { lives, seconds }  — How a scored run ends. Most games set one or the other; a game may set both, and then whichever
- `src/modes/arcade/types.ts`  const PRACTICE_RULES  : ArcadeRunRules
- `src/modes/arcade/types.ts`  interface ArcadeConfig  { mode, seed, athlete, difficulty, modifiers, rules, mirror }  — Everything a session needs to know about the run it is part of.
- `src/modes/arcade/types.ts`  interface ArcadeAttempt  { made, points, quality, label, costsLife, events }  — One discrete thing the player attempted, reported by the game as it happens.
- `src/modes/arcade/types.ts`  interface ArcadeOutcome  { made, label, quality, points }  — The last attempt, as the HUD shows it.
- `src/modes/arcade/types.ts`  const ARCADE_END_REASONS
- `src/modes/arcade/types.ts`  type ArcadeEndReason  = (typeof ARCADE_END_REASONS)[number]
- `src/modes/arcade/types.ts`  interface ArcadeHost  { rng, calibration, config, elapsed, remaining, lives, attempt, bonus, finish }  — What the framework gives a running session. Deliberately push-shaped: the game tells the host what
- `src/modes/arcade/types.ts`  interface ArcadeSession  { prompt, update, view, draw, end }  — A mounted, running mini-game.
- `src/modes/arcade/types.ts`  interface ArcadeGameView  { meter, target, caption, urgency }  — What every game exposes to the shared HUD. Generic on purpose: a HUD that understood "rack" or
- `src/modes/arcade/types.ts`  interface ArcadeBand  { from, to }
- `src/modes/arcade/types.ts`  interface ArcadeLayout  { width, height, mirror, reducedMotion }  — The canvas rectangle a game draws into, in device-independent pixels.
- `src/modes/arcade/types.ts`  interface ArcadeGameDef  { id, sport, name, blurb, durationSeconds, unlockAchievement, scored, stars, ratings, calibrate, …+1 }  — One mini-game. This is the whole of the arcade seam — adding a game means adding one of these to
- `src/modes/arcade/types.ts`  interface ArcadeResult  { game, sport, mode, seed, athleteId, difficulty, score, stars, attempts, made, …+5 }  — The result of a finished run. Plain data: it is stored, ranked, and shared as a code.
- `src/modes/arcade/types.ts`  type StarCount  = 0 | 1 | 2 | 3
- `src/modes/arcade/types.ts`  const ARCADE_PHASES
- `src/modes/arcade/types.ts`  type ArcadePhase  = (typeof ARCADE_PHASES)[number]
- `src/modes/arcade/types.ts`  interface ArcadeView  { phase, prompt, score, stars, toNextStar, lives, livesMax, remaining, elapsed, attempts, …+7 }  — What the shared arcade HUD draws. Rebuilt each frame; plain data.

- `src/modes/arcade/unlocks.ts`  const ACHIEVEMENTS_LANDED  — Flipped to `true` by T-8.6, when something actually writes to the `achievements` store. Until
- `src/modes/arcade/unlocks.ts`  interface AchievementRecord  { id, unlockedAt }  — One unlocked achievement, as Phase 8 will store it. Declared here so the read has a shape.
- `src/modes/arcade/unlocks.ts`  interface UnlockState  { unlocked, requirement }
- `src/modes/arcade/unlocks.ts`  function unlockStateFor  (game, unlocked) => UnlockState
- `src/modes/arcade/unlocks.ts`  function earnedAchievements  (db) => Promise<ReadonlySet<AchievementId>>  — Every achievement earned on this device. Empty until Phase 8 starts writing them.
- `src/modes/arcade/unlocks.ts`  function unlockStates  (games, unlocked) => ReadonlyMap<string, UnlockState>  — Unlock state for a whole catalogue, keyed by game id — one read for the hub grid.

## src/modes/

- `src/modes/assists.ts`  interface AssistSettings  { aim, pass, autoSwitch, timing }
- `src/modes/assists.ts`  const NO_ASSISTS  : AssistSettings  — Everything off — the no-assist run, and what the bonus is paid for.
- `src/modes/assists.ts`  function defaultAssists  (difficulty) => AssistSettings  — What a level starts you on, from `06` §7's assist and timing-window rows. Legend's row is "off by
- `src/modes/assists.ts`  function assistsOff  (assists) => boolean  — True when nothing is helping — the state the bonus is paid for.
- `src/modes/assists.ts`  const NO_ASSIST_BONUS  — The bonus for turning the help off, as a fraction of the payout (US-7.3 — "a small reward
- `src/modes/assists.ts`  function assistMultiplier  (assists) => number  — The multiplier a payout applies for playing unaided. Phase 8 owns the wallet (T-8.9) — this is
- `src/modes/assists.ts`  function normaliseAssists  (value, fallback) => AssistSettings  — Repairs anything out of range or missing — settings come out of storage written by old builds.

- `src/modes/catalogue.ts`  const PLAY_MODES
- `src/modes/catalogue.ts`  type PlayModeId  = (typeof PLAY_MODES)[number]
- `src/modes/catalogue.ts`  interface PlayMode  { id, name, blurb, hint, sports, route, pending }
- `src/modes/catalogue.ts`  const PLAY_MODE_CATALOGUE  : readonly PlayMode[]
- `src/modes/catalogue.ts`  function playMode  (id) => PlayMode | undefined
- `src/modes/catalogue.ts`  function isModeAvailable  (mode, sport) => boolean  — Whether this sport can be started in this mode right now.
- `src/modes/catalogue.ts`  function modesForSport  (sport) => readonly PlayMode[]  — The modes a sport can be played in, in catalogue order. Empty is a real answer.

- `src/modes/difficulty.ts`  const DIFFICULTIES
- `src/modes/difficulty.ts`  type Difficulty  = (typeof DIFFICULTIES)[number]
- `src/modes/difficulty.ts`  interface DifficultyProfile  { id, label, cpuLatencyMs, decisionNoise, executionError, aggression, assist, timingWindow, rewardMultiplier, tactics, …+1 }
- `src/modes/difficulty.ts`  const DIFFICULTY_PROFILES  : Readonly<Record<Difficulty, DifficultyProfile>>  — `06` §7's table, one row per level. The word-valued rows ("high", "passive", "generous") become
- `src/modes/difficulty.ts`  const DEFAULT_DIFFICULTY  : Difficulty
- `src/modes/difficulty.ts`  function isDifficulty  (value) => value is Difficulty
- `src/modes/difficulty.ts`  function difficultyProfile  (difficulty) => DifficultyProfile  — The profile for a level, falling back to Pro for anything unrecognised out of storage.
- `src/modes/difficulty.ts`  function aiTuning  (difficulty) => AiTuning  — The level as the engine's decision framework wants it (`engine/ai/decider.ts`). The engine takes

- `src/modes/last-played.ts`  interface PlayChoice  { sport, mode }
- `src/modes/last-played.ts`  function lastSport  () => SportId  — The remembered sport, or the default when nothing playable is stored.
- `src/modes/last-played.ts`  function lastMode  (sport) => PlayMode | undefined  — The remembered mode for a sport, or that sport's first available one. `undefined` only when the
- `src/modes/last-played.ts`  function quickPlay  () => PlayChoice | null  — What the big button on the home screen should start, or `null` when nothing has been played yet
- `src/modes/last-played.ts`  function rememberPlay  (sport, mode) => void  — Records a start. Called when a match is actually launched, never when a card is merely focused.
- `src/modes/last-played.ts`  function lastDifficulty  () => Difficulty  — The remembered difficulty (US-7.2 — "selectable per match and rememberable as a default"). One
- `src/modes/last-played.ts`  function rememberDifficulty  (difficulty) => void  — Records a choice. Called when a match is started, or when the picker is changed.
- `src/modes/last-played.ts`  function loadAssists  (difficulty?) => AssistSettings  — The player's assist settings (`06` §2, US-7.3). Nothing stored means "whatever this level starts
- `src/modes/last-played.ts`  function saveAssists  (assists) => void  — Records a change. Called from the settings screen as each dial moves.
- `src/modes/last-played.ts`  function assistsAreCustom  () => boolean  — True when the player has an opinion — used to show "following your difficulty" in settings.
- `src/modes/last-played.ts`  function resetAssists  () => void  — Puts the dials back to whatever the current level starts you on.
- `src/modes/last-played.ts`  function forgetPlay  () => void  — Forgets everything this module stores. For the data screen's reset, and for tests.

## src/modes/live/

- `src/modes/live/audio.ts`  type ToneType  = 'sine' | 'square' | 'sawtooth' | 'triangle'  — The oscillator waveform names every real and fake `OscillatorLike` shares.
- `src/modes/live/audio.ts`  interface AudioParamLike  { setValueAtTime, linearRampToValueAtTime, exponentialRampToValueAtTime }  — The subset of `AudioParam` every tone in this file uses. Real `AudioParam`s satisfy it as-is.
- `src/modes/live/audio.ts`  interface AudioNodeLike  { connect }
- `src/modes/live/audio.ts`  interface OscillatorLike  { type, frequency, start, stop }
- `src/modes/live/audio.ts`  interface GainLike  { gain }
- `src/modes/live/audio.ts`  interface AudioContextLike  { currentTime, destination, createOscillator, createGain }  — The subset of `AudioContext` this layer needs. Real `AudioContext` satisfies it as-is.
- `src/modes/live/audio.ts`  interface AudioSettings  { muted, reducedAudio, volume }
- `src/modes/live/audio.ts`  const DEFAULT_AUDIO_SETTINGS  : AudioSettings
- `src/modes/live/audio.ts`  const AUDIO_CUES  — The sounds a match can make. Small on purpose: a vocabulary a sport picks from is what keeps two
- `src/modes/live/audio.ts`  type AudioCue  = (typeof AUDIO_CUES)[number]
- `src/modes/live/audio.ts`  interface SportAudio  { cue }  — A sport's own answer to "what does this event sound like".
- `src/modes/live/audio.ts`  class MatchAudio  (context, settings?, sport?) => MatchAudio
- `src/modes/live/audio.ts`  function isAudioSupported  (scope?) => boolean  — Whether the platform has a usable WebAudio implementation, without constructing one.

- `src/modes/live/box-score.ts`  interface PlayerLine  { athlete, side, points, fieldGoalsMade, fieldGoalsAttempted, threesMade, threesAttempted, freeThrowsMade, freeThrowsAttempted, rebounds, …+6 }  — One athlete's line. Every counting stat a basketball box score has.
- `src/modes/live/box-score.ts`  interface TeamLine  { side, players }  — A side's totals. Derived, never accumulated separately — two counters always drift apart.
- `src/modes/live/box-score.ts`  interface BoxScore  { lines, teamTurnovers, lastPass }
- `src/modes/live/box-score.ts`  function createBoxScore  () => BoxScore
- `src/modes/live/box-score.ts`  function applyEvent  (box, event) => void  — Folds one event in.
- `src/modes/live/box-score.ts`  function linesFor  (box, side) => PlayerLine[]  — Every line for a side, in entity order.
- `src/modes/live/box-score.ts`  function teamLine  (box, side) => TeamLine  — A side's totals, summed from its lines so the two can never disagree.
- `src/modes/live/box-score.ts`  function shootingLine  (made, attempted) => string  — `made/attempted` with a percentage, or a dash when nothing was attempted.

- `src/modes/live/hud.ts`  interface SafeArea  { top, right, bottom, left }  — Safe-area insets in CSS pixels, as the shell reads them from the environment.
- `src/modes/live/hud.ts`  const NO_INSETS  : SafeArea
- `src/modes/live/hud.ts`  interface HudLayout  { width, height, board, minimap, meter, scale }
- `src/modes/live/hud.ts`  function hudLayout  (width, height, insets?, uiScale?) => HudLayout  — The whole layout, from a viewport and its insets.
- `src/modes/live/hud.ts`  interface HudTheme  { panel, text, dim, accent, warn, teams }  — Colours the HUD draws in. Supplied by the caller so the theme lives in one place.
- `src/modes/live/hud.ts`  const DEFAULT_HUD_THEME  : HudTheme
- `src/modes/live/hud.ts`  function formatClock  (seconds) => string  — `M:SS`, or one decimal inside the last minute — the convention every match clock uses.
- `src/modes/live/hud.ts`  function formatActionClock  (seconds) => string  — The action clock, which is always whole seconds and always rounded *up*.
- `src/modes/live/hud.ts`  function formatElapsedClock  (seconds) => string  — `M:SS` in whole seconds, for a clock that counts *up*.
- `src/modes/live/hud.ts`  interface HudSpec  { showShotClock, clock, foulLabel }  — How the HUD reads a sport it has never heard of. A subset of `SportHudSpec`, so `hud.ts` depends
- `src/modes/live/hud.ts`  const DEFAULT_HUD_SPEC  : HudSpec  — What a sport gets by saying nothing. Basketball's conventions, because they were the HUD's
- `src/modes/live/hud.ts`  function drawHud  (ctx, view, layout, spec?, theme?) => void  — The whole HUD, in one call.
- `src/modes/live/hud.ts`  function clockText  (view, spec) => string  — The game clock as this sport keeps it.
- `src/modes/live/hud.ts`  function foulLabel  (fouls, bonus, label?) => string
- `src/modes/live/hud.ts`  function drawMinimap  (ctx, view, world, fieldWidth, fieldHeight, layout, theme?, sides?) => void  — The minimap: the whole court, both squads, and the ball, at a glance.
- `src/modes/live/hud.ts`  interface EdgeIndicator  { athlete, x, y, angle, distance }  — An off-screen teammate, as an arrow on the viewport edge.
- `src/modes/live/hud.ts`  function offScreenIndicators  (world, view, toScreen, layout, margin?, athletes?) => EdgeIndicator[]  — Where to put arrows for athletes the camera cannot see (`06` §4, US-2.3).
- `src/modes/live/hud.ts`  function drawEdgeIndicators  (ctx, indicators, theme?) => void  — Draws the arrows `offScreenIndicators` located.
- `src/modes/live/hud.ts`  interface BoxRow  { label, points, shooting, rebounds, assists }  — The live box score, as rows of plain strings for whoever is drawing it.
- `src/modes/live/hud.ts`  function boxRows  (view, side) => BoxRow[]  — The live box score as text rows, so the same data serves the in-match panel (T-2.10) and the
- `src/modes/live/hud.ts`  const NO_CONTROLLED  — Exported for the screen: nothing is controlled before the first step.

- `src/modes/live/match.ts`  interface MatchView  { phase, period, periodName, finished, score, status, box, playerSide, steps }  — Everything the HUD, the pause menu, and the summary read. Plain data, rebuilt each frame.
- `src/modes/live/match.ts`  interface MatchOptions  { seed, sport, playerSide, squadSize, rosters, difficulty, assists }
- `src/modes/live/match.ts`  class LiveMatch  (options) => LiveMatch  — One match. Construct, `step()` until `finished`, read `view()` whenever you want to draw.
- `src/modes/live/match.ts`  function simulateMatch  (options) => LiveMatch  — Runs a whole match with no input — the balance harness (T-2.13) and the determinism tests.

- `src/modes/live/screen.ts`  function zoomFloor  (viewWidth, fieldWidth) => number  — The zoom the camera must not go below: enough to keep `VISIBLE_SPAN` metres on screen, or the
- `src/modes/live/screen.ts`  interface LiveScreenOptions  { sport, seed, playerSide, difficulty, assists }
- `src/modes/live/screen.ts`  function readSafeArea  (window, element) => SafeArea  — Reads the safe-area insets the shell publishes as CSS custom properties (`10` §4).
- `src/modes/live/screen.ts`  function liveScreen  (options) => Screen
- `src/modes/live/screen.ts`  function buttonCaptions  (sport, view) => readonly [string, string, string]  — What the three buttons currently do, in `defaultButtons()` order: A, B, then the modifier.
- `src/modes/live/screen.ts`  interface MatchSettings  { leftHanded, sound }  — Settings a player can change without leaving the match (`06` §2, `10` §9).
- `src/modes/live/screen.ts`  interface PausePanelOptions  { onResume, onQuit, settings, onSettingsChange }
- `src/modes/live/screen.ts`  function pausePanel  (doc, match, options) => HTMLElement  — The pause menu (`06` §4). Resume is the primary action and the first focusable thing, because it
- `src/modes/live/screen.ts`  function settingsPanel  (doc, settings, onChange) => HTMLElement  — In-match settings, as real checkboxes with real labels so they are reachable and announced.
- `src/modes/live/screen.ts`  function summaryPanel  (doc, match, onDone) => HTMLElement  — The post-match summary (`06` §4). Coins, XP, and achievements arrive with Phase 8.
- `src/modes/live/screen.ts`  function resultText  (home, away, playerSide) => string  — Says what happened in words, not only in numbers — the score alone is not a result.
- `src/modes/live/screen.ts`  function boxTable  (doc, match) => HTMLElement  — The box score, as a real table so a screen reader can navigate it (`10` §6).

## src/modes/

- `src/modes/local-players.ts`  const PARTY_LIMITS  — `09` §4 — two to four people, passing one device.
- `src/modes/local-players.ts`  const MAX_NAME_LENGTH
- `src/modes/local-players.ts`  interface LocalPlayer  { id, name }
- `src/modes/local-players.ts`  function cleanName  (name, seat) => string  — A name is trimmed and length-capped, never rejected: a blank one becomes a numbered seat, because
- `src/modes/local-players.ts`  function loadPlayers  () => readonly LocalPlayer[]
- `src/modes/local-players.ts`  function savePlayers  (players) => boolean  — Replaces the stored list. Returns false when storage refused (private mode, or full).
- `src/modes/local-players.ts`  function forgetPlayers  () => void
- `src/modes/local-players.ts`  function seatPlayers  (count, remembered?) => readonly LocalPlayer[]  — A party of `count` seats, reusing remembered names for the seats that have them and numbering the
- `src/modes/local-players.ts`  function renamePlayer  (players, id, name) => readonly LocalPlayer[]  — Renames one seat, leaving the rest alone.

## src/modes/playbook/

- `src/modes/playbook/diagram.ts`  interface DiagramPoint  { x, y }  — A point in field fractions: `0–1` along the field, `0–1` across it.
- `src/modes/playbook/diagram.ts`  interface DiagramMarker  { id, side, label, from, to, primary }  — One athlete on the diagram. Markers always exist; they simply may not move.
- `src/modes/playbook/diagram.ts`  function markerLabel  (player) => string  — What goes inside a marker: the jersey number, or two initials when there is none.
- `src/modes/playbook/diagram.ts`  const DIAGRAM_SHAPES
- `src/modes/playbook/diagram.ts`  type DiagramShapeKind  = (typeof DIAGRAM_SHAPES)[number]
- `src/modes/playbook/diagram.ts`  interface DiagramShape  { kind, from, to, at, until, made }  — One drawn beat. `at`/`until` are fractions of the diagram's own duration, so a sport describes
- `src/modes/playbook/diagram.ts`  interface TurnDiagram  { seconds, markers, shapes, basket, caption }
- `src/modes/playbook/diagram.ts`  interface ShapeFrame  { progress }  — A shape being drawn right now, with how much of it has been drawn.
- `src/modes/playbook/diagram.ts`  interface MarkerFrame  { at }
- `src/modes/playbook/diagram.ts`  interface DiagramFrame  { markers, shapes, progress, finished }
- `src/modes/playbook/diagram.ts`  function diagramAt  (diagram, seconds) => DiagramFrame  — The diagram at a moment. `seconds` past the end clamps to the final frame, so a caller that
- `src/modes/playbook/diagram.ts`  function finalFrame  (diagram) => DiagramFrame  — The last frame, for reduced motion and for a diagram the player skipped.
- `src/modes/playbook/diagram.ts`  interface DiagramLayout  { x, y, width, height, mirror }  — Where the diagram is drawn, in device-independent pixels.
- `src/modes/playbook/diagram.ts`  interface DiagramPalette  { home, away, line, made, missed }  — The two team colours plus the ink. Supplied by the screen from the design tokens (`10` §3).
- `src/modes/playbook/diagram.ts`  const DEFAULT_PALETTE  : DiagramPalette
- `src/modes/playbook/diagram.ts`  function drawDiagram  (ctx, frame, layout, palette?) => void  — Draws one frame. Deliberately dumb: it renders exactly what `diagramAt` produced and makes no

- `src/modes/playbook/hot-seat.ts`  const HOT_SEAT_PHASES
- `src/modes/playbook/hot-seat.ts`  type HotSeatPhase  = (typeof HOT_SEAT_PHASES)[number]
- `src/modes/playbook/hot-seat.ts`  interface HotSeatSeat  { side, player }
- `src/modes/playbook/hot-seat.ts`  interface HotSeatOptions  { seats, curtain, firstSide }
- `src/modes/playbook/hot-seat.ts`  interface HotSeatView  { phase, side, name, prompt, concealing, called }
- `src/modes/playbook/hot-seat.ts`  class HotSeat  (options) => HotSeat  — The hand-over state machine for one turn. Construct per turn, or call `nextTurn()` — it holds no
- `src/modes/playbook/hot-seat.ts`  function promptFor  (phase, name) => string  — The one line each phase shows. Names, never seat numbers (`09` §4).
- `src/modes/playbook/hot-seat.ts`  function seatsFor  (players) => readonly [HotSeatSeat, HotSeatSeat] | null  — Builds the two seats from a local-player list, in side order.

- `src/modes/playbook/key-moment.ts`  const KEY_MOMENT_RULES  : ArcadeRunRules  — One attempt and it is over: a key moment is a moment, not a round (`09` §2.4).
- `src/modes/playbook/key-moment.ts`  function findAthlete  (state, id) => PlaybookAthlete | undefined  — The athlete a moment belongs to, or `undefined` when the id is not on the floor.
- `src/modes/playbook/key-moment.ts`  function keyMomentConfig  (state, invocation, seed) => ArcadeConfig | null  — The run's configuration. The athlete and the difficulty are all `calibrate()` will ever see, and
- `src/modes/playbook/key-moment.ts`  function startKeyMoment  (games, state, invocation, seed) => ArcadeRun | null  — Starts the moment. `null` when the sport has no game by that id, which is a bug worth seeing.
- `src/modes/playbook/key-moment.ts`  type KeyMomentResult  = Omit<KeyMomentOutcome, 'invocation' | 'simWouldHave' | 'simPoin…  — What the caller passes to `settleKeyMoment()`.
- `src/modes/playbook/key-moment.ts`  function outcomeOf  (run) => KeyMomentResult  — Reads a finished — or first-attempt — run as an outcome.
- `src/modes/playbook/key-moment.ts`  function attemptLanded  (run) => boolean  — True once the moment has been decided — one attempt is the whole run.
- `src/modes/playbook/key-moment.ts`  function playKeyMoment  (run, input, dt?, maxSteps?) => KeyMomentResult  — Drives a moment to its single attempt with a supplied input, for the headless callers — the

- `src/modes/playbook/match.ts`  const PLAYBOOK_PHASES
- `src/modes/playbook/match.ts`  type PlaybookPhase  = (typeof PLAYBOOK_PHASES)[number]
- `src/modes/playbook/match.ts`  interface PlaybookMatchOptions  { seed, adapter, sport, rules, squads, playerSide, difficulty, keyMoments }
- `src/modes/playbook/match.ts`  class PlaybookMatch  (options) => PlaybookMatch  — One Playbook match. Construct, drive the turn cycle, read `view()` whenever you want to draw.
- `src/modes/playbook/match.ts`  interface PlaybookView  { phase, turn, turnKind, period, periodClock, possession, score, playerSide, finished, lastTurn, …+1 }  — What the turn screen, the hot-seat hand-over, and the balance harness all read.
- `src/modes/playbook/match.ts`  function simulatePlaybookMatch  (options) => PlaybookMatch<S>  — Plays a whole match with both sides on auto-call — the balance harness (T-5.11) and the

- `src/modes/playbook/narration.ts`  function shortName  (athlete, fallback?) => string  — Family name where there is one, so a line reads like commentary rather than like a database.
- `src/modes/playbook/narration.ts`  function pickLine  (options, turn, key) => string  — Stable pick: the same turn always reads the same way.
- `src/modes/playbook/narration.ts`  function fill  (template, values) => string  — Fills `{a}`, `{d}`, and any extra placeholders a sport defines, all occurrences of each.

- `src/modes/playbook/pace.ts`  const TURN_SPEEDS
- `src/modes/playbook/pace.ts`  type TurnSpeed  = (typeof TURN_SPEEDS)[number]
- `src/modes/playbook/pace.ts`  const SPEED_MULTIPLIERS  : Readonly<Record<TurnSpeed, number>>  — `09` §2.1 puts a turn at 4–8 seconds of resolution, so `normal` is that and the others bracket
- `src/modes/playbook/pace.ts`  const FAST_FORWARD  — What holding the screen is worth, on top of the chosen speed.
- `src/modes/playbook/pace.ts`  function isTurnSpeed  (value) => value is TurnSpeed
- `src/modes/playbook/pace.ts`  function paceFor  (speed, holding, reducedMotion) => number  — The multiplier in force this frame. Reduced motion outranks everything, because the setting is a
- `src/modes/playbook/pace.ts`  const AUTO_CALL_MODES  — Whether play-calling is being handled for you (`09` §2.1).
- `src/modes/playbook/pace.ts`  type AutoCallMode  = (typeof AUTO_CALL_MODES)[number]
- `src/modes/playbook/pace.ts`  interface PacePrefs  { speed, autoCall }
- `src/modes/playbook/pace.ts`  const DEFAULT_PACE  : PacePrefs
- `src/modes/playbook/pace.ts`  function coachTakesTurn  (prefs, hasKeyMoment) => boolean  — Whether the coach takes this turn. Auto-call covers the ordinary possessions and hands back the
- `src/modes/playbook/pace.ts`  class TurnPlayback  (diagram) => TurnPlayback  — Plays one turn's diagram at a chosen pace. Deliberately not a component: the screen owns the

- `src/modes/playbook/report.ts`  interface CallLine  { call, turns, points, perTurn, expectedPerTurn }  — How one call did, over the match.
- `src/modes/playbook/report.ts`  interface KeyMomentLine  { played, made, simWouldHaveMade, pointSwing, quality }  — `09` §2.4's comparison, counted.
- `src/modes/playbook/report.ts`  interface SideReport  { side, points, turns, expectedPoints, calls, keyMoments }
- `src/modes/playbook/report.ts`  interface PlaybookReport  { turns, sides, winner, swingTurn }
- `src/modes/playbook/report.ts`  function keyMomentsFor  (turns, side) => KeyMomentLine  — The moments this side played. A steal is played by the *defending* side, so a moment is
- `src/modes/playbook/report.ts`  function buildReport  (state, turns) => PlaybookReport
- `src/modes/playbook/report.ts`  function describeKeyMoments  (line) => string  — `09` §2.4's line, in words. "Both honest and funny" is the brief, and the funny half only works
- `src/modes/playbook/report.ts`  function describeCalls  (report) => string  — How a side's calls did against what the model expected of them.
- `src/modes/playbook/report.ts`  function describeLuck  (report) => string  — Luck, stated as a number rather than as a feeling.

- `src/modes/playbook/types.ts`  const TURN_KINDS  — `09` §2.2 vs §2.3: basketball turns are possessions, soccer turns are phases of play.
- `src/modes/playbook/types.ts`  type TurnKind  = (typeof TURN_KINDS)[number]
- `src/modes/playbook/types.ts`  type CallId  = string
- `src/modes/playbook/types.ts`  const CALL_SIDES  — Offence and defence pick from separate catalogues (`09` §2.2's two tables).
- `src/modes/playbook/types.ts`  type CallSide  = (typeof CALL_SIDES)[number]
- `src/modes/playbook/types.ts`  interface CallOption  { id, name, side, blurb, keys, targeted, persists, dimension }  — One thing a side can call this turn. Plain data so the call sheet, the CPU, and the assistant
- `src/modes/playbook/types.ts`  interface PlaybookCall  { side, call, target, intents }  — A side's decision for one turn.
- `src/modes/playbook/types.ts`  interface CallPair  { offence, defence }  — Both decisions, in the order resolution reads them.
- `src/modes/playbook/types.ts`  interface PlaybookAthlete  { id, athlete, ratings, role, stamina }  — One athlete as Playbook sees them: ratings, not attributes, and a body that gets tired.
- `src/modes/playbook/types.ts`  interface PlaybookSquad  { side, players }
- `src/modes/playbook/types.ts`  interface PlaybookState  { sport, turnKind, difficulty, playerSide, turn, period, clock, possession, score, squads, …+1 }  — What the engine tracks between turns, and hands to the adapter on every call. The sport's own
- `src/modes/playbook/types.ts`  interface TurnExpectation  { successChance, expectedPoints, because }  — What the resolution *expected* before the roll — the honest baseline `09` §2.4 asks for, and what
- `src/modes/playbook/types.ts`  interface TurnResolution  { turn, calls, attacking, outcome, actor, target, points, scores, seconds, retainsPossession, …+3 }  — One resolved turn. Everything downstream — narration, the diagram, the box score, the arcade
- `src/modes/playbook/types.ts`  interface TurnScore  { points, actor }  — One scoring play inside a turn. The engine emits each as its own `score` event.
- `src/modes/playbook/types.ts`  const KEY_MOMENT_FREQUENCIES  — How often the sim hands a moment over (`09` §2.4).
- `src/modes/playbook/types.ts`  type KeyMomentFrequency  = (typeof KEY_MOMENT_FREQUENCIES)[number]
- `src/modes/playbook/types.ts`  interface ArcadeInvocation  { game, actor, leverage, prompt }  — A moment the sim wants the player to play themselves. The adapter proposes; the engine decides
- `src/modes/playbook/types.ts`  interface KeyMomentOutcome  { invocation, made, quality, simWouldHave, simPoints }  — What came back from the mini-game, in the terms resolution can use.
- `src/modes/playbook/types.ts`  interface NarrationLine  { text, tone }
- `src/modes/playbook/types.ts`  const NARRATION_TONES
- `src/modes/playbook/types.ts`  type NarrationTone  = (typeof NARRATION_TONES)[number]
- `src/modes/playbook/types.ts`  interface PlaybookSetup  { seed, difficulty, playerSide, squads }  — Everything the adapter needs to build its own state at kick-off.
- `src/modes/playbook/types.ts`  interface PlaybookClock  { periodSeconds, overtimeSeconds, secondsPerStep }  — The sport's clock, in the units its HUD shows. Playbook spends the same simulation steps Live
- `src/modes/playbook/types.ts`  interface PlaybookAdapter  { turnKind, clock, squads, createState, calls, resolve, keyMoment, narrate, diagram, applyKeyMoment, …+4 }

## src/pwa/

- `src/pwa/boot.ts`  interface PwaRuntime  { detector, controller, integrity, install, persistence, activity, runningBuild, runningVersion, readiness, refreshReadiness }
- `src/pwa/boot.ts`  interface BootOptions  { bannerHost, prefs, onStuck }
- `src/pwa/boot.ts`  function pwaRuntime  () => PwaRuntime | null
- `src/pwa/boot.ts`  function bootPwa  (options) => Promise<PwaRuntime>

- `src/pwa/install.ts`  type InstallAvailability  = /** Already running as an installed app — offer nothing. */ | '…
- `src/pwa/install.ts`  interface BeforeInstallPromptEvent  { prompt, userChoice }
- `src/pwa/install.ts`  interface PlatformHints  { userAgent, standalone }
- `src/pwa/install.ts`  function isIosSafari  (userAgent) => boolean  — iOS Safari, including iPadOS reporting itself as a Mac with touch.
- `src/pwa/install.ts`  function readStandalone  (win?) => boolean
- `src/pwa/install.ts`  function availability  (hints, hasPrompt) => InstallAvailability
- `src/pwa/install.ts`  const IOS_STEPS  : readonly string[]  — The iOS steps, in the order they appear on screen. Shown only where they apply (`10` §10).
- `src/pwa/install.ts`  type InstallListener  = (state: InstallAvailability) => void
- `src/pwa/install.ts`  interface InstallOptions  { window }
- `src/pwa/install.ts`  class InstallController  (options?) => InstallController  — Captures the deferred prompt and exposes it behind our own button. The event fires once, so

- `src/pwa/integrity.ts`  const CHECK_INTERVAL_MS  — `11` §5.2 — on launch, and once per 24 h.
- `src/pwa/integrity.ts`  type Readiness  = /** Everything the app needs is cached. "Ready to play offline.…
- `src/pwa/integrity.ts`  function isCritical  (url, base) => boolean  — An entry whose absence means the app cannot start. The document itself and the entry JavaScript
- `src/pwa/integrity.ts`  function classify  (missing, base, online, precacheSize) => Readiness
- `src/pwa/integrity.ts`  function describeReadiness  (readiness) => string  — Plain-language copy for each state (`10` §9 — short, warm, never blames the player).
- `src/pwa/integrity.ts`  function requestPrecacheManifest  (container, timeoutMs?) => Promise<readonly string[]>  — Asks the active worker for its precache manifest; only the worker has it.
- `src/pwa/integrity.ts`  interface IntegrityOptions  { prefs, base, container, isOnline, now, manifest, missing }
- `src/pwa/integrity.ts`  class IntegrityChecker  (options) => IntegrityChecker

- `src/pwa/register.ts`  type RegistrationOutcome  = | { readonly status: 'registered'; readonly registration: Servi…
- `src/pwa/register.ts`  const SW_URL
- `src/pwa/register.ts`  const SW_SCOPE
- `src/pwa/register.ts`  interface RegisterOptions  { container }
- `src/pwa/register.ts`  function registerServiceWorker  (options?) => Promise<RegistrationOutcome>  — Registers, and immediately asks the browser to re-check the script — trigger 1 of the five in
- `src/pwa/register.ts`  function unregisterOurWorkers  (options?) => Promise<number>  — Unregisters every worker in our scope. Used by Repair (`11` §6) — and by nothing else, because

- `src/pwa/repair.ts`  interface RepairReport  { workersUnregistered, cachesDeleted }
- `src/pwa/repair.ts`  interface RepairOptions  { container, reload, baseUrl }
- `src/pwa/repair.ts`  const REPAIR_PROMISE  — What Repair will and will not touch, in the words the UI must use verbatim (`11` §6).
- `src/pwa/repair.ts`  function repair  (options?) => Promise<RepairReport>  — `11` §6, steps 2–6. Step 1 — offering a backup download — belongs to the caller, because it is

- `src/pwa/safe-point.ts`  interface AppActivity  { path, inMatch, unsavedEditor, midCeremony, lastInteractionAt }
- `src/pwa/safe-point.ts`  const IDLE_REQUIRED_MS  — `11` §4 — idle for at least this long before a silent reload is acceptable.
- `src/pwa/safe-point.ts`  function isSafePoint  (activity, now) => boolean
- `src/pwa/safe-point.ts`  class ActivityTracker  () => ActivityTracker  — Tracks activity so the controller can ask, rather than every screen having to report.

- `src/pwa/strategies.ts`  type Strategy  = /** Never cached. The source of truth about what is deployed (`…
- `src/pwa/strategies.ts`  const NAVIGATION_TIMEOUT_MS  — `11` §2 — network-first navigation gives up on the network after this and serves the shell.
- `src/pwa/strategies.ts`  const VERSION_FILE
- `src/pwa/strategies.ts`  const MANIFEST_FILE
- `src/pwa/strategies.ts`  interface RouteInput  { url, base, origin, isNavigation, method }
- `src/pwa/strategies.ts`  function routeFor  (input) => Strategy
- `src/pwa/strategies.ts`  function cacheKindFor  (strategy) => 'precache' | 'shell' | 'runtime' | null  — Which cache a strategy writes into. `network-only` writes nowhere.
- `src/pwa/strategies.ts`  function fetchWithTimeout  (request, timeoutMs, fetcher) => Promise<Response | null>  — Races a fetch against a timeout. Resolves `null` on timeout or network failure — both mean
- `src/pwa/strategies.ts`  function isCacheable  (response) => boolean  — A response worth caching: a real 200 from our own origin, not an opaque or partial one.

- `src/pwa/update-controller.ts`  const SNOOZE_MS  — `11` §4 — "Later" is remembered for 24 hours, then the banner returns.
- `src/pwa/update-controller.ts`  type BannerDecision  = | { readonly show: false } | { readonly show: true; readonly di…
- `src/pwa/update-controller.ts`  interface DecideInput  { status, activity, autoUpdate, snoozedUntil, now }
- `src/pwa/update-controller.ts`  type UpdateAction  = 'apply-silently' | 'show-banner' | 'offer-repair' | 'nothing'
- `src/pwa/update-controller.ts`  function decide  (input) => UpdateAction  — The whole of `11` §4 as one pure function, so the policy is testable without a service worker.
- `src/pwa/update-controller.ts`  function bannerFor  (status, action) => BannerDecision
- `src/pwa/update-controller.ts`  interface ControllerOptions  { detector, registration, prefs, getActivity, onBanner, onStuck, container, reload, now }
- `src/pwa/update-controller.ts`  class UpdateController  (options) => UpdateController

- `src/pwa/update-detector.ts`  const FOREGROUND_MIN_INTERVAL_MS  — `11` §3, trigger 2 — re-check on foreground, but not more than once a minute.
- `src/pwa/update-detector.ts`  const POLL_INTERVAL_MS  — `11` §3, trigger 3 — while the app is open and online.
- `src/pwa/update-detector.ts`  type UpdateState  = /** Nothing found, or we have not been able to check. */ | { re…
- `src/pwa/update-detector.ts`  interface DetectorStatus  { state, lastCheckedAt, deployed, checking }
- `src/pwa/update-detector.ts`  type StatusListener  = (status: DetectorStatus) => void
- `src/pwa/update-detector.ts`  interface DetectorOptions  { registration, runningVersion, runningBuild, window, document, fetchVersionImpl, now }
- `src/pwa/update-detector.ts`  class UpdateDetector  (options) => UpdateDetector

- `src/pwa/version.ts`  interface VersionInfo  { buildHash, version, builtAt, minSupportedVersion }
- `src/pwa/version.ts`  const RUNNING_BUILD  : string  — The build this running code came from. Injected by Vite at build.
- `src/pwa/version.ts`  const VERSION_URL
- `src/pwa/version.ts`  type VersionFetchResult  = | { readonly status: 'ok'; readonly info: VersionInfo } /** Off…
- `src/pwa/version.ts`  interface FetchVersionOptions  { fetcher, timeoutMs, url }
- `src/pwa/version.ts`  function fetchVersion  (options?) => Promise<VersionFetchResult>  — Fetches `version.json` with `cache: 'no-store'`. Never throws — being offline is the expected
- `src/pwa/version.ts`  function compareVersions  (a, b) => number  — Compares dotted numeric versions. Returns <0, 0, or >0. Non-numeric parts sort as 0.
- `src/pwa/version.ts`  interface VersionComparison  { differs, forced }
- `src/pwa/version.ts`  function compareToDeployed  (deployed, runningBuild, runningVersion) => VersionComparison
- `src/pwa/version.ts`  function describeAge  (builtAt, now?) => string  — "3 days ago" — used in Settings so "am I on the new one?" is always answerable (`11` §4).

## src/sports/basketball/arcade/

- `src/sports/basketball/arcade/buzzer-beater.ts`  const buzzerBeaterGame  : ArcadeGameDef

- `src/sports/basketball/arcade/fast-break.ts`  const fastBreakGame  : ArcadeGameDef

- `src/sports/basketball/arcade/free-throw.ts`  const freeThrowGame  : ArcadeGameDef

- `src/sports/basketball/arcade/index.ts`  const BASKETBALL_ARCADE  : readonly ArcadeGameDef[]
- `src/sports/basketball/arcade/index.ts`  re-export buzzerBeaterGame
- `src/sports/basketball/arcade/index.ts`  re-export fastBreakGame
- `src/sports/basketball/arcade/index.ts`  re-export freeThrowGame
- `src/sports/basketball/arcade/index.ts`  re-export pickpocketGame
- `src/sports/basketball/arcade/index.ts`  re-export threePointGame

- `src/sports/basketball/arcade/pickpocket.ts`  const TELL_SECONDS  — How long the handler telegraphs before the ball leaves. The same for every athlete, on purpose.
- `src/sports/basketball/arcade/pickpocket.ts`  const pickpocketGame  : ArcadeGameDef

- `src/sports/basketball/arcade/shared.ts`  const BASKETBALL_ARCADE_SPORT
- `src/sports/basketball/arcade/shared.ts`  const ARCADE_ACTOR  — The entity every arcade event is attributed to. An arcade run has one athlete and no world, but
- `src/sports/basketball/arcade/shared.ts`  function basketballCalibration  (athlete, difficulty, ratings, shape?) => ArcadeCalibration  — The calibration for one game, given the ratings it reads. Every basketball game goes through here,
- `src/sports/basketball/arcade/shared.ts`  function shotEvents  (options) => readonly SportEvent[]  — A made shot, in the shape Live emits it (INV-9). `step` is stamped by the framework; `zone` is
- `src/sports/basketball/arcade/shared.ts`  function stealEvent  () => SportEvent  — A steal, and the foul that a mistimed one becomes.
- `src/sports/basketball/arcade/shared.ts`  function foulEvent  () => SportEvent
- `src/sports/basketball/arcade/shared.ts`  re-export ARCADE_COLOURS  from '../../../modes/arcade/draw.ts'
- `src/sports/basketball/arcade/shared.ts`  re-export METER_THUMB_SIDE  from '../../../modes/arcade/draw.ts'
- `src/sports/basketball/arcade/shared.ts`  re-export bar  from '../../../modes/arcade/draw.ts'
- `src/sports/basketball/arcade/shared.ts`  re-export drawMeter  from '../../../modes/arcade/draw.ts'
- `src/sports/basketball/arcade/shared.ts`  re-export label  from '../../../modes/arcade/draw.ts'
- `src/sports/basketball/arcade/shared.ts`  re-export mirrorX  from '../../../modes/arcade/draw.ts'

- `src/sports/basketball/arcade/three-point.ts`  const RACKS
- `src/sports/basketball/arcade/three-point.ts`  const BALLS_PER_RACK
- `src/sports/basketball/arcade/three-point.ts`  const threePointGame  : ArcadeGameDef

## src/sports/basketball/

- `src/sports/basketball/art.ts`  type Theme  = 'dark' | 'light'
- `src/sports/basketball/art.ts`  interface CourtPalette  { floor, paint, line, rim }
- `src/sports/basketball/art.ts`  interface TeamPalette  { fill, onFill }  — A team's fill, and the ink its kit marking and facing tick draw in on top of that fill.
- `src/sports/basketball/art.ts`  interface BasketballPalette  { court, teams, ball, ballSeam }
- `src/sports/basketball/art.ts`  function paletteFor  (theme?) => BasketballPalette  — Picks the palette for a theme. Defaults dark, per `10` §3.1's "dark-first".
- `src/sports/basketball/art.ts`  const ATHLETE_RADIUS  — World-space radii for callers that don't have the entity's own (e.g. a test fixture).
- `src/sports/basketball/art.ts`  const BALL_RADIUS
- `src/sports/basketball/art.ts`  interface AthleteDrawOptions  { team, controlled, radius }
- `src/sports/basketball/art.ts`  function drawAthlete  (ctx, x, y, facing, teamColour, detail, options) => void  — One athlete. `detail` trims what's drawn, decoration first: `FULL` adds a shadow and a facing
- `src/sports/basketball/art.ts`  function drawControlledMarker  (ctx, x, y, radius, detail) => void  — The controlled-athlete marker: a two-tone ring around the athlete, not a tint on it. A dark halo
- `src/sports/basketball/art.ts`  interface BallDrawOptions  { radius }
- `src/sports/basketball/art.ts`  function drawBall  (ctx, x, y, z, palette, detail, options?) => void  — The ball. `z` (height off the floor) has no direct picture on a top-down view, so height is

- `src/sports/basketball/control.ts`  interface Candidate  { athlete, toBall, toOwnBasket, carrier }  — One athlete the player could be, with everything the policy needs to judge them.
- `src/sports/basketball/control.ts`  const CONTROL
- `src/sports/basketball/control.ts`  function pickControlled  (candidates, current, autoSwitch) => EntityId  — Who the player should be, given who they are now.
- `src/sports/basketball/control.ts`  function cycleControlled  (order, current) => EntityId  — Manual cycle: the next athlete in a stable order, wrapping.
- `src/sports/basketball/control.ts`  function shouldAutoSwitch  (previousPossession, possession, autoSwitch) => boolean  — Whether a possession change should force a switch. Separate from `pickControlled` because the

- `src/sports/basketball/court-render.ts`  function drawCourt  (ctx, field, theme?) => void  — `theme` defaults dark (`10` §3.1's dark-first default) so the current call site —
- `src/sports/basketball/court-render.ts`  function courtKey  (field, view) => string  — Cache key for the static field layer — the court is fixed, so only the viewport can change it.

- `src/sports/basketball/court.ts`  type Side  = 0 | 1  — Which side of the court a team defends.
- `src/sports/basketball/court.ts`  const COURT  — The court, in metres. Every literal in the basketball module traces back to one of these.
- `src/sports/basketball/court.ts`  const CENTRE_Y  — Half the court's width — the centre line's y, and the axis every basket sits on.
- `src/sports/basketball/court.ts`  const CENTRE_X  — Half-court line.
- `src/sports/basketball/court.ts`  const basketballCourt  : FieldGeometry
- `src/sports/basketball/court.ts`  function defendedBasket  (side) => Goal  — The basket `side` defends.
- `src/sports/basketball/court.ts`  function attackedBasket  (side) => Goal  — The basket `side` attacks — the one it scores in.
- `src/sports/basketball/court.ts`  function attackDirection  (side) => 1 | -1  — Sign of the direction `side` attacks in: `+1` towards high x, `-1` towards low x.
- `src/sports/basketball/court.ts`  function shotDistance  (x, y, side) => number  — Floor distance from a point to the basket `side` attacks — what a shot is measured by.
- `src/sports/basketball/court.ts`  function isThreePointShot  (x, y, side) => boolean  — Whether a shot from here is worth three.
- `src/sports/basketball/court.ts`  function shotValue  (x, y, side) => 2 | 3  — Points a made shot from here is worth.
- `src/sports/basketball/court.ts`  const CORNER_ARC_X  — Where the arc crosses a corner line, measured along the court from the baseline.
- `src/sports/basketball/court.ts`  function isInAttackingPaint  (x, y, side) => boolean  — Inside the key the given side attacks. Includes the free-throw line itself.
- `src/sports/basketball/court.ts`  function isInRestrictedArea  (x, y, side) => boolean  — Inside the no-charge semicircle under the basket the given side attacks.
- `src/sports/basketball/court.ts`  function isInFrontcourt  (x, y, side) => boolean  — Whether a point is in the attacking half for `side`. Backcourt violations (T-2.2) are decided by
- `src/sports/basketball/court.ts`  type ShotZone  = 'restricted' | 'paint' | 'midRange' | 'cornerThree' | 'wingThre…  — Shot zones, for the box score and for CPU shot selection.
- `src/sports/basketball/court.ts`  function shotZone  (x, y, side) => ShotZone  — Classifies a shot location. Wing and top are split by the angle off the axis through the basket,
- `src/sports/basketball/court.ts`  function isInBounds  (x, y, margin?) => boolean  — Inside the lines. `margin` shrinks the legal area, for an entity with a radius.
- `src/sports/basketball/court.ts`  type Boundary  = 'baseline0' | 'baseline1' | 'sidelineLow' | 'sidelineHigh'  — Which boundary a point is past, or `null` if it is on the court.
- `src/sports/basketball/court.ts`  function crossedBoundary  (x, y) => Boundary | null  — The boundary a point most escaped through. Deepest overshoot wins, so a ball that clears the
- `src/sports/basketball/court.ts`  interface Spot  { x, y }
- `src/sports/basketball/court.ts`  function throwInSpot  (x, y) => Spot  — Where play restarts after the ball goes out. The nearest point on the boundary, pulled away from
- `src/sports/basketball/court.ts`  function tipOffSpot  () => Spot  — The centre circle, where the opening tip happens.
- `src/sports/basketball/court.ts`  function freeThrowSpot  (side) => Spot  — The free-throw line for a shooter attacking `side`'s target basket.
- `src/sports/basketball/court.ts`  function mirrorX  (x) => number  — Mirrors a point to the other end of the court. Role placements and set plays are authored once

- `src/sports/basketball/cpu.ts`  interface Spot  { x, y }  — A place to stand.
- `src/sports/basketball/cpu.ts`  const CPU
- `src/sports/basketball/cpu.ts`  function spotFor  (spot, side) => Spot  — Absolute court position for a fractional spot, for a side measured from the end it defends.
- `src/sports/basketball/cpu.ts`  function offensiveSpot  (roleIndex, side) => Spot  — The offensive spot for a role.
- `src/sports/basketball/cpu.ts`  function zoneSpot  (roleIndex, side, ball) => Spot  — The zone spot for a role, shaded towards the ball. A zone that ignores where the ball is is not a
- `src/sports/basketball/cpu.ts`  function shotBar  (shotClockSeconds, possessionValue?) => number  — The bar a shot has to clear, in expected points. Falls to nothing as the clock runs out, because
- `src/sports/basketball/cpu.ts`  function expectedPoints  (probability, value) => number  — Expected points from a shot: what it is worth times how often it goes in.
- `src/sports/basketball/cpu.ts`  function shouldShoot  (probability, value, shotClockSeconds, possessionValue?) => boolean  — Whether to shoot.
- `src/sports/basketball/cpu.ts`  const Decision  — What the ball-handler has decided to do.
- `src/sports/basketball/cpu.ts`  type DecisionName  = (typeof Decision)[keyof typeof Decision]
- `src/sports/basketball/cpu.ts`  interface Look  { expected, contest }
- `src/sports/basketball/cpu.ts`  function decide  (own, best, laneContest, shotClockSeconds, possessionValue?) => DecisionName  — The possession decision, in priority order: take the shot that clears the bar, move the ball to a
- `src/sports/basketball/cpu.ts`  function shouldCut  (distanceToBasket, ballIsHeld, rng) => boolean  — Whether an off-ball athlete should start a cut to the rim from where they are.
- `src/sports/basketball/cpu.ts`  function screenSpot  (handler, defender) => Spot  — Where a screener stands: just in front of the handler's defender, on the side the handler is
- `src/sports/basketball/cpu.ts`  function shouldHelp  (distanceToBall, ballDistanceToBasket, markIsCarrier) => boolean  — Whether a defender should leave their mark to help on a drive: the ball is getting to the rim,

- `src/sports/basketball/defence.ts`  interface DefenderRatings  { perimeterD, interiorD, vertical, strength, agility, discipline }  — What a defender brings. `05` §3.1 splits perimeter and interior defence, and so does this.
- `src/sports/basketball/defence.ts`  interface AttackerRatings  { ballHandling, strength, agility }  — What the athlete being defended brings to resisting it.
- `src/sports/basketball/defence.ts`  interface FreeThrowRatings  { freeThrow, composure }  — What a free-throw shooter brings.
- `src/sports/basketball/defence.ts`  const DEFENCE
- `src/sports/basketball/defence.ts`  function assignMarks  (attackers, defenders) => Map<EntityId, EntityId>  — Man assignments: like marks like, by role. Deterministic and cheap, and the thing a scheme
- `src/sports/basketball/defence.ts`  function markingSpot  (mark, basket, ratings, inPaint) => { x: number; y: number }  — Where a defender should stand: on the line between their mark and the basket their mark is
- `src/sports/basketball/defence.ts`  function boxOutSpot  (attacker, basket) => { x: number; y: number }  — Where to stand to seal an attacker off the glass: between them and the basket, right on them.
- `src/sports/basketball/defence.ts`  function stealChance  (defender, carrier, distance) => number  — Chance of coming away with the ball on a steal attempt.
- `src/sports/basketball/defence.ts`  function blockChance  (defender, distance, releaseQuality) => number  — Chance of getting a hand to a shot. Interior defence and vertical, against a distance that goes
- `src/sports/basketball/defence.ts`  function foulChance  (ratings, approach, speedDifferential) => number  — Whether a challenge is a foul.
- `src/sports/basketball/defence.ts`  function approachAngle  (defenderVelocity, attackerVelocity) => number  — How across-the-body a challenge was, `0–1`. A defender moving the same way as the attacker is
- `src/sports/basketball/defence.ts`  function freeThrowProbability  (ratings, release, pressure?) => number  — Make chance for a free throw. No defender, no distance — just the shooter and their nerve.
- `src/sports/basketball/defence.ts`  const StealResult  — A steal attempt's outcome.
- `src/sports/basketball/defence.ts`  type StealResultName  = (typeof StealResult)[keyof typeof StealResult]
- `src/sports/basketball/defence.ts`  function resolveSteal  (defender, carrier, distance, approach, speedDifferential, rng) => StealResultName  — Resolves a steal. Order matters: the ball first, the whistle second — a defender who gets the
- `src/sports/basketball/defence.ts`  const BlockResult  — A block attempt's outcome. Same order, same reason.
- `src/sports/basketball/defence.ts`  type BlockResultName  = (typeof BlockResult)[keyof typeof BlockResult]
- `src/sports/basketball/defence.ts`  function resolveBlock  (defender, distance, releaseQuality, approach, speedDifferential, rng) => BlockResultName

- `src/sports/basketball/dribbling.ts`  interface HandlerRatings  { ballHandling, agility, strength, composure }  — What an athlete brings to carrying the ball and to the contact that comes with it.
- `src/sports/basketball/dribbling.ts`  interface BodyRatings  { strength, agility, interiorD, perimeterD }  — What a defender brings to taking it away by standing in the way.
- `src/sports/basketball/dribbling.ts`  const DRIBBLING
- `src/sports/basketball/dribbling.ts`  function fumbleChance  (ratings, pressure, speed, maxSpeed, sprinting) => number  — Per-step chance of losing the handle.
- `src/sports/basketball/dribbling.ts`  function fumbleBall  (world, ball, carrier, rng) => void  — Knocks the ball loose ahead of the carrier, so a fumble reads as a fumble rather than as the ball
- `src/sports/basketball/dribbling.ts`  const Contact  — What happened when a driver met a body.
- `src/sports/basketball/dribbling.ts`  type ContactName  = (typeof Contact)[keyof typeof Contact]
- `src/sports/basketball/dribbling.ts`  interface ContactResult  { kind, speedFactor, severity }
- `src/sports/basketball/dribbling.ts`  function resolveContact  (carrier, defender, closingSpeed, rng) => ContactResult  — Resolves contact on a drive: strength and agility against strength and interior defence.
- `src/sports/basketball/dribbling.ts`  function blowByChance  (carrier, defender) => number  — Chance of beating a defender off the dribble. Agility against perimeter defence, and nothing
- `src/sports/basketball/dribbling.ts`  function canBlowBy  (world, carrier, defender, targetX, targetY) => boolean  — Whether a blow-by is even on the table: close enough, fast enough, and going *past* the defender
- `src/sports/basketball/dribbling.ts`  function staggerFactor  (steps) => number  — Speed multiplier for an athlete with `steps` of stagger left, `0` meaning none.

- `src/sports/basketball/duties.ts`  const BASKETBALL_DUTIES  : DutyTable  — The five, keyed by `RoleTable` id. Deliberately the same ids the seam already publishes, so a
- `src/sports/basketball/duties.ts`  const BASKETBALL_TRANSITION_STEPS  — How many steps after a change of possession still counts as transition, for basketball. A fast

- `src/sports/basketball/index.ts`  interface BasketballState  { ball, ballState, profiles, sides, roleIndex, ratings, coupling, athleteIds, meter, shooter, …+29 }
- `src/sports/basketball/index.ts`  const basketball  : SportModule<BasketballState>
- `src/sports/basketball/index.ts`  function createBasketballMatch  (world, seed, playerSide?, rosters?, difficulty?) => { state: BasketballState; rng: Rng }  — Builds a match ready to step — used by the rules tests and, later, the balance harness.
- `src/sports/basketball/index.ts`  re-export BasketballEvent

- `src/sports/basketball/offball.ts`  interface Spot  { x, y }  — A place to stand. Structurally `cpu.ts`'s, kept separate so neither file owns the other.
- `src/sports/basketball/offball.ts`  const OFFBALL
- `src/sports/basketball/offball.ts`  const RollChoice  — How a screener leaves the screen.
- `src/sports/basketball/offball.ts`  type RollChoice  = (typeof RollChoice)[keyof typeof RollChoice]
- `src/sports/basketball/offball.ts`  function rollOrPop  (ratings) => RollChoice  — Whether a screener rolls or pops, from their own ratings and nothing else.
- `src/sports/basketball/offball.ts`  function rollTarget  (choice, handler, side, screener?) => Spot  — Where the screener goes once the screen is set: at the rim on a roll, and behind the handler —
- `src/sports/basketball/offball.ts`  interface ScreenLook  { id, distance, handlerSeparation, ratings, big }
- `src/sports/basketball/offball.ts`  interface DecisionNoise  { noise, rng, threshold }
- `src/sports/basketball/offball.ts`  function screenChoice  (looks, options?) => { readonly id: number; readonly urge: number } | null  — Who sets the ball screen, or `null` for "nobody, this possession does not want one".
- `src/sports/basketball/offball.ts`  interface CutLook  { toBasket, separation, laneGap, toBall, ratings, inSight }
- `src/sports/basketball/offball.ts`  function cutUrge  (look) => number  — How badly an off-ball athlete wants to cut, `0–1`. `0` is a veto — there is no cut here at all.
- `src/sports/basketball/offball.ts`  function possessionValueFor  (team) => number  — The continuation value of a possession for *this* offence, in points.
- `src/sports/basketball/offball.ts`  interface SchemeLook  { opponents, fouls }
- `src/sports/basketball/offball.ts`  function schemeFor  (look) => 'man' | 'zone'  — Man or zone, chosen from who is on the other side of the ball rather than from the match seed.

- `src/sports/basketball/passing.ts`  interface PasserRatings  { passing, composure }  — What a passer brings. `05` §3.1's `passing` row is awareness-heavy, and this is why.
- `src/sports/basketball/passing.ts`  interface ReceiverRatings  { ballHandling }  — What a receiver brings to holding on to it.
- `src/sports/basketball/passing.ts`  interface InterceptorRatings  { perimeterD }  — What a defender brings to reading it.
- `src/sports/basketball/passing.ts`  const PASSING
- `src/sports/basketball/passing.ts`  interface PassInFlight  { passer, side, target, releaseStep, expireStep, leadDistance, contested }  — A pass currently in the air.
- `src/sports/basketball/passing.ts`  function passSpeed  (distance, power?) => number  — Throw speed for a distance and a power input, so a short pass is not a rocket.
- `src/sports/basketball/passing.ts`  function leadTarget  (world, from, receiver, power?) => { x: number; y: number; flightTime: number }  — Where to throw so the ball and a moving receiver arrive together.
- `src/sports/basketball/passing.ts`  function passError  (ratings, distance, pressure) => number  — Angular error, in radians, that a passer puts on the throw. Distance and pressure both make it
- `src/sports/basketball/passing.ts`  function selectPassTarget  (world, from, aimX, aimY, candidates, assist?) => EntityId  — Pass assist (`06` §2): pick the teammate closest to where the player is aiming.
- `src/sports/basketball/passing.ts`  function throwPass  (world, ball, passer, side, target, toX, toY, flightTime, ratings, pressure, step, rng) => PassInFlight  — Puts a pass in the air towards a target point, with the passer's error on it.
- `src/sports/basketball/passing.ts`  function catchControl  (ratings, ballSpeed) => number  — How likely a receiver is to hold on to it. A fast ball into traffic is not a given.
- `src/sports/basketball/passing.ts`  function interceptControl  (ratings) => number  — How likely a defender is to come up with a ball they have got a hand to.
- `src/sports/basketball/passing.ts`  function canIntercept  (world, ball, athlete) => boolean  — Whether an athlete can get a hand to the ball this step.
- `src/sports/basketball/passing.ts`  function ballSpeed  (world, ball) => number  — Speed of the ball right now, for the catch and intercept draws.
- `src/sports/basketball/passing.ts`  function contest  (world, ball, athlete, control, rng) => boolean  — A contested attempt on a pass. Returns whether it was taken cleanly.

## src/sports/basketball/playbook/

- `src/sports/basketball/playbook/calls.ts`  const SHOT_AREAS  — Which contest a defensive call is shifting. Zones collapse to three places on the floor.
- `src/sports/basketball/playbook/calls.ts`  type ShotArea  = (typeof SHOT_AREAS)[number]
- `src/sports/basketball/playbook/calls.ts`  function areaOf  (zone) => ShotArea
- `src/sports/basketball/playbook/calls.ts`  interface OffensiveProfile  { id, zone, distance, movement, picks, attackKey, defendKey, turnover, foul, seconds, …+3 }  — What an offensive call tends to produce, before the defence has its say.
- `src/sports/basketball/playbook/calls.ts`  const OFFENSIVE_PROFILES  : readonly OffensiveProfile[]  — `09` §2.2's offensive table. The `keys off` column becomes `picks`/`attackKey`/`defendKey`; the
- `src/sports/basketball/playbook/calls.ts`  interface DefensiveProfile  { id, contest, turnover, foul, rebound, effort, imposes, broken, doubleTeam, opensTeammates }  — What a defensive call does to the possession it is defending.
- `src/sports/basketball/playbook/calls.ts`  const DEFENSIVE_PROFILES  : readonly DefensiveProfile[]  — `09` §2.2's defensive table. Man is the baseline — every number is zero — and everything else is
- `src/sports/basketball/playbook/calls.ts`  const BASKETBALL_CALLS  : readonly CallOption[]  — The call sheet's own words. `09` §2.2's "best when" and "effect" columns, verbatim in spirit.
- `src/sports/basketball/playbook/calls.ts`  function offensiveProfile  (id) => OffensiveProfile  — Motion and Man are the fallbacks: an unrecognised call plays the least opinionated thing.
- `src/sports/basketball/playbook/calls.ts`  function defensiveProfile  (id) => DefensiveProfile
- `src/sports/basketball/playbook/calls.ts`  function callOption  (id) => CallOption | undefined

- `src/sports/basketball/playbook/coach.ts`  interface ScoredCall  { call, score, because }
- `src/sports/basketball/playbook/coach.ts`  function scoreOffence  (state, side) => ScoredCall[]  — Every offensive call, priced for this squad and this moment, best first.
- `src/sports/basketball/playbook/coach.ts`  function scoreDefence  (state, side) => ScoredCall[]  — Every defensive call, priced for this squad. Simpler than the offence on purpose: without reading
- `src/sports/basketball/playbook/coach.ts`  function scoreCalls  (state, side) => ScoredCall[]
- `src/sports/basketball/playbook/coach.ts`  function coachCall  (state, side, rng) => PlaybookCall  — The coach's call. Samples the scores rather than taking the maximum, so a stretch of auto-called
- `src/sports/basketball/playbook/coach.ts`  function explainCall  (state, side, call) => string  — What the coach would say about its own choice, for the turn screen's one line.

- `src/sports/basketball/playbook/cpu.ts`  const READ_WINDOW  — How many recent possessions the read covers. Twelve is about an eighth of a side's match: long
- `src/sports/basketball/playbook/cpu.ts`  interface CallRead  { call, times, perTurn, share }  — What the CPU has noticed about one of the opponent's calls.
- `src/sports/basketball/playbook/cpu.ts`  function readTendencies  (turns, attacking, window?) => CallRead[]  — What the opponent has been calling lately, most-used first. Reads only committed turns — the same
- `src/sports/basketball/playbook/cpu.ts`  function readAdjustment  (reads, defensiveCall) => number  — The defensive adjustment a read is worth, per call. A defence that suppresses the area the
- `src/sports/basketball/playbook/cpu.ts`  function readAdjustmentForOffence  (turns, attacking, offensiveCall, window?) => number  — The offensive adjustment a read of the *defence* is worth. Symmetrical, and the reason the CPU
- `src/sports/basketball/playbook/cpu.ts`  function temperatureFor  (state) => number  — Temperature for a difficulty. Higher noise, wider sampling, worse decisions.
- `src/sports/basketball/playbook/cpu.ts`  function cpuCall  (state, side, rng, turns?) => PlaybookCall  — The CPU's call. Scores the sheet the way the coach does, adds what it has read of the opponent,
- `src/sports/basketball/playbook/cpu.ts`  function starOf  (state, side, turns, window?) => number | undefined  — Who to double: whoever has actually been scoring, falling back to whoever the opponent's best

- `src/sports/basketball/playbook/diagram.ts`  function buildDiagram  (state, resolution) => TurnDiagram  — The turn, as a timeline. Markers move first, then the pass, then the shot — the order `09` §2.1

- `src/sports/basketball/playbook/index.ts`  type BasketballPlaybook  = PlaybookAdapter<BasketballPlaybookState>
- `src/sports/basketball/playbook/index.ts`  const basketballPlaybook  : BasketballPlaybook  — The adapter. Every member delegates to the module that owns it: `calls.ts` for the catalogue,
- `src/sports/basketball/playbook/index.ts`  function createBasketballPlaybook  (options) => PlaybookMatch<BasketballPlaybookState>  — A basketball Playbook match, ready to take calls. The one door — the turn screen, the hot-seat
- `src/sports/basketball/playbook/index.ts`  re-export MOMENT_GAMES  from './key-moments.ts'
- `src/sports/basketball/playbook/index.ts`  re-export detectKeyMoment  from './key-moments.ts'
- `src/sports/basketball/playbook/index.ts`  re-export leverageFor  from './key-moments.ts'
- `src/sports/basketball/playbook/index.ts`  re-export coachCall  from './coach.ts'
- `src/sports/basketball/playbook/index.ts`  re-export explainCall  from './coach.ts'
- `src/sports/basketball/playbook/index.ts`  re-export scoreCalls  from './coach.ts'
- `src/sports/basketball/playbook/index.ts`  re-export scoreDefence  from './coach.ts'
- `src/sports/basketball/playbook/index.ts`  re-export scoreOffence  from './coach.ts'
- `src/sports/basketball/playbook/index.ts`  re-export READ_WINDOW  from './cpu.ts'
- `src/sports/basketball/playbook/index.ts`  re-export cpuCall  from './cpu.ts'
- `src/sports/basketball/playbook/index.ts`  re-export readTendencies  from './cpu.ts'
- `src/sports/basketball/playbook/index.ts`  re-export starOf  from './cpu.ts'
- `src/sports/basketball/playbook/index.ts`  re-export temperatureFor  from './cpu.ts'
- `src/sports/basketball/playbook/index.ts`  re-export BASKETBALL_CALLS  from './calls.ts'
- `src/sports/basketball/playbook/index.ts`  re-export basketballSquad  from './squad.ts'
- `src/sports/basketball/playbook/index.ts`  re-export playbookRatings  from './squad.ts'
- `src/sports/basketball/playbook/index.ts`  re-export basketballSquads
- `src/sports/basketball/playbook/index.ts`  re-export BasketballPlaybookState  from './resolution.ts'

- `src/sports/basketball/playbook/key-moments.ts`  const MOMENT_GAMES  — `09` §2.4's five, against `09` §3.2's five.
- `src/sports/basketball/playbook/key-moments.ts`  function leverageFor  (state, base) => number  — How much this moment matters, `0–1`. Late and close lifts every moment towards its ceiling; early
- `src/sports/basketball/playbook/key-moments.ts`  function detectKeyMoment  (state, resolution) => ArcadeInvocation | null  — The moment worth playing, or `null`. Order matters: a buzzer-beater outranks the shot it happens
- `src/sports/basketball/playbook/key-moments.ts`  function applyKeyMomentOutcome  (state, resolution, outcome) => TurnResolution  — The player's result, folded back in. The moment decides what "made" means: a shot goes in or does

- `src/sports/basketball/playbook/narration.ts`  function shortName  (athlete) => string  — Family name where there is one, so a line reads like commentary rather than like a database.
- `src/sports/basketball/playbook/narration.ts`  function pickLine  (options, turn, outcome) => string  — Stable pick: the same turn always reads the same way. A hash rather than a draw, so narration
- `src/sports/basketball/playbook/narration.ts`  function narrateTurn  (state, resolution) => NarrationLine
- `src/sports/basketball/playbook/narration.ts`  const NARRATED_OUTCOMES  — Every outcome the resolution model produces has a line, and the test asserts exactly that.

- `src/sports/basketball/playbook/resolution.ts`  interface BasketballPlaybookState  { teamFouls, lastWasStop, period }  — What the sport tracks between turns. Small on purpose — the engine owns everything shared.
- `src/sports/basketball/playbook/resolution.ts`  const RESOLUTION  — Tuning. One table, for the same reason `SHOOTING` is one table: a balance pass that has to hunt
- `src/sports/basketball/playbook/resolution.ts`  function shooterRatings  (athlete) => ShooterRatings  — The five ratings the shooting model reads, pulled out of a derived-rating bag.
- `src/sports/basketball/playbook/resolution.ts`  function zoneValue  (zone) => 2 | 3  — 2 or 3, from the zone alone — the same split `court.ts` makes by geometry.
- `src/sports/basketball/playbook/resolution.ts`  function primaryOption  (players, profile, target) => PlaybookAthlete  — Who takes it. A targeted call takes the named athlete when they are on the floor; otherwise the
- `src/sports/basketball/playbook/resolution.ts`  function primaryDefender  (players, profile) => PlaybookAthlete  — The defender the play runs at: whoever the opponent's scheme puts in the way.
- `src/sports/basketball/playbook/resolution.ts`  function matchupEdge  (attack, defend, stamina) => number  — The matchup, `-0.5 … +0.5`. Positive favours the offence. Logistic over a rating difference, the
- `src/sports/basketball/playbook/resolution.ts`  function releaseFor  (ratings, key, rng) => number  — How well the shooter got it off. Rating sets the centre; composure narrows the spread.
- `src/sports/basketball/playbook/resolution.ts`  interface ResolveInput  { state, calls, rng }
- `src/sports/basketball/playbook/resolution.ts`  function resolvePossession  (input) => TurnResolution  — One possession. Everything a turn is: the branch, the shot, the rebound, and the events that
- `src/sports/basketball/playbook/resolution.ts`  function describeOutcome  (input) => string  — The sport's own name for what happened. Narration (T-5.3) reads this and nothing else.
- `src/sports/basketball/playbook/resolution.ts`  function because  (input) => string  — One line naming what actually drove the number — `09` §2.4's honesty, at turn scale.
- `src/sports/basketball/playbook/resolution.ts`  function drainStamina  (players, effort) => void  — Stamina after a turn: the side running the play works, the side that just watched recovers.

- `src/sports/basketball/playbook/squad.ts`  function playbookRatings  (athlete) => DerivedRatings  — Every number a Playbook turn can read off one athlete.
- `src/sports/basketball/playbook/squad.ts`  function basketballSquad  (athletes, side) => PlaybookSquad  — Builds one side's floor. Entity ids follow Live's convention — side 0 takes `0…n-1`, side 1 takes
- `src/sports/basketball/playbook/squad.ts`  function basketballSquads  (home, away) => readonly [PlaybookSquad, PlaybookSquad]

## src/sports/basketball/

- `src/sports/basketball/rebounding.ts`  interface RebounderRatings  { rebounding, vertical, strength }  — What an athlete brings to the glass. `rebounding` already carries the height modifier.
- `src/sports/basketball/rebounding.ts`  const REBOUNDING
- `src/sports/basketball/rebounding.ts`  interface Contender  { athlete, side, weight, boxedOut, timing }  — One athlete's claim on the ball.
- `src/sports/basketball/rebounding.ts`  function jumpTiming  (ratings, rng) => number  — How well an athlete timed the jump, `0–1`.
- `src/sports/basketball/rebounding.ts`  function reboundSkill  (ratings) => number  — The rating half of the contest: `06` §3.1's height (via `rebounding`), vertical, and strength.
- `src/sports/basketball/rebounding.ts`  function contenderWeight  (ratings, distanceToBall, boxedOut, timing) => number  — One athlete's weight in the draw.
- `src/sports/basketball/rebounding.ts`  function isBoxedOut  (athlete, opponent, basket) => boolean  — Whether an opponent has sealed an athlete off the glass: standing between them and the basket,
- `src/sports/basketball/rebounding.ts`  function pickRebounder  (contenders, rng) => Contender | null  — Picks the winner by weight. Deterministic given the rng, and stable in the face of an athlete

- `src/sports/basketball/roster.ts`  const BASKETBALL_TABLES
- `src/sports/basketball/roster.ts`  const BASKETBALL_SPORT_ID
- `src/sports/basketball/roster.ts`  interface BasketballRatings  { finishing, midRange, threePoint, freeThrow, composure, passing, ballHandling, perimeterD, interiorD, agility, …+4 }  — Everything basketball's models read off one athlete. Mirrors the module's `AthleteRatings`.
- `src/sports/basketball/roster.ts`  function basketballRatings  (athlete) => BasketballRatings  — One athlete's basketball numbers, with fatigue applied.
- `src/sports/basketball/roster.ts`  function basketballMovement  (athlete) => MovementProfile  — How this athlete moves. `courtSpeed` is a *derived* rating — it already folds speed,
- `src/sports/basketball/roster.ts`  function basketballCoupling  (athlete) => Coupling  — How lost this athlete is in basketball (T-3.6). `NO_COUPLING`-equal for anyone at home.
- `src/sports/basketball/roster.ts`  type SideRoster  = readonly Athlete[]  — One side's athletes, in the order they fill the role table.
- `src/sports/basketball/roster.ts`  interface MatchRosters  { home, away }  — The rosters a match is played with. Absent, or short, and the module fills the rest from the
- `src/sports/basketball/roster.ts`  interface RosterEntry  { athleteId, ratings, movement, coupling }  — Everything the module needs about one athlete, computed once at spawn.
- `src/sports/basketball/roster.ts`  function rosterEntry  (athlete) => RosterEntry

- `src/sports/basketball/rules.ts`  const TIMING  — Every duration basketball cares about, in the units the player reads them in.
- `src/sports/basketball/rules.ts`  const CLOCK_COMPRESSION  — Game seconds per real second. Derived, never authored — the two quarter figures define it.
- `src/sports/basketball/rules.ts`  function gameSecondsToSteps  (gameSeconds) => number  — Game seconds → simulation steps. The single place compression is applied.
- `src/sports/basketball/rules.ts`  function stepsToGameSeconds  (steps) => number  — Simulation steps → the game seconds a HUD should show, rounded up as a real clock does.
- `src/sports/basketball/rules.ts`  const BASKETBALL_RULES  : MatchRules
- `src/sports/basketball/rules.ts`  const FOULS  — Foul limits.
- `src/sports/basketball/rules.ts`  const RestartKind  — Why play is stopped and how it starts again.
- `src/sports/basketball/rules.ts`  type RestartKindName  = (typeof RestartKind)[keyof typeof RestartKind]
- `src/sports/basketball/rules.ts`  const BasketballEvent  — Sport-specific event names, carried on `EventKind.SPORT` as `sportKind` (INV-9).
- `src/sports/basketball/rules.ts`  interface Restart  { kind, side, x, y, reason }
- `src/sports/basketball/rules.ts`  interface RulesState  { possession, arrow, shotClock, shotClockRunning, frontcourt, restart, restartReady, restartClock, lastTouch, personalFouls, …+3 }  — The whole rule book's state. Deliberately flat and JSON-shaped: it goes into snapshots and
- `src/sports/basketball/rules.ts`  interface FreeThrowSet  { shooter, side, total, remaining, made }  — A trip to the line.
- `src/sports/basketball/rules.ts`  function createRulesState  (arrow?) => RulesState
- `src/sports/basketball/rules.ts`  function inBonus  (state, side) => boolean  — Whether a side has fouled enough this period that every further foul is two shots.
- `src/sports/basketball/rules.ts`  function personalFouls  (state, athlete) => number
- `src/sports/basketball/rules.ts`  function isFouledOut  (state, athlete) => boolean
- `src/sports/basketball/rules.ts`  function recordFoul  (state, offender, offenderSide, fouled, step, options?) => SportEvent[]  — Records a foul and decides what it costs.
- `src/sports/basketball/rules.ts`  function awardFreeThrows  (state, shooter, side, count, step) => SportEvent[]  — Sends a shooter to the line. The clock is stopped and stays stopped until the last one lands.
- `src/sports/basketball/rules.ts`  function resolveFreeThrow  (state, made, step) => SportEvent[]  — Resolves one free throw. The last one is what decides how play restarts: a make is inbounded by
- `src/sports/basketball/rules.ts`  function shotClockSeconds  (state) => number  — Shot clock as the HUD shows it: seconds remaining, one decimal under five.
- `src/sports/basketball/rules.ts`  function gameClockSeconds  (stepInPeriod, period) => number  — Game clock remaining in the period, in game seconds, from the engine's step-in-period.
- `src/sports/basketball/rules.ts`  function formatClock  (gameSeconds) => string  — `M:SS`, or `S.T` inside the last minute — the convention every basketball clock uses.
- `src/sports/basketball/rules.ts`  const ShotClockReset  — How much shot clock a new possession starts with.
- `src/sports/basketball/rules.ts`  type ShotClockResetName  = (typeof ShotClockReset)[keyof typeof ShotClockReset]
- `src/sports/basketball/rules.ts`  function grantPossession  (state, side, step, reset?, ballX?, actor?) => SportEvent[]  — Hands the ball to a side with the ball live — a rebound, a steal, a completed inbound.
- `src/sports/basketball/rules.ts`  function registerTouch  (state, side) => void  — Records who touched the ball last, which is what decides an out-of-bounds award.
- `src/sports/basketball/rules.ts`  function tickClocks  (state, ballX, step) => SportEvent[]  — Advances every count that runs while the ball is live, and returns whatever they caused.
- `src/sports/basketball/rules.ts`  function checkOutOfBounds  (state, ballX, ballY, step) => SportEvent[]  — Whether the ball has left the court, and what to do about it. The award goes *against* whoever
- `src/sports/basketball/rules.ts`  function checkBackcourt  (state, ballX, step) => SportEvent[]  — The backcourt violation: once the offence has established the frontcourt, the ball may not go
- `src/sports/basketball/rules.ts`  function onBasketMade  (state, scoringSide, step) => SportEvent[]  — A made basket: the conceding side inbounds from its own baseline.
- `src/sports/basketball/rules.ts`  function onPeriodStart  (state, period, step) => SportEvent[]  — The opening tip, and the start of every subsequent period.
- `src/sports/basketball/rules.ts`  function awardRestart  (state, restart, step) => SportEvent[]  — Puts a restart in place, stopping the clocks that should not run through it.
- `src/sports/basketball/rules.ts`  function markRestartReady  (state, step) => SportEvent[]  — The inbounder has the ball at the spot: start the five-second count.
- `src/sports/basketball/rules.ts`  function completeRestart  (state, step, ballX) => SportEvent[]  — The inbound pass has been released — the ball is live again.
- `src/sports/basketball/rules.ts`  function inboundAfterScoreSpot  (concedingSide) => Spot  — Where the conceding side inbounds after giving up a basket — beside the backboard, not behind it.

- `src/sports/basketball/shooting.ts`  interface ShooterRatings  { finishing, midRange, threePoint, freeThrow, composure }  — The basketball ratings a shot reads, `0–100`. Supplied by the athlete layer from T-3.3; until
- `src/sports/basketball/shooting.ts`  const ShotMovement  — How the shooter was moving when they let go. `06` §3.1 names exactly these three.
- `src/sports/basketball/shooting.ts`  type ShotMovementName  = (typeof ShotMovement)[keyof typeof ShotMovement]
- `src/sports/basketball/shooting.ts`  interface ShotInput  { ratings, distance, zone, contest, release, movement, stamina, clockPressure }  — Everything the make-probability model reads.
- `src/sports/basketball/shooting.ts`  const SHOOTING  — Tuning. One table, because a balance pass (T-2.13) that has to hunt for numbers is a balance
- `src/sports/basketball/shooting.ts`  function ratingForZone  (ratings, zone) => number  — Which rating a shot from here is judged on. `06` §3.1 names three; the zones map onto them.
- `src/sports/basketball/shooting.ts`  function baseChance  (distance) => number  — Make chance from distance alone, for an average shooter with everything else perfect.
- `src/sports/basketball/shooting.ts`  function shotProbability  (input) => number  — The model. Multiplicative rather than additive: a smothered shot from an elite shooter is still a
- `src/sports/basketball/shooting.ts`  function contestLevel  (distance, reach?) => number  — How hard the nearest defender is making it. Distance does most of the work; reach adds the
- `src/sports/basketball/shooting.ts`  function contestFromDirection  (distance, alignment, reach?) => number  — How much of a contest a defender in a given *direction* is.
- `src/sports/basketball/shooting.ts`  interface ShotMeter  { charge, window, movement }  — The release meter (`06` §2). A shot is charged by holding and taken by letting go; how close the
- `src/sports/basketball/shooting.ts`  function releaseWindow  (rating, timingAssist?) => number  — Window half-width. A great shooter has a forgiving window and a poor one a tiny one (`06` §2);
- `src/sports/basketball/shooting.ts`  function startShot  (ratings, zone, movement, timingAssist?) => ShotMeter
- `src/sports/basketball/shooting.ts`  function isOverheld  (meter) => boolean  — True once the shooter has held so long the shot goes up on its own.
- `src/sports/basketball/shooting.ts`  function releaseQuality  (meter) => number  — Release quality, `0–1`. Linear inside the window and zero outside it: a curve that stays generous
- `src/sports/basketball/shooting.ts`  interface ShotInFlight  { shooter, side, value, made, resolveStep, probability, zone, release, contest }  — A shot the ball is currently in the air for.
- `src/sports/basketball/shooting.ts`  function flightTime  (distance) => number  — Flight time for a shot, in seconds. Long shots hang longer, which is what makes them readable.
- `src/sports/basketball/shooting.ts`  function takeShot  (world, ball, shooter, side, input, step, rng) => ShotInFlight  — Puts the ball in the air and decides the shot.
- `src/sports/basketball/shooting.ts`  function caromOffRim  (world, ball, side, rng) => void  — Sends a missed ball off the rim, so the rebound (T-2.6) is a real loose ball rather than a
- `src/sports/basketball/shooting.ts`  function dropThroughNet  (world, ball, side) => void  — Drops a made ball through the net, so the restart starts from under the basket.
- `src/sports/basketball/shooting.ts`  function shotInputAt  (x, y, side, ratings, options?) => ShotInput  — Builds the model's input from a position and the shooter's circumstances.

- `src/sports/basketball/weights.ts`  const BASKETBALL_WEIGHTS  : RatingWeightTable  — Rows sum to 1.0. A missing attribute is a zero — written out rather than implied, so a row that
- `src/sports/basketball/weights.ts`  const BASKETBALL_HEIGHT_MODIFIERS  : Readonly<Record<string, number>>  — Height adjustments applied after the weighted sum, per centimetre away from 195 cm.
- `src/sports/basketball/weights.ts`  const BASKETBALL_REFERENCE_HEIGHT_CM  — The height the modifiers are measured from.
- `src/sports/basketball/weights.ts`  const BASKETBALL_PHYSICAL  : PhysicalModifiers  — The same table in the shape the derivation engine consumes (T-3.3).
- `src/sports/basketball/weights.ts`  const BASKETBALL_POSITION_WEIGHTS  : PositionWeightTable  — Position → derived-rating weights, for overall and position fit (`05` §3.4). Rows sum to 1.0.

- `src/sports/basketball/xp.ts`  const BASKETBALL_XP_AWARDS  : XpAwardTable  — Every rating named here exists in `BASKETBALL_WEIGHTS`; a test asserts it, because a typo would

## src/sports/

- `src/sports/catalogue.ts`  interface RateableSport  { id, displayName, playable, tables }
- `src/sports/catalogue.ts`  const RATEABLE_SPORTS  : readonly RateableSport[]  — In the order the sport switcher shows them: playable first, then merely rateable.
- `src/sports/catalogue.ts`  function rateableSport  (id) => RateableSport | undefined
- `src/sports/catalogue.ts`  function sportsForAthlete  (primarySport) => readonly RateableSport[]  — The sports to show for an athlete, their own first. Someone opening a soccer player's card

- `src/sports/playable.ts`  interface PlayableSport  { id, displayName, load }
- `src/sports/playable.ts`  const PLAYABLE_SPORTS  : readonly PlayableSport[]  — In the order a picker should show them. Adding a sport is adding a row.
- `src/sports/playable.ts`  const DEFAULT_SPORT  : SportId
- `src/sports/playable.ts`  function playableSport  (id) => PlayableSport
- `src/sports/playable.ts`  function isPlayable  (id) => boolean  — Whether a sport id names something playable, for a router that should 404 rather than guess.
- `src/sports/playable.ts`  function loadSport  (id) => Promise<SportModule<SportState>>  — Loads a sport module by id, falling back to the default rather than throwing.

## src/sports/soccer/arcade/

- `src/sports/soccer/arcade/free-kick.ts`  const FREE_KICK_ROUNDS  — Kicks in a scored run.
- `src/sports/soccer/arcade/free-kick.ts`  const FREE_KICK_DISTANCE  — How far out a kick is taken from, in metres. The near end is a shooting-practice free kick just
- `src/sports/soccer/arcade/free-kick.ts`  function windLabel  (wind) => string  — What the caption says about the wind. Words, never an arrow alone (`10` §11, T-4.12).
- `src/sports/soccer/arcade/free-kick.ts`  const freeKickGame  : ArcadeGameDef

- `src/sports/soccer/arcade/header.ts`  const HEADER_ROUNDS  — Crosses in a scored run. A count, not a clock.
- `src/sports/soccer/arcade/header.ts`  const CONTACT_HEIGHT_M  — The height a cross worth attacking arrives at, in metres.
- `src/sports/soccer/arcade/header.ts`  const CROSSES  — The two crosses, and the trade they represent.
- `src/sports/soccer/arcade/header.ts`  type CrossKind  = keyof typeof CROSSES
- `src/sports/soccer/arcade/header.ts`  const headerGame  : ArcadeGameDef

- `src/sports/soccer/arcade/index.ts`  const SOCCER_ARCADE  : readonly ArcadeGameDef[]
- `src/sports/soccer/arcade/index.ts`  re-export penaltyShootoutGame
- `src/sports/soccer/arcade/index.ts`  re-export freeKickGame
- `src/sports/soccer/arcade/index.ts`  re-export oneOnOneGame
- `src/sports/soccer/arcade/index.ts`  re-export headerGame
- `src/sports/soccer/arcade/index.ts`  re-export lastLineGame
- `src/sports/soccer/arcade/index.ts`  re-export ROUNDS_PER_RUN  from './penalty-shootout.ts'
- `src/sports/soccer/arcade/index.ts`  re-export pickSide  from './penalty-shootout.ts'
- `src/sports/soccer/arcade/index.ts`  re-export FREE_KICK_ROUNDS  from './free-kick.ts'
- `src/sports/soccer/arcade/index.ts`  re-export FREE_KICK_DISTANCE  from './free-kick.ts'
- `src/sports/soccer/arcade/index.ts`  re-export windLabel  from './free-kick.ts'
- `src/sports/soccer/arcade/index.ts`  re-export ONE_ON_ONE_ROUNDS  from './one-on-one.ts'
- `src/sports/soccer/arcade/index.ts`  re-export APPROACH_SECONDS  from './one-on-one.ts'
- `src/sports/soccer/arcade/index.ts`  re-export HEADER_ROUNDS  from './header.ts'
- `src/sports/soccer/arcade/index.ts`  re-export CROSSES  from './header.ts'
- `src/sports/soccer/arcade/index.ts`  re-export CONTACT_HEIGHT_M  from './header.ts'
- `src/sports/soccer/arcade/index.ts`  re-export LAST_LINE_SECONDS  from './last-line.ts'
- `src/sports/soccer/arcade/index.ts`  re-export SOCCER_ARCADE_SPORT  from './shared.ts'
- `src/sports/soccer/arcade/index.ts`  re-export saveEvent  from './shared.ts'
- `src/sports/soccer/arcade/index.ts`  re-export shotEvents  from './shared.ts'
- `src/sports/soccer/arcade/index.ts`  re-export soccerCalibration  from './shared.ts'

- `src/sports/soccer/arcade/last-line.ts`  const LAST_LINE_SECONDS  — How long a run lasts. The only clocked game in soccer's set (`09` §3.1 — 20–90 s).
- `src/sports/soccer/arcade/last-line.ts`  const lastLineGame  : ArcadeGameDef

- `src/sports/soccer/arcade/one-on-one.ts`  const ONE_ON_ONE_ROUNDS  — Chances in a scored run. A count, not a clock — the athlete's speed must not buy more of them.
- `src/sports/soccer/arcade/one-on-one.ts`  const APPROACH_SECONDS  — How long the approach lasts, in seconds — from through-on-goal to smothered.
- `src/sports/soccer/arcade/one-on-one.ts`  const oneOnOneGame  : ArcadeGameDef

- `src/sports/soccer/arcade/penalty-shootout.ts`  const ROUNDS_PER_RUN  — Rounds in a scored run: five each way, as a shootout is, then five more because a run that ends
- `src/sports/soccer/arcade/penalty-shootout.ts`  interface ShootoutRound  { taking, scored }  — What a round is: who is taking it, and how it went.
- `src/sports/soccer/arcade/penalty-shootout.ts`  function pickSide  (draw) => GoalSide  — Which third of the goal a `0–1` draw names. Even thirds; the keeper has no favourite side.
- `src/sports/soccer/arcade/penalty-shootout.ts`  const penaltyShootoutGame  : ArcadeGameDef

- `src/sports/soccer/arcade/shared.ts`  const SOCCER_ARCADE_SPORT
- `src/sports/soccer/arcade/shared.ts`  const ARCADE_ACTOR  — The entity every arcade event is attributed to. An arcade run has one athlete and no world, but
- `src/sports/soccer/arcade/shared.ts`  function soccerCalibration  (athlete, difficulty, ratings, shape?) => ArcadeCalibration  — The calibration for one game, given the ratings it reads. Every soccer game goes through here, so
- `src/sports/soccer/arcade/shared.ts`  function shotEvents  (options) => readonly SportEvent[]  — A shot on goal, in the shape Live emits it (INV-9). `step` is stamped by the framework.
- `src/sports/soccer/arcade/shared.ts`  function saveEvent  () => SportEvent  — A save. The one keeper action that reliably emits, and what `SOCCER_XP_AWARDS` pays for.

## src/sports/soccer/

- `src/sports/soccer/art.ts`  type Theme  = 'dark' | 'light'
- `src/sports/soccer/art.ts`  interface KitPalette  { fill, onFill }  — A team's fill, and the ink its kit marking and facing tick draw in on top of that fill.
- `src/sports/soccer/art.ts`  interface SoccerPalette  { teams, keeper, ball, ballPanel }
- `src/sports/soccer/art.ts`  function paletteFor  (theme?) => SoccerPalette  — Picks the palette for a theme. Defaults dark, per `10` §3.1's dark-first.
- `src/sports/soccer/art.ts`  const ATHLETE_RADIUS  — World-space radii, for a caller without an entity of its own (a fixture, the gallery).
- `src/sports/soccer/art.ts`  const BALL_RADIUS
- `src/sports/soccer/art.ts`  interface AthleteDrawOptions  { team, controlled, radius, keeper }
- `src/sports/soccer/art.ts`  function drawAthlete  (ctx, x, y, facing, kit, detail, options) => void  — One athlete, seen from above.
- `src/sports/soccer/art.ts`  interface BallDrawOptions  { radius }
- `src/sports/soccer/art.ts`  function drawBall  (ctx, x, y, z, palette, detail, options?) => void  — The ball. A football: white, with dark panels, and **no seam line** — the seam is the one thing

- `src/sports/soccer/ball.ts`  const SOCCER_BALL_PHYSICS  : BallPhysics
- `src/sports/soccer/ball.ts`  const ROLL_DECAY_PER_METRE  — How fast a rolling ball loses speed *per metre travelled*, rather than per second.

- `src/sports/soccer/defending.ts`  interface TacklerRatings  { tackling, marking }  — What a defender brings. Both are derived soccer ratings from `05` §3.2.
- `src/sports/soccer/defending.ts`  interface CarrierDefenceRatings  { dribbling, pace }  — What the carrier brings to keeping it.
- `src/sports/soccer/defending.ts`  type TackleKind  = 'standing' | 'slide'
- `src/sports/soccer/defending.ts`  const DEFENDING
- `src/sports/soccer/defending.ts`  function pressureOn  (carrier, defenders) => number  — How closely the carrier is being closed down, `0–1`.
- `src/sports/soccer/defending.ts`  function tackleReach  (kind) => number  — How far the challenge can reach.
- `src/sports/soccer/defending.ts`  function tackleTiming  (distanceToBall, kind) => number  — How well the challenge is placed, `0–1`.
- `src/sports/soccer/defending.ts`  function tackleOdds  (tackler, carrier, timing, kind) => number  — The defender's chance of winning the ball cleanly, before the draw.
- `src/sports/soccer/defending.ts`  interface TackleOutcome  { won, foul, committed }
- `src/sports/soccer/defending.ts`  function resolveTackle  (tackler, carrier, timing, kind, closingSpeed, rng) => TackleOutcome  — Resolves one challenge.
- `src/sports/soccer/defending.ts`  function severityOf  (timing, kind, closingSpeed) => FoulSeverity  — How bad the foul was.

- `src/sports/soccer/dribbling.ts`  interface CarrierRatings  { dribbling, pace }  — What a carrier brings. All three are derived soccer ratings from `05` §3.2 — unlike shooting,
- `src/sports/soccer/dribbling.ts`  interface ShieldingRatings  { tackling, marking }  — What a defender brings to leaning on one.
- `src/sports/soccer/dribbling.ts`  const DRIBBLE
- `src/sports/soccer/dribbling.ts`  type StaminaState  = Record<number, number>  — Stamina per athlete, `0–1`.
- `src/sports/soccer/dribbling.ts`  function createStamina  () => StaminaState
- `src/sports/soccer/dribbling.ts`  function stamina  (state, athlete) => number  — An athlete not yet in the record is fresh, which is the right default for a substitute.
- `src/sports/soccer/dribbling.ts`  function tickStamina  (state, athlete, effort) => number  — Advances one athlete's stamina by one step.
- `src/sports/soccer/dribbling.ts`  function staminaFactor  (value) => number  — Multiplier stamina puts on top speed. `1` fresh, `DRIBBLE.exhaustedSpeed` empty.
- `src/sports/soccer/dribbling.ts`  function movementRatingsFor  (ratings) => MovementRatings  — Soccer's ratings in the engine's terms.
- `src/sports/soccer/dribbling.ts`  function dribbleProfile  (ratings, staminaValue, options?) => MovementProfile  — The profile an athlete actually moves with: their body, then the ball, the sprint, and the tank.
- `src/sports/soccer/dribbling.ts`  function touchDistance  (ratings, sprinting) => number  — How far in front of the carrier the ball sits.
- `src/sports/soccer/dribbling.ts`  function shieldPosition  (carrier, ball, defender) => number  — How well the carrier's body is between the defender and the ball, `-1…1`.
- `src/sports/soccer/dribbling.ts`  function shieldOdds  (carrier, defender, position) => number  — The carrier's chance of keeping it, without drawing. What the CPU reads before committing.
- `src/sports/soccer/dribbling.ts`  function resolveShield  (carrier, defender, position, rng) => boolean  — The seeded half: whether the carrier holds them off.

- `src/sports/soccer/duties.ts`  type Line  = 'keeper' | 'defence' | 'midfield' | 'attack'  — Which band of the pitch a role belongs to. Everything else here follows from it.
- `src/sports/soccer/duties.ts`  function lineOf  (role) => Line  — The line a role plays in, from where it stands. By position rather than by id, so a formation
- `src/sports/soccer/duties.ts`  function soccerDuties  (formationId?) => DutyTable  — Every role of a formation, with its duties. Keyed by role id, like the seam's `RoleTable`.
- `src/sports/soccer/duties.ts`  const SOCCER_TRANSITION_STEPS  — Steps after a change of possession that still count as transition, for soccer. Longer than

- `src/sports/soccer/formations.ts`  type PlayPhase  = 'attacking' | 'building' | 'defending'  — What the team is doing, which is what shape answers to.
- `src/sports/soccer/formations.ts`  interface FormationRole  { id, name, x, y, push, drop, tuck }  — One position in a formation.
- `src/sports/soccer/formations.ts`  interface Formation  { id, name, aggression, roles }
- `src/sports/soccer/formations.ts`  const FORMATIONS  : readonly Formation[]  — The three formations `03` asks for.
- `src/sports/soccer/formations.ts`  const DEFAULT_FORMATION
- `src/sports/soccer/formations.ts`  function formation  (id) => Formation
- `src/sports/soccer/formations.ts`  function soccerRoles  (id?) => RoleTable  — Every formation's role ids, for the seam's `RoleTable`. Keyed on the default shape.
- `src/sports/soccer/formations.ts`  interface RoleSpot  { id, x, y }  — One athlete's target spot, in metres.
- `src/sports/soccer/formations.ts`  function roleSpot  (role, phase, side) => RoleSpot  — Where a role should be right now.
- `src/sports/soccer/formations.ts`  function shapeFor  (formationId, phase, side) => readonly RoleSpot[]  — The whole shape, in metres, for one side in one phase. Allocates; see `cachedShape`.
- `src/sports/soccer/formations.ts`  function cachedShape  (formationId, phase, side) => readonly RoleSpot[]  — `shapeFor`, memoised and frozen — the version the simulation calls sixty times a second.
- `src/sports/soccer/formations.ts`  function defensiveLineX  (formationId, phase, side) => number  — How high the defensive line sits, as an x in metres.
- `src/sports/soccer/formations.ts`  function phaseFor  (side, possession, ballX) => PlayPhase  — Which phase a side is in, from possession and where the ball is.

- `src/sports/soccer/fouls.ts`  const ADVANTAGE_REAL_SECONDS  — How long the referee lets an advantage run before deciding it came to nothing.
- `src/sports/soccer/fouls.ts`  type FoulKind  = 'trip' | 'push' | 'holding' | 'handball' | 'slideTackle' | 'dan…  — The offences the tackle and challenge models can produce.
- `src/sports/soccer/fouls.ts`  type FoulSeverity  = 'careless' | 'reckless' | 'excessive'  — The Laws' own three degrees. T-6.8's tackle model produces one of these.
- `src/sports/soccer/fouls.ts`  type CardColour  = 'yellow' | 'red'
- `src/sports/soccer/fouls.ts`  interface FoulContext  { offender, offenderSide, victim, x, y, kind, severity, dogso, advantage }  — Everything the referee saw. Plain data, so a replay reproduces the decision exactly.
- `src/sports/soccer/fouls.ts`  interface FoulOutcome  { restart, card, sentOff, events }
- `src/sports/soccer/fouls.ts`  function restartForFoul  (context) => Restart  — Where and what the restart is. A foul by the defending side inside its own penalty area is a
- `src/sports/soccer/fouls.ts`  function cardFor  (context) => CardColour | null  — The card, before second-yellow bookkeeping.
- `src/sports/soccer/fouls.ts`  function commitFoul  (state, context, step) => FoulOutcome  — Records a foul and decides what it costs: the card, the send-off, and either the restart or an
- `src/sports/soccer/fouls.ts`  function tickAdvantage  (state, stillAttacking, step) => SportEvent[]  — Advances an advantage by one step.
- `src/sports/soccer/fouls.ts`  function isPlayingAdvantage  (state) => boolean  — Whether an advantage is currently running — the check the module's `step()` needs.

- `src/sports/soccer/index.ts`  interface SoccerState  { sport, ballState, rules, stamina, sides, roleIndex, ratings, athleteIds, keepers, formations, …+9 }
- `src/sports/soccer/index.ts`  const soccer  : SoccerModule

- `src/sports/soccer/keeper.ts`  interface KeeperRatings  { goalkeeping }  — What a keeper brings. `goalkeeping` is a derived soccer rating (`05` §3.2).
- `src/sports/soccer/keeper.ts`  const KEEPER
- `src/sports/soccer/keeper.ts`  function keeperSpot  (ballX, ballY, side, aggression?) => Spot  — Where the keeper should be standing, given where the ball is.
- `src/sports/soccer/keeper.ts`  function reactionTime  (ratings) => number  — Seconds lost before the keeper moves at all.
- `src/sports/soccer/keeper.ts`  function diveReach  (ratings, seconds) => number  — How far the keeper can get in a given time, including their standing reach.
- `src/sports/soccer/keeper.ts`  function interceptPoint  (from, aim, goalX, keeperX, releaseZ?) => { y: number; z: number }  — Where the ball passes the keeper's own depth, rather than where it ends up in the goal.
- `src/sports/soccer/keeper.ts`  function diveDistance  (keeper, ball, keeperHeight?) => number  — How far the keeper has to travel to reach the ball, across the goal and up it.
- `src/sports/soccer/keeper.ts`  function saveChance  (ratings, keeper, aim, flightSeconds) => number  — The chance of a save, `0–1`.
- `src/sports/soccer/keeper.ts`  type SaveResult  = 'caught' | 'parried' | 'beaten'
- `src/sports/soccer/keeper.ts`  function saveOutcome  (ratings, keeper, aim, flightSeconds, ballSpeed, rng) => SaveResult  — Whether the keeper stops it, and whether they hold it.
- `src/sports/soccer/keeper.ts`  function claimChance  (ratings, keeper, ball) => number  — Whether the keeper can come and claim a ball in the air.
- `src/sports/soccer/keeper.ts`  function resolveClaim  (ratings, keeper, ball, rng) => boolean  — The seeded half of a claim.
- `src/sports/soccer/keeper.ts`  function distributionChoice  (pressure, hasShortOption) => PassKind  — What the keeper does with it once they have it.
- `src/sports/soccer/keeper.ts`  function isKeeperManual  (restartKind, playerControlsKeeper) => boolean  — Whether the player takes the gloves for this moment.

- `src/sports/soccer/offside.ts`  interface PlayerPosition  { id, x, y }  — One position the law cares about. Deliberately not an entity: the law is about coordinates.
- `src/sports/soccer/offside.ts`  function attackDepth  (x, attackingSide) => number  — How far towards the goal a side is attacking a point is. The whole law is comparisons on this
- `src/sports/soccer/offside.ts`  function isOffsideExempt  (kind) => boolean
- `src/sports/soccer/offside.ts`  function offsideLine  (defenders, attackingSide) => number  — The offside line, as an attacking depth: the second-last defender, or the halfway line, whichever
- `src/sports/soccer/offside.ts`  function isInOffsidePosition  (x, attackingSide, line, ballX) => boolean  — Whether a point is in an offside position, given the line and where the ball was played from.
- `src/sports/soccer/offside.ts`  interface OffsideSnapshot  { side, passer, line, ballX, flagged, exempt }  — The picture at the moment the ball was played, and the only thing `judgeOffside` is allowed to
- `src/sports/soccer/offside.ts`  function captureOffside  (side, passer, ballX, attackers, defenders, restartKind?) => OffsideSnapshot  — Freezes the picture as the ball is played.
- `src/sports/soccer/offside.ts`  function judgeOffside  (snapshot, receiver) => boolean  — Whether the player who just touched the ball was offside when it was played.
- `src/sports/soccer/offside.ts`  function offenceSpot  (snapshot, receiver) => PlayerPosition | undefined  — Where a flagged player was standing when the ball was played — where the free kick is taken.
- `src/sports/soccer/offside.ts`  interface OffsideOffence  { restart, events }  — The offence: an indirect free kick to the defending side, from where the offside player was
- `src/sports/soccer/offside.ts`  function offsideOffence  (snapshot, receiver, step) => OffsideOffence | null

- `src/sports/soccer/passing.ts`  type PassKind  = 'short' | 'through' | 'lofted' | 'cross'  — The four passes `06` §3.2 asks for.
- `src/sports/soccer/passing.ts`  interface PasserRatings  { shortPass, longPass, crossing }  — What a passer brings.
- `src/sports/soccer/passing.ts`  interface PassProfile  { rating, grounded, arrivalSpeed, maxSpeed, hang, releaseHeight, arrivalHeight, baseError, errorFreeMetres, errorPerMetre, …+2 }
- `src/sports/soccer/passing.ts`  const PASS_PROFILES  : Readonly<Record<PassKind, PassProfile>>  — The four passes, as numbers.
- `src/sports/soccer/passing.ts`  interface PassInFlight  { kind, passer, side, target, releaseStep, expireStep, toX, toY, arrivalSpeed, offside, …+1 }  — A pass in the air (or on the grass), and what it needs to be judged on arrival.
- `src/sports/soccer/passing.ts`  function groundReleaseSpeed  (profile, distance, weight?) => number  — The release speed needed to arrive at a given speed, `d` metres away.
- `src/sports/soccer/passing.ts`  function groundArrivalSpeed  (releaseSpeed, distance) => number  — What a ground pass will actually be doing when it reaches the target. `0` if it never does.
- `src/sports/soccer/passing.ts`  function groundFlightTime  (releaseSpeed, distance) => number  — How long a ground pass takes to cover the distance.
- `src/sports/soccer/passing.ts`  function airFlightTime  (profile, distance) => number  — Flight time for an aerial pass: a flat ball's, stretched by the profile's hang.
- `src/sports/soccer/passing.ts`  function passError  (profile, ratings, distance, pressure) => { angle: number; weight: number }  — The error a passer puts on the ball: angular, and on the weight.
- `src/sports/soccer/passing.ts`  function selectPassTarget  (world, from, aimX, aimY, candidates, assist?) => EntityId  — Pass assist (`06` §2): the teammate closest to where the player is aiming.
- `src/sports/soccer/passing.ts`  function leadTarget  (world, from, receiver, kind, runOn?) => { x: number; y: number; flightTime: number }  — Where to play the ball so it and a moving receiver arrive together.
- `src/sports/soccer/passing.ts`  interface PassAttempt  { kind, passer, side, target, toX, toY, ratings, pressure, curve, power }  — Everything a pass attempt needs beyond the world.
- `src/sports/soccer/passing.ts`  interface PassContext  { attackers, defenders, restartKind }  — The squad picture, for the offside snapshot taken at release.
- `src/sports/soccer/passing.ts`  function throwPass  (world, ball, attempt, step, rng, context?) => PassInFlight  — Strikes the pass: applies the error, releases the ball, and freezes the offside picture.
- `src/sports/soccer/passing.ts`  function isLive  (pass, step) => boolean  — Whether a pass is still a pass, or has become a loose ball.
- `src/sports/soccer/passing.ts`  function markContested  (pass, defender) => boolean  — Records that a defender has had their one read at this pass.

- `src/sports/soccer/pitch-render.ts`  type Theme  = 'dark' | 'light'
- `src/sports/soccer/pitch-render.ts`  interface PitchPalette  { grass, stripe, line, net }
- `src/sports/soccer/pitch-render.ts`  function paletteFor  (theme) => PitchPalette
- `src/sports/soccer/pitch-render.ts`  function drawPitch  (ctx, field, theme?) => void  — `theme` defaults dark for the same reason `drawCourt`'s does: the current call site is
- `src/sports/soccer/pitch-render.ts`  function pitchKey  (field, view) => string  — Cache key for the static layer, so a theme or a resize redraws it and nothing else does.

- `src/sports/soccer/pitch.ts`  type Side  = 0 | 1  — Which side of the pitch a team defends.
- `src/sports/soccer/pitch.ts`  const PITCH  — The pitch, in metres. Every literal in the soccer module traces back to one of these.
- `src/sports/soccer/pitch.ts`  const CENTRE_Y  — Half the pitch's width — the halfway line's y, and the axis both goals sit on.
- `src/sports/soccer/pitch.ts`  const CENTRE_X  — The halfway line.
- `src/sports/soccer/pitch.ts`  const soccerPitch  : FieldGeometry
- `src/sports/soccer/pitch.ts`  function defendedGoal  (side) => Goal  — The goal `side` defends.
- `src/sports/soccer/pitch.ts`  function attackedGoal  (side) => Goal  — The goal `side` attacks — the one it scores in.
- `src/sports/soccer/pitch.ts`  function attackDirection  (side) => 1 | -1  — Sign of the direction `side` attacks in: `+1` towards high x, `-1` towards low x.
- `src/sports/soccer/pitch.ts`  function defendedGoalLineX  (side) => number  — x of the goal line `side` defends.
- `src/sports/soccer/pitch.ts`  function shotDistance  (x, y, side) => number  — Floor distance from a point to the centre of the goal `side` attacks.
- `src/sports/soccer/pitch.ts`  function goalAngle  (x, y, side) => number  — The angle, in radians, that the goal mouth subtends from a point — how much goal the shooter can
- `src/sports/soccer/pitch.ts`  function isGoal  (x, y, z, defendingSide) => boolean  — Whether a ball at this position has scored against `side` — over its goal line, between the
- `src/sports/soccer/pitch.ts`  function isInDefendedPenaltyArea  (x, y, side) => boolean  — Inside the penalty area `side` defends — where a foul by `side` is a penalty.
- `src/sports/soccer/pitch.ts`  function isInAttackingPenaltyArea  (x, y, side) => boolean  — Inside the penalty area `side` attacks — where a shot is "inside the box".
- `src/sports/soccer/pitch.ts`  function isInDefendedGoalArea  (x, y, side) => boolean  — Inside the six-yard box `side` defends — where a goal kick is taken from.
- `src/sports/soccer/pitch.ts`  function isInPenaltyArc  (x, y, defendingSide) => boolean  — Inside the arc struck from the penalty spot — the "D".
- `src/sports/soccer/pitch.ts`  function isInCentreCircle  (x, y) => boolean  — Inside the centre circle.
- `src/sports/soccer/pitch.ts`  function isInAttackingHalf  (x, y, side) => boolean  — Whether a point is in the half `side` attacks. The halfway line belongs to neither half for
- `src/sports/soccer/pitch.ts`  type Third  = 'defensive' | 'middle' | 'attacking'  — Which third of the pitch a point is in, named from `side`'s point of view.
- `src/sports/soccer/pitch.ts`  function thirdFor  (x, side) => Third
- `src/sports/soccer/pitch.ts`  function goalOpenness  (x, y, side) => number  — How much of the goal is available compared with the best a shooter could do from that far out:
- `src/sports/soccer/pitch.ts`  type ShotZone  = 'sixYard' | 'penaltyArea' | 'edgeOfBox' | 'wide' | 'longRange' …  — Shot zones, for the box score and for CPU shot selection.
- `src/sports/soccer/pitch.ts`  function shotZone  (x, y, side) => ShotZone  — Classifies a shot location. Distance alone would call a shot from the by-line a good one, so
- `src/sports/soccer/pitch.ts`  function isInBounds  (x, y, margin?) => boolean  — Inside the lines. `margin` shrinks the legal area, for an entity with a radius.
- `src/sports/soccer/pitch.ts`  type Boundary  = 'goalLine0' | 'goalLine1' | 'touchlineLow' | 'touchlineHigh'  — Which boundary a point is past, or `null` if it is on the pitch.
- `src/sports/soccer/pitch.ts`  function crossedBoundary  (x, y) => Boundary | null  — The boundary a ball most escaped through. Deepest overshoot wins, so a ball that clears the
- `src/sports/soccer/pitch.ts`  interface Spot  { x, y }
- `src/sports/soccer/pitch.ts`  function throwInSpot  (x, y) => Spot  — Where a throw-in is taken: the point on the touchline the ball left through, kept out of the
- `src/sports/soccer/pitch.ts`  function cornerSpot  (defendingSide, y) => Spot  — The corner the attacking side takes from, given where the ball crossed the defending side's goal
- `src/sports/soccer/pitch.ts`  function goalKickSpot  (defendingSide, y) => Spot  — Where a goal kick is taken. The law allows anywhere in the goal area; the keeper takes it from
- `src/sports/soccer/pitch.ts`  function penaltySpot  (defendingSide) => Spot  — The penalty spot in front of the goal `defendingSide` defends.
- `src/sports/soccer/pitch.ts`  function kickOffSpot  () => Spot  — The centre spot, where each half and each restart after a goal begins.
- `src/sports/soccer/pitch.ts`  function mirrorX  (x) => number  — Mirrors a point to the other end of the pitch. Formations and set-piece placements are authored

## src/sports/soccer/playbook/

- `src/sports/soccer/playbook/cpu.ts`  const SOCCER_READ_WINDOW  — How many recent turns the read covers.
- `src/sports/soccer/playbook/cpu.ts`  const PRESS_COUNTERS  : Readonly<Record<string, Readonly<Record<string, number>>>>  — The one relationship the effect figures cannot express: tempo against press line.
- `src/sports/soccer/playbook/cpu.ts`  function tempoCounter  (tempo, press) => number  — The mirror of it, for the attacking side choosing a tempo against a press it has been shown.
- `src/sports/soccer/playbook/cpu.ts`  interface IntentRead  { option, times, share }  — What one side has been setting on one dimension lately, most-used first.
- `src/sports/soccer/playbook/cpu.ts`  function readIntents  (turns, side, dimension, role, window?) => IntentRead[]  — What the opponent has been setting, per dimension. Reads only committed turns — exactly the
- `src/sports/soccer/playbook/cpu.ts`  function phaseValue  (effect, phase) => number  — The shift in the odds this option is worth, in the phase the turn is actually being played in.
- `src/sports/soccer/playbook/cpu.ts`  function clockValue  (effect, state, side) => number  — What stretching the turn is worth, given the scoreboard.
- `src/sports/soccer/playbook/cpu.ts`  function squadFit  (players, keys) => number  — How suited this squad is to what an option asks for, in the same units as an effect.
- `src/sports/soccer/playbook/cpu.ts`  function scoreOption  (option, state, side, role, phase) => number  — Everything an option is worth to this side, this turn, before the read.
- `src/sports/soccer/playbook/cpu.ts`  function readValue  (option, turns, opponent, role) => number  — What the read adds to an option.
- `src/sports/soccer/playbook/cpu.ts`  function temperatureFor  (state) => number  — Temperature for a difficulty. Higher noise, wider sampling, worse decisions.
- `src/sports/soccer/playbook/cpu.ts`  function scoreDimension  (dimension, state, side, role, phase, turns) => { id: CallId; score: number }[]  — Every option on one dimension, scored. Exported because it is what the tests read.
- `src/sports/soccer/playbook/cpu.ts`  function markTarget  (state, side, role, turns, window?) => EntityId | undefined  — Who to follow, when the CPU's focus lands on an athlete.
- `src/sports/soccer/playbook/cpu.ts`  function cpuCall  (state, side, rng, turns?) => PlaybookCall  — The CPU's call: a value on each of the four dimensions it is asked about, scored, read, and

- `src/sports/soccer/playbook/diagram.ts`  const GOAL  : DiagramPoint  — The goal being attacked. The `x` is inside the net, so a scored shot's line finishes past it.
- `src/sports/soccer/playbook/diagram.ts`  const SECONDS  : Readonly<Record<string, number>>  — `09` §2.1 — 4–8 seconds of resolution. A goal is given the longest look and a lost ball the
- `src/sports/soccer/playbook/diagram.ts`  const BLOCK  — How far the block follows the ball.
- `src/sports/soccer/playbook/diagram.ts`  function rolePoint  (roleId) => DiagramPoint  — Where a formation role stands, averaged across every formation that names it.
- `src/sports/soccer/playbook/diagram.ts`  function ballLine  (phase) => number  — Where the ball is for this phase, as a fraction along the attacking direction.
- `src/sports/soccer/playbook/diagram.ts`  function toDiagram  (x, y, attacking) => DiagramPoint  — Pitch metres into the diagram's frame, with the attacking side always going left-to-right.
- `src/sports/soccer/playbook/diagram.ts`  function pushed  (base, line, spread) => DiagramPoint  — Where the phase sends a role that starts at `base`.
- `src/sports/soccer/playbook/diagram.ts`  function buildDiagram  (state, resolution) => TurnDiagram  — The turn, as a timeline.

- `src/sports/soccer/playbook/index.ts`  type SoccerPlaybook  = PlaybookAdapter<SoccerPlaybookState>
- `src/sports/soccer/playbook/index.ts`  const EXTRA_TIME_HALVES  — Halves of extra time a level match plays before it is left level. See `isFinished` below.
- `src/sports/soccer/playbook/index.ts`  const soccerPlaybook  : SoccerPlaybook
- `src/sports/soccer/playbook/index.ts`  function createSoccerPlaybook  (options) => PlaybookMatch<SoccerPlaybookState>  — A soccer Playbook match, ready to take calls. The one door — the turn screen, the hot-seat flow,
- `src/sports/soccer/playbook/index.ts`  re-export DEFAULT_INTENTS  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export INTENT_DIMENSIONS  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export INTENT_OPTIONS  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export callFrom  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export callOptionsFor  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export composeEffect  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export dimensionsFor  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export headlineDimension  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export intentsFrom  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export optionsFor  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export IntentDimension  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export IntentOption  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export SoccerIntents  from './intents.ts'
- `src/sports/soccer/playbook/index.ts`  re-export narrateTurn  from './narration.ts'
- `src/sports/soccer/playbook/index.ts`  re-export PHASE_TURN_SECONDS  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export SOCCER_PHASES  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export nextPhase  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export phaseBallX  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export phaseName  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export phaseThird  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export PHASE_ODDS  from './resolution.ts'
- `src/sports/soccer/playbook/index.ts`  re-export phaseOutcomeOf  from './resolution.ts'
- `src/sports/soccer/playbook/index.ts`  re-export resolvePhaseTurn  from './resolution.ts'
- `src/sports/soccer/playbook/index.ts`  re-export soccerSquad  from './squad.ts'
- `src/sports/soccer/playbook/index.ts`  re-export soccerSquads
- `src/sports/soccer/playbook/index.ts`  re-export buildDiagram  from './diagram.ts'
- `src/sports/soccer/playbook/index.ts`  re-export PRESS_COUNTERS  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export SOCCER_READ_WINDOW  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export cpuCall  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export markTarget  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export readIntents  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export scoreDimension  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export temperatureFor  from './cpu.ts'
- `src/sports/soccer/playbook/index.ts`  re-export SoccerPhase  from './phases.ts'
- `src/sports/soccer/playbook/index.ts`  re-export SoccerPlaybookState  from './resolution.ts'

- `src/sports/soccer/playbook/intents.ts`  const INTENT_DIMENSIONS  — The five axes `09` §2.3 names, in the order the call sheet shows them.
- `src/sports/soccer/playbook/intents.ts`  type IntentDimension  = (typeof INTENT_DIMENSIONS)[number]
- `src/sports/soccer/playbook/intents.ts`  type SoccerIntents  = Readonly<Record<IntentDimension, CallId>>  — A side's full set of intents: one value per dimension.
- `src/sports/soccer/playbook/intents.ts`  interface IntentEffect  { climb, create, setPiece, finish, duration, effort }  — What one intent value is worth, as shifts on the odds the phase model draws from.
- `src/sports/soccer/playbook/intents.ts`  interface IntentOption  { id, dimension, name, blurb, keys, attack, defend, targeted }  — One option on one dimension: what it is called, what it does with the ball, and without it.
- `src/sports/soccer/playbook/intents.ts`  const INTENT_OPTIONS  : readonly IntentOption[]  — The catalogue.
- `src/sports/soccer/playbook/intents.ts`  const DEFAULT_INTENTS  : SoccerIntents  — What a match starts with, and what an unrecognised value falls back to.
- `src/sports/soccer/playbook/intents.ts`  function intentOption  (id) => IntentOption | undefined
- `src/sports/soccer/playbook/intents.ts`  function optionsFor  (dimension) => readonly IntentOption[]  — Every option on one dimension, in catalogue order — one row of the call sheet.
- `src/sports/soccer/playbook/intents.ts`  function dimensionsFor  (role) => readonly IntentDimension[]  — The dimensions a side is actually asked about this turn.
- `src/sports/soccer/playbook/intents.ts`  function callOptionsFor  (role) => readonly CallOption[]  — The catalogue as the seam's `CallOption`s, for whichever role this side is in.
- `src/sports/soccer/playbook/intents.ts`  function intentsFrom  (previous, call) => SoccerIntents  — The intents a call actually carries, given what this side had set before it.
- `src/sports/soccer/playbook/intents.ts`  function headlineDimension  (role) => IntentDimension  — The dimension whose value `PlaybookCall.call` carries, per role.
- `src/sports/soccer/playbook/intents.ts`  function callFrom  (side, role, intents, target?) => PlaybookCall  — A `PlaybookCall` carrying a full set of intents, with the right headline for the role.
- `src/sports/soccer/playbook/intents.ts`  function composeEffect  (intents, role) => IntentEffect  — The composed shift this side's intents apply, in the role they are in.

- `src/sports/soccer/playbook/key-moments.ts`  const MOMENT_GAMES  — `09` §2.4's soccer row, against `09` §3.2's soccer set.
- `src/sports/soccer/playbook/key-moments.ts`  type SoccerMoment  = keyof typeof MOMENT_GAMES
- `src/sports/soccer/playbook/key-moments.ts`  function leverageFor  (state, base) => number  — How much this moment matters, `0–1`. Late and close lifts every moment towards its ceiling; early
- `src/sports/soccer/playbook/key-moments.ts`  function detectKeyMoment  (state, resolution) => ArcadeInvocation | null  — The moment worth playing, or `null`.
- `src/sports/soccer/playbook/key-moments.ts`  function applyKeyMomentOutcome  (state, resolution, outcome) => TurnResolution  — The player's result, folded back in.

- `src/sports/soccer/playbook/model.ts`  interface PassPlan  { kind, distance, count }  — The sequence of passes a phase is carried by.
- `src/sports/soccer/playbook/model.ts`  const PASS_MODEL  — Tuning for the passing half. Distances come from the pitch; only the risk figures are here.
- `src/sports/soccer/playbook/model.ts`  function passPlanFor  (phase, tempo, width, span) => PassPlan  — How a tempo carries the ball through a phase.
- `src/sports/soccer/playbook/model.ts`  function passCompletion  (plan, ratings, pressure) => number  — The chance one pass of this plan finds its man: Live's own `passError`, read as geometry.
- `src/sports/soccer/playbook/model.ts`  function interceptChance  (plan, pressure, edge) => number  — The chance one pass of this plan is cut out. Distance and pressure raise it; class lowers it.
- `src/sports/soccer/playbook/model.ts`  function sequenceSuccess  (plan, ratings, pressure, edge) => number  — The chance the whole sequence comes off: every pass found its man and none was cut out.
- `src/sports/soccer/playbook/model.ts`  interface ShotSetup  { x, y, distance, openness, power, pressure }  — Where the phase's shot is taken from, and how it is struck.
- `src/sports/soccer/playbook/model.ts`  const SHOT_MODEL
- `src/sports/soccer/playbook/model.ts`  function shotSetupFor  (phase, side, ratings, channel, pressure, rng) => ShotSetup  — Where the shot comes from, given the phase and where the attack was focused.
- `src/sports/soccer/playbook/model.ts`  type ShotResult  = 'goal' | 'saved' | 'parried' | 'off-target' | 'blocked'  — What a Playbook shot came to. `blocked` never reaches the keeper; the rest are their business.
- `src/sports/soccer/playbook/model.ts`  interface ShotInput  { setup, shooter, keeper, defenderMarking, side }  — The shot, put through Live's own models.
- `src/sports/soccer/playbook/model.ts`  function expectedGoalChance  (input) => number  — What this shot was worth before anybody drew for it — soccer's xG, from the same three stages the
- `src/sports/soccer/playbook/model.ts`  function resolveShot  (input) => { readonly result: ShotResult; readonly onTarget: boolean; read…  — One attempt, put through Live's own models.
- `src/sports/soccer/playbook/model.ts`  interface Attempt  { result, onTarget, expected }  — One attempt and what it came to, for the box score and T-6.21's narration.
- `src/sports/soccer/playbook/model.ts`  function resolvePressure  (input) => { readonly result: ShotResult; readonly attempts: readonly Atte…  — A phase of pressure: several attempts, ending at the first one that goes in.
- `src/sports/soccer/playbook/model.ts`  function attemptCount  (phase, rng) => number  — How many goes a phase of pressure gets.
- `src/sports/soccer/playbook/model.ts`  const MODEL_CALIBRATION  — What the model has to be shifted by to land on the turn budget `09` §2.3 asks for.
- `src/sports/soccer/playbook/model.ts`  function erf  (x) => number  — The error function, Abramowitz & Stegun 7.1.26 — max absolute error 1.5 × 10⁻⁷.
- `src/sports/soccer/playbook/model.ts`  function spanOf  (phase, side) => number  — The pitch distance a phase covers, for the pass plan.
- `src/sports/soccer/playbook/model.ts`  re-export PHASE_TURN_SECONDS
- `src/sports/soccer/playbook/model.ts`  re-export KEEPER
- `src/sports/soccer/playbook/model.ts`  re-export SHOOTING

- `src/sports/soccer/playbook/narration.ts`  function templatesFor  (outcome, phase) => readonly string[]  — The template set for a turn: the phase-specific list where one exists, the outcome's own list
- `src/sports/soccer/playbook/narration.ts`  function narrateTurn  (state, resolution) => NarrationLine
- `src/sports/soccer/playbook/narration.ts`  function narrateExpectation  (resolution) => string | null  — A second line, for the turn screen's detail row: what the turn was worth, when that is a number
- `src/sports/soccer/playbook/narration.ts`  const NARRATED_OUTCOMES  : readonly string[]  — Every outcome the resolution model produces has a line, and the test asserts exactly that.
- `src/sports/soccer/playbook/narration.ts`  re-export TurnFacts

- `src/sports/soccer/playbook/phases.ts`  const SOCCER_PHASES  — The five phases of `09` §2.3, in ladder order. A possession climbs; losing it drops the ball to
- `src/sports/soccer/playbook/phases.ts`  type SoccerPhase  = (typeof SOCCER_PHASES)[number]
- `src/sports/soccer/playbook/phases.ts`  const OPENING_PHASE  : SoccerPhase  — The phase every kick-off, goal kick, and second half starts from.
- `src/sports/soccer/playbook/phases.ts`  const PHASE_TURN_SECONDS  : Readonly<Record<SoccerPhase, number>>  — Game seconds a turn in each phase is worth.
- `src/sports/soccer/playbook/phases.ts`  const PHASE_THIRD  : Readonly<Record<SoccerPhase, Third>>  — Which third of the pitch the phase happens in, from the attacking side's point of view.
- `src/sports/soccer/playbook/phases.ts`  function phaseBallX  (phase, side) => number
- `src/sports/soccer/playbook/phases.ts`  function phaseThird  (phase, side) => Third  — Where the phase leaves the ball, as a third — the reverse check on `phaseBallX`.
- `src/sports/soccer/playbook/phases.ts`  function phaseName  (phase, attacking) => string  — What the phase is called, from one side's point of view. `09` §2.3's "defensive equivalents",
- `src/sports/soccer/playbook/phases.ts`  const PHASE_OUTCOMES  — Every way a phase turn can end. The graph below maps each to what happens next.
- `src/sports/soccer/playbook/phases.ts`  type PhaseOutcome  = (typeof PHASE_OUTCOMES)[number]
- `src/sports/soccer/playbook/phases.ts`  interface PhaseTransition  { phase, retains }  — Where the ball is after a turn: the next phase and whether the same side still has it.
- `src/sports/soccer/playbook/phases.ts`  function nextPhase  (phase, outcome) => PhaseTransition  — The transition graph. Every `(phase, outcome)` pair the resolution can produce has exactly one
- `src/sports/soccer/playbook/phases.ts`  function outcomesFor  (phase) => readonly PhaseOutcome[]  — Which outcomes are reachable from a phase, for the tests and for T-6.22's CPU.
- `src/sports/soccer/playbook/phases.ts`  function isShootingPhase  (phase) => boolean  — True when the phase is one a shot comes out of — what T-6.22's key moments hang off.

- `src/sports/soccer/playbook/resolution.ts`  interface SoccerPlaybookState  { phase, period, intent, shots }  — What soccer tracks between turns. The engine owns the clock, the score, and possession.
- `src/sports/soccer/playbook/resolution.ts`  const TURN_OUTCOMES  — The one thing the player says about a turn beyond the intent, and what narration reads.
- `src/sports/soccer/playbook/resolution.ts`  type TurnOutcome  = (typeof TURN_OUTCOMES)[number]
- `src/sports/soccer/playbook/resolution.ts`  function phaseOutcomeOf  (outcome) => PhaseOutcome  — The graph's view of a turn outcome. Total: an unknown string is a loss of possession.
- `src/sports/soccer/playbook/resolution.ts`  const PHASE_ODDS  — What is left of the odds table after T-6.20.
- `src/sports/soccer/playbook/resolution.ts`  const SOCCER_RESOLUTION  — Tuning that is about the *matchup* rather than the phase.
- `src/sports/soccer/playbook/resolution.ts`  const PRESSURE  — How closed down the ball is, as Live's models take it.
- `src/sports/soccer/playbook/resolution.ts`  function keyRating  (athlete, keys) => number  — One athlete's average across a phase's key ratings. Missing keys read as an average athlete.
- `src/sports/soccer/playbook/resolution.ts`  interface FocusBias  { channel, athlete }  — Where the focus intent points, once the id has been read (T-6.19).
- `src/sports/soccer/playbook/resolution.ts`  const NO_FOCUS  : FocusBias
- `src/sports/soccer/playbook/resolution.ts`  const FOCUS_PULL  — How hard the focus intent pulls, in rating points on the selection score.
- `src/sports/soccer/playbook/resolution.ts`  function primaryFor  (squad, keys, rng, own?, opposing?) => PlaybookAthlete  — Who the turn is about: the outfielder best suited to the phase, with a small seeded wobble so a
- `src/sports/soccer/playbook/resolution.ts`  function focusBias  (intents, target?) => FocusBias  — What the focus dimension of a set of intents points at.
- `src/sports/soccer/playbook/resolution.ts`  function matchupEdge  (attack, defend, stamina) => number  — How far one side is ahead in this phase, in logistic-ish units, clamped.
- `src/sports/soccer/playbook/resolution.ts`  interface ResolveInput  { state, calls, rng, phase }
- `src/sports/soccer/playbook/resolution.ts`  function resolvePhaseTurn  (input) => TurnResolution  — Resolves one phase turn.
- `src/sports/soccer/playbook/resolution.ts`  function passerRatings  (athlete) => PasserRatings  — Live's `PasserRatings`, off a Playbook athlete.
- `src/sports/soccer/playbook/resolution.ts`  function shooterRatings  (athlete) => ShooterRatings  — Live's `ShooterRatings`, off a Playbook athlete.
- `src/sports/soccer/playbook/resolution.ts`  function intentsOf  (state, side, call) => SoccerIntents  — The intents a side is playing this turn: what they had set, overlaid with what this call says.
- `src/sports/soccer/playbook/resolution.ts`  function drainStamina  (players, effort) => void  — Stamina, spent and recovered. Both sides work — a high press costs the pressing side more than a
- `src/sports/soccer/playbook/resolution.ts`  const SOCCER_STAMINA
- `src/sports/soccer/playbook/resolution.ts`  function createSoccerPlaybookState  () => SoccerPlaybookState  — The state a match starts from. Exported so the tests do not have to build one by hand.

- `src/sports/soccer/playbook/squad.ts`  function soccerPlaybookRatings  (athlete) => DerivedRatings  — Playbook reads ratings by name off a flat record; `SoccerRatings` already is one.
- `src/sports/soccer/playbook/squad.ts`  function soccerSquad  (athletes, side, formationId?) => PlaybookSquad  — Builds one side's eleven. Entity ids follow Live's convention — side 0 takes `0…n-1`, side 1 takes
- `src/sports/soccer/playbook/squad.ts`  function soccerSquads  (home, away, formationId?) => readonly [PlaybookSquad, PlaybookSquad]
- `src/sports/soccer/playbook/squad.ts`  function keeperOf  (squad) => PlaybookAthlete  — The goalkeeper: the formation's first role, as Live spawns it.
- `src/sports/soccer/playbook/squad.ts`  function outfieldOf  (squad) => readonly PlaybookAthlete[]  — Everybody but the keeper — who a phase turn can actually be about.
- `src/sports/soccer/playbook/squad.ts`  type Channel  = 'left' | 'centre' | 'right'  — Which side of the pitch a role lives on — what T-6.19's focus intent steers play towards.
- `src/sports/soccer/playbook/squad.ts`  function channelOf  (roleId) => Channel  — The channel a formation role belongs to, from the role's own `y` across every formation that

- `src/sports/soccer/playbook/turn-facts.ts`  interface ShotFact  { x, y, made, onTarget, distance, chance }  — One attempt at goal, in the terms the diagram draws and narration mentions.
- `src/sports/soccer/playbook/turn-facts.ts`  interface PassFact  { kind, count }  — The ball being moved: what kind of pass, and how many of them.
- `src/sports/soccer/playbook/turn-facts.ts`  interface TurnFacts  { phase, marked, tempo, width, press, pass, shots, corner }
- `src/sports/soccer/playbook/turn-facts.ts`  function turnFacts  (resolution) => TurnFacts  — Reads one resolved turn. Total: a turn whose events a future change reshapes still produces a
- `src/sports/soccer/playbook/turn-facts.ts`  function keyShot  (facts) => ShotFact | null  — The attempt worth talking about: the one that went in, or the best one that did not.

## src/sports/soccer/

- `src/sports/soccer/roster.ts`  const SOCCER_TABLES
- `src/sports/soccer/roster.ts`  const SOCCER_SPORT_ID
- `src/sports/soccer/roster.ts`  interface SoccerRatings  { finishing, shotPower, shortPass, longPass, crossing, dribbling, heading, tackling, marking, offBall, …+4 }  — Everything soccer's models read off one athlete.
- `src/sports/soccer/roster.ts`  function soccerRatings  (athlete) => SoccerRatings  — One athlete's soccer numbers, with fatigue applied.
- `src/sports/soccer/roster.ts`  function soccerMovement  (athlete) => MovementProfile  — How this athlete moves on a pitch. `pace` is a *derived* rating, so it already folds speed and
- `src/sports/soccer/roster.ts`  function soccerCoupling  (athlete) => Coupling
- `src/sports/soccer/roster.ts`  interface RosterEntry  { athleteId, ratings, movement, coupling }
- `src/sports/soccer/roster.ts`  function rosterEntry  (athlete) => RosterEntry
- `src/sports/soccer/roster.ts`  function rollRatings  (rng, roleIndex) => SoccerRatings  — A seeded stand-in, for a match started with no roster.

- `src/sports/soccer/rules.ts`  const TIMING  — Every duration soccer cares about, in the units the player reads them in.
- `src/sports/soccer/rules.ts`  const CLOCK_COMPRESSION  — Game seconds per real second. Derived, never authored — the two half figures define it.
- `src/sports/soccer/rules.ts`  function gameSecondsToSteps  (gameSeconds) => number  — Game seconds → simulation steps. The single place compression is applied.
- `src/sports/soccer/rules.ts`  function stepsToGameSeconds  (steps) => number  — Simulation steps → the game seconds a HUD should show.
- `src/sports/soccer/rules.ts`  const SOCCER_RULES  : MatchRules
- `src/sports/soccer/rules.ts`  const ADDED_TIME  — How much added time each kind of stoppage buys back, in game seconds, and the ceiling on the
- `src/sports/soccer/rules.ts`  type AddedTimeCause  = 'goal' | 'card' | 'penalty' | 'injury' | 'substitution'  — The stoppages that buy time back. A throw-in is deliberately not one of them.
- `src/sports/soccer/rules.ts`  const RestartKind  — Why play is stopped and how it starts again.
- `src/sports/soccer/rules.ts`  type RestartKindName  = (typeof RestartKind)[keyof typeof RestartKind]
- `src/sports/soccer/rules.ts`  const SoccerEvent  — Sport-specific event names, carried on `EventKind.SPORT` as `sportKind` (INV-9).
- `src/sports/soccer/rules.ts`  interface Restart  { kind, side, x, y, reason }
- `src/sports/soccer/rules.ts`  interface RulesState  { possession, lastTouch, restart, restartReady, restartClock, restartDelay, addedGameSeconds, boardAddedMinutes, kickOffSide, yellowCards, …+3 }  — The whole rule book's state. Deliberately flat and JSON-shaped: it goes into snapshots and
- `src/sports/soccer/rules.ts`  interface AdvantageState  { side, restart, steps }  — A foul the referee has seen but not yet whistled, because the fouled side still has the ball.
- `src/sports/soccer/rules.ts`  function createRulesState  (kickOffSide?) => RulesState
- `src/sports/soccer/rules.ts`  function opponent  (side) => PitchSide  — The other side. Soccer asks this constantly.
- `src/sports/soccer/rules.ts`  function yellowCards  (state, athlete) => number  — Yellows an athlete is carrying. Two is a red, which `fouls.ts` enforces.
- `src/sports/soccer/rules.ts`  function isSentOff  (state, athlete) => boolean
- `src/sports/soccer/rules.ts`  function playersRemaining  (state, squad) => number  — How many of a side's athletes are still on the pitch. Takes the squad rather than a side,
- `src/sports/soccer/rules.ts`  function registerTouch  (state, side) => void  — Records who touched the ball last, which is what decides every out-of-play award.
- `src/sports/soccer/rules.ts`  function grantPossession  (state, side, step, actor?) => SportEvent[]  — Hands the ball to a side with play live.
- `src/sports/soccer/rules.ts`  function restartFor  (x, y, lastTouch) => Restart | null  — What the restart is when the ball leaves the pitch, given who touched it last.
- `src/sports/soccer/rules.ts`  function awardRestart  (state, restart, step, delaySteps?) => SportEvent[]  — Puts a restart on. Play is dead from here until `completeRestart`.
- `src/sports/soccer/rules.ts`  function readyRestart  (state, step) => SportEvent[]  — The taker has the ball at the spot. The count to take it starts now, and not before.
- `src/sports/soccer/rules.ts`  function completeRestart  (state, step) => SportEvent[]  — The restart has been taken; play is live again.
- `src/sports/soccer/rules.ts`  function awardKickOff  (state, side, step, reason, delaySteps?) => SportEvent[]  — The restart every half and every goal begins with.
- `src/sports/soccer/rules.ts`  function startHalf  (state, period, step) => SportEvent[]  — What a period starts with: the ball on the centre spot, and the side that did not kick off the
- `src/sports/soccer/rules.ts`  function onGoalScored  (state, scoringSide, step) => SportEvent[]  — The restart after a goal: the side that conceded kicks off, once the celebration is over.
- `src/sports/soccer/rules.ts`  function accrueAddedTime  (state, cause, step) => SportEvent[]  — Buys time back for a long stoppage, and returns the events plus — when the board figure moves —
- `src/sports/soccer/rules.ts`  function pendingExtensionSteps  (state, alreadyExtended) => number  — Steps the half should be extended by right now, given what the board says and what has already
- `src/sports/soccer/rules.ts`  function tickRestart  (state, step) => SportEvent[]  — Advances the counts that run while play is dead, and returns whatever they caused.
- `src/sports/soccer/rules.ts`  function isBallDead  (state) => boolean  — Whether play is dead — the one check the module's `step()` needs.
- `src/sports/soccer/rules.ts`  function periodGameSeconds  (state, period) => number  — Total game seconds a period runs for, including whatever the board has added.
- `src/sports/soccer/rules.ts`  function elapsedGameSeconds  (stepInPeriod, period) => number  — The clock as soccer shows it: game seconds *elapsed* in this half, counting up, and continuing
- `src/sports/soccer/rules.ts`  function remainingGameSeconds  (state, stepInPeriod, period) => number  — Game seconds left in the period, for the seam's `SportStatus.periodClock`.
- `src/sports/soccer/rules.ts`  function formatClock  (gameSeconds) => string  — `M:SS`, counting up, as every soccer clock in the world does.

- `src/sports/soccer/shooting.ts`  interface ShooterRatings  { finishing, shotPower, coordination }  — What a shooter brings.
- `src/sports/soccer/shooting.ts`  const SHOOTING
- `src/sports/soccer/shooting.ts`  function chargePower  (heldSteps) => number  — Meter fill from how long the button has been held, `0–1`.
- `src/sports/soccer/shooting.ts`  function shotSpeed  (ratings, power) => number  — How fast the ball leaves the boot.
- `src/sports/soccer/shooting.ts`  interface AimPoint  { y, z }  — A point on the goal mouth, in world terms: across the mouth, and above the ground.
- `src/sports/soccer/shooting.ts`  function aimPoint  (placeAcross, placeUp, inset?) => AimPoint  — Where the joystick is pointing, as a point in the goal.
- `src/sports/soccer/shooting.ts`  function placementError  (ratings, distance, power, pressure) => { across: number; up: number }  — Placement error, in metres at the goal mouth.
- `src/sports/soccer/shooting.ts`  function curveFrom  (approachAngle, coordination) => number  — Spin from the angle the shooter is running at relative to where they are shooting.
- `src/sports/soccer/shooting.ts`  interface ShotAttempt  { shooter, side, ratings, power, placeAcross, placeUp, pressure, approachAngle }
- `src/sports/soccer/shooting.ts`  interface ShotInFlight  { shooter, side, releaseStep, aim, speed, spin, distance, openness, deflected }
- `src/sports/soccer/shooting.ts`  function takeShot  (world, ball, attempt, step, rng) => ShotInFlight  — Strikes the shot: resolves power, placement, and curve, and puts the ball in the air.
- `src/sports/soccer/shooting.ts`  function deflectShot  (world, ball, shot, rng) => boolean  — A defender gets something on it.

- `src/sports/soccer/weights.ts`  const SOCCER_WEIGHTS  : RatingWeightTable  — Rows sum to 1.0.
- `src/sports/soccer/weights.ts`  const SOCCER_REFERENCE_HEIGHT_CM  — The height the modifiers are measured from — roughly a senior outfield average.
- `src/sports/soccer/weights.ts`  const SOCCER_PHYSICAL  : PhysicalModifiers  — `05` §3.2 gives no physical modifier table, so these are read off `05` §2.1's prose: "height
- `src/sports/soccer/weights.ts`  const SOCCER_POSITION_WEIGHTS  : PositionWeightTable  — Position → weights over soccer's derived ratings, for overall and position fit (`05` §3.4). Rows
- `src/sports/soccer/weights.ts`  const SOCCER_DEFAULT_POSITION  — The row used for a role id the table does not name — a generic outfielder.

- `src/sports/soccer/xp.ts`  const SOCCER_XP_AWARDS  : XpAwardTable  — Every rating named here exists in `SOCCER_WEIGHTS`; a test asserts it, because a typo would

## src/sports/testsport/

- `src/sports/testsport/index.ts`  interface TestSportState  { ball, ballState, profiles, sides, playerSide, controlled, step, scratch }
- `src/sports/testsport/index.ts`  const testSport  : SportModule<TestSportState>
- `src/sports/testsport/index.ts`  function createTestMatch  (world, seed, playerSide?, squadSize?) => ?  — Builds a match ready to step — used by the determinism tests and the perf harness.

## src/sports/

- `src/sports/types.ts`  type SportId  = string
- `src/sports/types.ts`  interface SportMeta  { displayName, squadSize, periodName }
- `src/sports/types.ts`  interface FieldGeometry  { width, height, zones, goals }  — Field geometry in world units (metres), origin at a corner.
- `src/sports/types.ts`  interface Rect  { x, y, width, height }
- `src/sports/types.ts`  interface Goal  { side, x, y, z, radius }
- `src/sports/types.ts`  type RatingWeightTable  = Readonly<Record<string, Readonly<Record<string, number>>>>  — Attribute → derived rating weights (`05` §3). Rows are per derived rating and sum to 1.0; the
- `src/sports/types.ts`  interface PhysicalModifierTable  { reference, perUnit }  — A body adjustment applied *after* the weighted sum (`05` §2.1, §3.1): height and weight are not
- `src/sports/types.ts`  interface PhysicalModifiers  { heightCm, weightKg }
- `src/sports/types.ts`  type PositionWeightTable  = Readonly<Record<string, Readonly<Record<string, number>>>>  — Position → weights over the sport's *derived ratings*, for overall and position fit (`05` §3.4).
- `src/sports/types.ts`  interface XpRule  { kind, sportKind, when, rating, xp, targetRating, targetXp }  — One "this action trains that sub-skill" rule (`05` §3.3 — "a made three grants three-point XP, a
- `src/sports/types.ts`  type XpAwardTable  = readonly XpRule[]
- `src/sports/types.ts`  interface RoleTable  { roles }  — Positions and their default field placement, as fractions of the field.
- `src/sports/types.ts`  interface MatchSetup  { seed, playerSide, squadSize, rosters, difficulty, assists }
- `src/sports/types.ts`  interface SportState  { sport, ball }  — Whatever the sport needs to track. The engine treats it as opaque.
- `src/sports/types.ts`  interface ActionIntent  { kind, power, targetX, targetY, target }  — An action the player or AI wants to take. Sports define their own verbs.
- `src/sports/types.ts`  interface SportAiAdapter  { options, score }
- `src/sports/types.ts`  interface SportRenderer  { drawField, drawAthletes, drawBall, drawOverlay, fieldKey }
- `src/sports/types.ts`  interface SportStatus  { actionClock, teamFouls, bonus, possession, controlled, stoppage, meter, periodClock, periodElapsed, buttonContext }  — What a sport tells the presentation layer about itself, beyond the score and the period the match
- `src/sports/types.ts`  interface SportHudSpec  { showShotClock, showPossession, clock, foulLabel, buttonLabels }
- `src/sports/types.ts`  interface SportModule  { id, meta, rules, field, ratingWeights, physicalModifiers, positionWeights, xpAwards, roles, createState, …+11 }  — The seam. Everything the rest of the game needs to play a sport it has never heard of.
- `src/sports/types.ts`  class SportRegistry  () => SportRegistry  — Registry of known sports. A map rather than a switch, which is the mechanical form INV-5 takes:

## src/storage/

- `src/storage/app-db.ts`  interface AppDatabase  { db, athletes, teams, migration }
- `src/storage/app-db.ts`  class DatabaseUnavailableError  (outcome) => DatabaseUnavailableError  — A rejected open: the build cannot safely read what is stored (`05` §9 rules 2 and 4).
- `src/storage/app-db.ts`  function ensureStarterRoster  () => Promise<void>  — Puts the starter roster into a fresh install (US-5.6 — "a fresh install contains a small set of
- `src/storage/app-db.ts`  function appDatabase  (onBlocked?) => Promise<AppDatabase>  — The shared handle, opening it if this is the first ask. A failed open is not cached, so a
- `src/storage/app-db.ts`  function closeAppDatabase  () => Promise<void>  — Closes the shared handle. Used by erase-all-data and by tests between cases.

- `src/storage/backup.ts`  const BACKUP_FORMAT_VERSION  — Bumped when the *envelope* changes, independently of the data schema inside it.
- `src/storage/backup.ts`  interface Backup  { formatVersion, schemaVersion, createdAt, app, stores }
- `src/storage/backup.ts`  function exportBackup  (db, options?) => Promise<Backup>  — Everything, in one object. Sized for a personal roster, not a warehouse.
- `src/storage/backup.ts`  function exportRoster  (db, options?) => Promise<Backup>  — Just the roster and the teams that hold it — US-5.8's narrower export.
- `src/storage/backup.ts`  type BackupProblem  = | { readonly kind: 'not-json'; readonly message: string } | { r…
- `src/storage/backup.ts`  interface StoreChange  { store, added, replaced, unchanged, notInBackup }
- `src/storage/backup.ts`  interface BackupPreview  { backup, changes, totalAdded, totalReplaced, totalUnchanged, noOp }
- `src/storage/backup.ts`  function parseBackup  (text) => { backup: Backup } | { problem: BackupProblem }  — Reads a backup file. Every failure is a value, not an exception — this is the one screen in the
- `src/storage/backup.ts`  function previewBackup  (db, backup) => Promise<BackupPreview>  — What restoring this backup would do, per store. Computed from the live database, so the numbers
- `src/storage/backup.ts`  type RestoreMode  = 'merge' | 'replace'  — `merge` leaves records the backup does not mention alone; `replace` clears each store the backup
- `src/storage/backup.ts`  function restoreBackup  (db, backup, mode?) => Promise<BackupPreview>
- `src/storage/backup.ts`  function backupFilename  (now) => string  — A filename that sorts chronologically and says what it is.
- `src/storage/backup.ts`  function serialiseBackup  (backup) => string

- `src/storage/caches.ts`  interface CacheHost  { open, keys, delete }  — The Cache Storage API, narrowed so tests can substitute a fake.
- `src/storage/caches.ts`  function openCache  (kind, host?) => Promise<Cache>  — Opens the cache for a resource class at the current build.
- `src/storage/caches.ts`  function ourCacheNames  (host?) => Promise<string[]>  — Every cache name on this origin that belongs to this app.
- `src/storage/caches.ts`  function deleteStaleCaches  (host?) => Promise<string[]>  — Deletes our caches from previous builds. Run on `activate`, so the new worker starts with only
- `src/storage/caches.ts`  function deleteCachesForBuild  (kinds, host?) => Promise<void>  — Deletes the caches this build created. Used to undo a failed precache install (`11` §5.1).
- `src/storage/caches.ts`  function deleteAllOurCaches  (host?) => Promise<string[]>  — Deletes every cache we own, current build included. This is Repair (`11` §6) — and it must not
- `src/storage/caches.ts`  function missingFromCache  (kind, urls, host?) => Promise<string[]>  — Reports which precached URLs are actually present. This is the read half of the integrity
- `src/storage/caches.ts`  re-export CACHE_KINDS
- `src/storage/caches.ts`  re-export CacheKind

- `src/storage/idb.ts`  interface StoreSpec  { name, keyPath, indexes }
- `src/storage/idb.ts`  const STORES  : readonly StoreSpec[]  — `05` §1. Singleton stores use an explicit key rather than a keyPath.
- `src/storage/idb.ts`  type StoreName  = (typeof STORES)[number]['name']
- `src/storage/idb.ts`  const DB_VERSION  — Bumped only by a schema change, which ships with its migration (`05` §9, T-0.13).
- `src/storage/idb.ts`  function requestToPromise  (request) => Promise<T>  — Promisifies a request, preserving the DOM error rather than flattening it to a string.
- `src/storage/idb.ts`  interface OpenOptions  { onUpgrade, onBlocked, version }
- `src/storage/idb.ts`  function openDatabase  (options?) => Promise<IDBDatabase>  — Opens the namespaced database, creating any missing stores and indexes.
- `src/storage/idb.ts`  function deleteDatabase  () => Promise<void>  — Deletes the namespaced database. Used by erase-all-data, never by Repair (INV-13).
- `src/storage/idb.ts`  class Database  (db) => Database  — A typed handle to the app's database. Holds the connection open for the session, which is what

- `src/storage/migrations.ts`  const CURRENT_SCHEMA_VERSION  — The schema version this build understands. Bumped only alongside a migration.
- `src/storage/migrations.ts`  interface MigrationContext  { db, from, to }
- `src/storage/migrations.ts`  interface Migration  { to, description, run }
- `src/storage/migrations.ts`  const MIGRATIONS  : readonly Migration[]  — The chain, in order. Version 1 is the initial schema, so it has no migration — the first entry
- `src/storage/migrations.ts`  interface Snapshot  { schemaVersion, takenAt, stores }  — A full copy of every store, used as the pre-migration snapshot (`05` §9 rule 2).
- `src/storage/migrations.ts`  interface MetaRecord  { schemaVersion }
- `src/storage/migrations.ts`  function readSchemaVersion  (db) => Promise<number>
- `src/storage/migrations.ts`  function writeSchemaVersion  (db, version) => Promise<void>
- `src/storage/migrations.ts`  function takeSnapshot  (db, now?) => Promise<Snapshot>  — Reads every store into memory. Sized for a personal roster, not a warehouse.
- `src/storage/migrations.ts`  function restoreSnapshot  (db, snapshot) => Promise<void>  — Restores a snapshot, replacing current contents store by store.
- `src/storage/migrations.ts`  type MigrationOutcome  = | { readonly status: 'up-to-date'; readonly version: number } |…
- `src/storage/migrations.ts`  interface RunOptions  { migrations, target, now }
- `src/storage/migrations.ts`  function runMigrations  (db, options?) => Promise<MigrationOutcome>  — Runs the chain from the stored version to the target. Pure per step, idempotent, and rolled
- `src/storage/migrations.ts`  function describeOutcome  (outcome) => string  — Plain-language copy for each outcome (`10` §10 — migration in progress, corrupt save).

- `src/storage/persistence.ts`  type Milestone  = 'first-write' | 'first-athlete' | 'first-pack' | 'first-match'  — Milestones worth re-asking after — the browser weighs engagement (`11` §7).
- `src/storage/persistence.ts`  type PersistenceState  = 'granted' | 'denied' | 'unsupported'
- `src/storage/persistence.ts`  interface StorageUsage  { usageBytes, quotaBytes, fraction }
- `src/storage/persistence.ts`  const QUOTA_WARNING_FRACTION  — `11` §7 — above this, surface a warning with a "manage data" action.
- `src/storage/persistence.ts`  interface StorageManagerLike  { persist, persisted, estimate }  — The slice of `navigator.storage` we use. Narrowed so tests can substitute a fake.
- `src/storage/persistence.ts`  function persistenceState  (manager?) => Promise<PersistenceState>  — Reports the current grant without asking for it.
- `src/storage/persistence.ts`  function requestPersistence  (manager?) => Promise<PersistenceState>  — Requests persistence. Returns the resulting state rather than throwing — a refusal is an
- `src/storage/persistence.ts`  function storageUsage  (manager?) => Promise<StorageUsage | null>
- `src/storage/persistence.ts`  function isQuotaPressured  (usage) => boolean
- `src/storage/persistence.ts`  function formatBytes  (bytes) => string  — Human-readable bytes. Binary units, because that is what quota is reported in.
- `src/storage/persistence.ts`  function describePersistence  (state) => string  — The plain-language line Settings shows (`10` §9 — never blames the player).
- `src/storage/persistence.ts`  class PersistenceNudger  (manager?) => PersistenceNudger  — Tracks which milestones have already prompted, so the app asks at useful moments and never

- `src/storage/prefs.ts`  interface KeyValueStore  { getItem, setItem, removeItem, key, length }  — The storage API we depend on. Narrowed so tests and private-mode fallbacks can substitute.
- `src/storage/prefs.ts`  function memoryStore  () => KeyValueStore  — In-memory stand-in for Safari private mode, where `setItem` throws on every write.
- `src/storage/prefs.ts`  class Prefs  (store?) => Prefs
- `src/storage/prefs.ts`  const prefs  — The app-wide instance. Tests construct their own against a fake store.

- `src/storage/scope.ts`  const SCOPE  : string  — The deployed base path — the repository directory, resolved at build (`tools/base-path.ts`).
- `src/storage/scope.ts`  const NS  — The prefix every name carries. Distinct per repository, so two of our apps never collide.
- `src/storage/scope.ts`  const BUILD  — The build identifier, injected by Vite. Suffixes cache names so activation can clean up.
- `src/storage/scope.ts`  function dbName  () => string  — The single IndexedDB database. Stores are listed in `05` §1.
- `src/storage/scope.ts`  function cacheName  (kind) => string  — A cache name for a resource class. The build hash is part of the name, so a new build gets
- `src/storage/scope.ts`  function lsKey  (key) => string  — A localStorage key.
- `src/storage/scope.ts`  function isOurs  (name) => boolean  — True when a name belongs to this app — the test every cleanup routine gates on.
- `src/storage/scope.ts`  function isStaleCache  (name) => boolean  — True when a cache belongs to this app but to an *older* build. Activation deletes exactly
- `src/storage/scope.ts`  function parseCacheName  (name) => { kind: string; build: string } | null  — Parses a cache name back into its parts. Returns `null` for anything not ours.
- `src/storage/scope.ts`  const CACHE_KINDS  — The resource classes from `11` §2. `version.json` deliberately has no cache.
- `src/storage/scope.ts`  type CacheKind  = (typeof CACHE_KINDS)[keyof typeof CACHE_KINDS]

## src/teams/

- `src/teams/lineup.ts`  interface LineupSlot  { id, name, x, y }  — A position an athlete can be slotted into, from the sport's own `RoleTable`.
- `src/teams/lineup.ts`  interface SlotAssessment  { slot, athlete, rating, fit, warn, unavailable }
- `src/teams/lineup.ts`  function assess  (athlete, sport, slotId, tables) => { rating: number; fit: number; warn: boolean }  — What this athlete is worth in this specific slot, and how well it suits them.
- `src/teams/lineup.ts`  function assessSquad  (options) => SlotAssessment[]  — Every slot in the formation, with whoever is in it and how well they fit (US-6.2).
- `src/teams/lineup.ts`  function lineupStrength  (assessments) => number  — Team strength: the mean of the filled slots' ratings. Empty slots count as zero, not as absent.
- `src/teams/lineup.ts`  interface AutoFillOptions  { slots, candidates, sport, tables, now, skipUnavailable, benchSize }
- `src/teams/lineup.ts`  function autoFill  (options) => { starters: Record<string, string>; bench: string[]; }  — The strongest legal lineup (US-6.2 — "auto-fill best").
- `src/teams/lineup.ts`  function place  (squad, slotId, athleteId, now) => Squad  — Swaps two slots, or moves an athlete into an empty one. The editor's only mutation.
- `src/teams/lineup.ts`  function lineupStatus  (assessments, squad, squadSize) => { ready: boolean; message: string }  — Whether this squad can start a match, and what to say if not (`10` §10).

- `src/teams/repository.ts`  class TeamRepository  (db) => TeamRepository

- `src/teams/types.ts`  const CREST_IDS  — Generic built-in crests (US-6.1 — "a crest picked from generic built-ins"). Shapes, not badges:
- `src/teams/types.ts`  type CrestId  = (typeof CREST_IDS)[number]
- `src/teams/types.ts`  interface TeamColours  { primary, secondary }
- `src/teams/types.ts`  interface Team  { id, schemaVersion, name, shortName, colours, crestId, createdAt, editable }
- `src/teams/types.ts`  interface Squad  { id, teamId, sportId, starters, bench, formationId, updatedAt }  — One team's lineup in one sport. Keyed `teamId:sportId` (`05` §1), and that key is built in
- `src/teams/types.ts`  const TEAM_BOUNDS
- `src/teams/types.ts`  function squadKey  (teamId, sportId) => string
- `src/teams/types.ts`  function isCrestId  (value) => value is CrestId
- `src/teams/types.ts`  const TEAM_PALETTES  : readonly { readonly id: string; readonly name: string; readonly…  — Colourblind-safe team palettes (`10` §11, US-13.2). Each pair is distinguishable under
- `src/teams/types.ts`  function squadMembers  (squad) => string[]  — Every athlete named in a squad, starters then bench, without duplicates. The one place squad
- `src/teams/types.ts`  function filledSlots  (squad) => number  — How many starting slots are filled — what the lineup editor's "ready" state reads.
- `src/teams/types.ts`  function squadIsValid  (squad, squadSize) => boolean  — Whether a squad is fieldable: every slot filled, and nobody named twice. Duplicates are the
- `src/teams/types.ts`  function newSquad  (teamId, sportId, now?) => Squad

## src/ui/components/

- `src/ui/components/athlete-card.ts`  interface CardSport  { id, displayName, tables, playable }  — One entry in the sport switcher. The screen supplies these; the card knows no sport ids.
- `src/ui/components/athlete-card.ts`  interface AthleteCardOptions  { athlete, sports, sportId, onSportChange, locale }
- `src/ui/components/athlete-card.ts`  function cardOverall  (athlete, sport) => SportOverall  — The number on the compact card. The arithmetic is `derivation.sportOverall`; this only unwraps a
- `src/ui/components/athlete-card.ts`  function athleteCardCompact  (doc, options) => HTMLElement  — Portrait, name, rarity frame, overall, familiarity ring, position chip (`10` §6).
- `src/ui/components/athlete-card.ts`  function athleteCardFull  (doc, options) => HTMLElement  — The full card `10` §6 describes, sport switcher and all.
- `src/ui/components/athlete-card.ts`  function initials  (name) => string
- `src/ui/components/athlete-card.ts`  function physicalDescription  (athlete) => string
- `src/ui/components/athlete-card.ts`  function compareRow  (doc, athlete, sport) => HTMLElement  — The compare view's row: this athlete's overall in one sport (`10` §6, T-3.9 builds on it).

- `src/ui/components/attribute-sliders.ts`  interface AttributeSlidersOptions  { initial, onChange }
- `src/ui/components/attribute-sliders.ts`  interface AttributeSlidersHandle  { element, setAttributes }
- `src/ui/components/attribute-sliders.ts`  function attributeSliders  (doc, options) => AttributeSlidersHandle

- `src/ui/components/button.ts`  type ButtonVariant  = 'primary' | 'secondary' | 'ghost' | 'destructive' | 'icon'
- `src/ui/components/button.ts`  type ButtonSize  = 'regular' | 'large'
- `src/ui/components/button.ts`  interface ButtonOptions  { label, variant, size, disabled, loading, icon, href, onClick }
- `src/ui/components/button.ts`  function button  (doc, options) => HTMLElement

- `src/ui/components/controls.ts`  interface SegmentOption  { value, label, disabled }
- `src/ui/components/controls.ts`  interface SegmentedOptions  { legend, name, options, value, onChange }
- `src/ui/components/controls.ts`  function segmented  (doc, options) => HTMLElement
- `src/ui/components/controls.ts`  interface SwitchOptions  { label, checked, disabled, description, onChange }
- `src/ui/components/controls.ts`  function switchControl  (doc, options) => HTMLElement

- `src/ui/components/crest.ts`  type CrestOutline  = | { readonly kind: 'path'; readonly d: string } | { readonly ki…  — The outline every crest is drawn (and, for the patterned crests, clipped) inside.
- `src/ui/components/crest.ts`  type CrestMark  = | { readonly kind: 'rect'; readonly x: number; readonly y: numb…  — A secondary-coloured shape drawn inside the outline, for the crests built from two fields.
- `src/ui/components/crest.ts`  interface CrestGeometry  { outline, marks }
- `src/ui/components/crest.ts`  const CREST_GEOMETRY  : Readonly<Record<CrestId, CrestGeometry>>  — Every crest, keyed by id, as plain geometry — testable with no DOM.
- `src/ui/components/crest.ts`  function crestLabel  (crestId) => string  — `shield` → `"Shield"`, for accessible names and picker labels.
- `src/ui/components/crest.ts`  interface CrestOptions  { crestId, colours, size, label }
- `src/ui/components/crest.ts`  function crest  (doc, options) => SVGElement  — Renders one crest as an inline, self-contained SVG (`07` D-04 — no artwork fetched or shipped).
- `src/ui/components/crest.ts`  const ALL_CREST_IDS  — All eight ids, in the order the crest picker offers them. Re-exported for convenience.

- `src/ui/components/feedback.ts`  interface DialogAction  { label, variant, onSelect }
- `src/ui/components/feedback.ts`  interface DialogOptions  { title, body, actions, dismissible, onDismiss }
- `src/ui/components/feedback.ts`  function dialog  (doc, options) => HTMLDialogElement  — Returns a `<dialog>`. The caller appends it and calls `showModal()`; keeping open/close with
- `src/ui/components/feedback.ts`  type SheetHeight  = 'half' | 'full'
- `src/ui/components/feedback.ts`  interface SheetOptions  { title, height, children, onClose }
- `src/ui/components/feedback.ts`  function sheet  (doc, options) => HTMLElement
- `src/ui/components/feedback.ts`  type ToastTone  = 'neutral' | 'success' | 'warning' | 'danger'
- `src/ui/components/feedback.ts`  interface ToastOptions  { message, tone, action }
- `src/ui/components/feedback.ts`  function toast  (doc, options) => HTMLElement
- `src/ui/components/feedback.ts`  interface BannerAction  { label, variant, onSelect }
- `src/ui/components/feedback.ts`  interface BannerOptions  { message, tone, actions }
- `src/ui/components/feedback.ts`  function banner  (doc, options) => HTMLElement  — The bottom banner. `11` §4: the update prompt is a banner, never a modal, and never during a

- `src/ui/components/meters.ts`  interface RatingBarOptions  { label, value, tone, hideValue }
- `src/ui/components/meters.ts`  function ratingBar  (doc, options) => HTMLElement
- `src/ui/components/meters.ts`  interface ProgressBarOptions  { label, value, valueText }
- `src/ui/components/meters.ts`  function progressBar  (doc, options) => HTMLElement
- `src/ui/components/meters.ts`  const FAMILIARITY_RANKS  — Plain-language familiarity ranks (`10` §6). The ring never shows a bare number alone.
- `src/ui/components/meters.ts`  type FamiliarityRank  = (typeof FAMILIARITY_RANKS)[number]
- `src/ui/components/meters.ts`  function familiarityRank  (value) => FamiliarityRank  — Maps a 0–1 familiarity to its rank. Boundaries are even fifths.
- `src/ui/components/meters.ts`  interface FamiliarityRingOptions  { value, sport, size, rank }
- `src/ui/components/meters.ts`  function familiarityRing  (doc, options) => HTMLElement
- `src/ui/components/meters.ts`  interface StarRatingOptions  { value, max, label }
- `src/ui/components/meters.ts`  function starRating  (doc, options) => HTMLElement
- `src/ui/components/meters.ts`  interface CoinPillOptions  { amount, signed }
- `src/ui/components/meters.ts`  function coinPill  (doc, options) => HTMLElement

- `src/ui/components/play-call.ts`  interface CallChoice  { call, target }  — What the sheet hands back once a choice is complete.
- `src/ui/components/play-call.ts`  interface CallSheetOptions  { calls, squad, opponentLastCall, hidden, onChoose }
- `src/ui/components/play-call.ts`  interface CallSheetHandle  { element, reset, pending }
- `src/ui/components/play-call.ts`  function readableRating  (id) => string  — `threePoint` → `Three point`. The catalogue stores rating ids; a card shows words.
- `src/ui/components/play-call.ts`  function callSheet  (doc, options) => CallSheetHandle  — The call sheet. Returns a handle rather than a bare element so the turn screen can send it back

- `src/ui/components/states.ts`  interface StateAction  { label, variant, href, onSelect }
- `src/ui/components/states.ts`  interface EmptyStateOptions  { heading, body, action }
- `src/ui/components/states.ts`  function emptyState  (doc, options) => HTMLElement
- `src/ui/components/states.ts`  interface ErrorStateOptions  { heading, body, action, detail }
- `src/ui/components/states.ts`  function errorState  (doc, options) => HTMLElement
- `src/ui/components/states.ts`  interface SkeletonOptions  { lines, label }
- `src/ui/components/states.ts`  function skeleton  (doc, options?) => HTMLElement

## src/ui/

- `src/ui/dom.ts`  type Child  = Node | string | null | undefined | false
- `src/ui/dom.ts`  interface ElementOptions  { class, text, attrs, dataset, on, children }
- `src/ui/dom.ts`  function el  (doc, tag, options?) => HTMLElementTagNameMap[K]  — Creates an element. Attributes whose value is `null`, `undefined`, or `false` are skipped.
- `src/ui/dom.ts`  function svg  (doc, tag, attrs?, children?) => SVGElement  — Creates an SVG element in the right namespace, which `createElement` cannot do.
- `src/ui/dom.ts`  function append  (parent, children) => void  — Appends children, skipping the falsy ones so `cond && node` reads naturally at call sites.
- `src/ui/dom.ts`  function clamp01  (value) => number  — Clamps to `[0, 1]`, treating `NaN` as 0 — meters must never render a nonsense width.
- `src/ui/dom.ts`  function percent  (value) => string  — Formats a fraction as a whole-number percentage string for CSS and for `aria-valuetext`.

## src/ui/gallery/

- `src/ui/gallery/gallery.ts`  function galleryScreen  () => Screen

## src/ui/screens/

- `src/ui/screens/app-updates.ts`  function appUpdatesScreen  () => Screen

- `src/ui/screens/arcade-game.ts`  function arcadeGameScreen  () => Screen

- `src/ui/screens/arcade.ts`  function arcadeScreen  () => Screen

- `src/ui/screens/assists.ts`  function strengthOf  (amount) => StrengthName  — The named strength nearest a stored amount — old builds and hand-edited storage both land here.
- `src/ui/screens/assists.ts`  function windowOf  (amount) => WindowName
- `src/ui/screens/assists.ts`  function bonusText  (assists) => string  — The sentence under the switches: whether the bonus is live, and what it is worth.
- `src/ui/screens/assists.ts`  function assistsScreen  () => Screen

- `src/ui/screens/athlete-compare.ts`  interface SportComparison  { sport, current, potential, position, familiarity, band, unplayed, matchesToCap }  — What a sport looks like for one athlete, ready to render.
- `src/ui/screens/athlete-compare.ts`  function compareSports  (athlete, sports) => SportComparison[]  — Every sport, best *potential* first after the athlete's own.
- `src/ui/screens/athlete-compare.ts`  function standoutRatings  (athlete, sport, limit?) => string[]  — The two or three ratings this athlete is best at in a sport — why they suit it (US-5.4).
- `src/ui/screens/athlete-compare.ts`  function comparisonRow  (doc, athlete, comparison) => HTMLElement
- `src/ui/screens/athlete-compare.ts`  function describeGap  (comparison, gap) => string  — The sentence under the bars. Says which number is real and what it would take to close the gap.
- `src/ui/screens/athlete-compare.ts`  function athleteCompareScreen  () => Screen

- `src/ui/screens/athlete-editor.ts`  function athleteEditorScreen  () => Screen

- `src/ui/screens/athlete.ts`  function athleteScreen  () => Screen

- `src/ui/screens/backup.ts`  function backupScreen  () => Screen

- `src/ui/screens/home.ts`  function homeScreen  () => Screen

- `src/ui/screens/lineup.ts`  function lineupScreen  () => Screen

- `src/ui/screens/placeholder.ts`  interface PlaceholderSpec  { heading, body, arrivesIn }
- `src/ui/screens/placeholder.ts`  function placeholderScreen  (spec) => Screen

- `src/ui/screens/play.ts`  function playScreen  () => Screen

- `src/ui/screens/playbook-match.ts`  function playbookMatchScreen  () => Screen
- `src/ui/screens/playbook-match.ts`  function periodLabel  (periodName, period) => string  — `Q3`, `H2` — the sport's own period name, initialled to fit a scoreboard.
- `src/ui/screens/playbook-match.ts`  function clockText  (gameSeconds) => string  — `M:SS`, as a scoreboard shows it.

- `src/ui/screens/playbook.ts`  interface PlaybookSetupChoice  { sport, difficulty, keyMoments, speed, hotSeat }  — What the setup screen collects, and what the match screen reads back off the URL.
- `src/ui/screens/playbook.ts`  const DEFAULT_SETUP  : PlaybookSetupChoice
- `src/ui/screens/playbook.ts`  function setupParams  (choice) => Record<string, string>  — Encodes the choice as router query parameters, so a match is a link and the back button works.
- `src/ui/screens/playbook.ts`  function readSetup  (query) => PlaybookSetupChoice  — Reads it back, falling back to the defaults for anything a newer build wrote.
- `src/ui/screens/playbook.ts`  function splitRoster  (roster, size) => { home: readonly Athlete[]; away: readonly Athlete[] } | null  — Enough athletes for two sides, or `null` with the reason.
- `src/ui/screens/playbook.ts`  function playbookScreen  () => Screen

- `src/ui/screens/roster-import.ts`  function rosterImportScreen  () => Screen

- `src/ui/screens/roster.ts`  function rosterScreen  () => Screen

- `src/ui/screens/settings.ts`  function settingsScreen  () => Screen

- `src/ui/screens/team-editor.ts`  function teamEditorScreen  () => Screen

- `src/ui/screens/teams.ts`  function teamsScreen  () => Screen
